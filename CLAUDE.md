# silly-kicks

Maintained fork of socceraction — SPADL event conversion + VAEP action valuation for soccer analytics.

## Architecture

- **Hexagonal**: All core functions are pure (pandas in, pandas out). Zero I/O, zero global state mutation.
- **Converters** (`spadl/`): Each provider (StatsBomb, Opta, Wyscout, Sportec, Metrica, Gradient Sports, SkillCorner, plus the Kloppy gateway) has its own module. All return `tuple[pd.DataFrame, ConversionReport]` with guaranteed columns/dtypes via `_finalize_output()`. **Gradient Sports goal-capture (4.13.0, ADR-018):** own goals = `RE`+`shotOutcomeType=="G"` → `bad_touch`+`owngoal` (conceding team/rebounder kept; post-LTR own-half geometry tripwire warns+reverts a mis-located one); cross-goals = `CR`+`G` → keep the cross + synthesize a `shot`/`shot_freekick`+`success` by the crosser (foul-synth 0.5-offset pattern); synthesized rows (cross-goal shot + foul) carry `is_synthetic=True` (new GS schema column) since they share the parent's `original_event_id`; voided `possessionEvents.nonEvent==True` events are excluded (optional column — absent → observable no-op warn, tallied in `excluded_counts`). `"O"` shot-outcome is off-target → `fail` (4.12.2, not own-goal). Empirically grounded in the full WC2022 catalog; owner-gated e2e in `tests/spadl/test_gradientsports_scoreline_e2e.py`. **GS null-actor team attribution (4.25.0, ADR-027):** the null-actor duel/foul events (594 `OTB`+`CH` + 28 `FOUL`+`FO` on the canonical WC2022 feed — both `gameEvents.teamId` AND `playerId` NULL; a CH is a two-sided 50/50 duel) carry NaN `team_id`/`player_id` (GS schema ids are now nullable `Int64`, NOT the `SPADL_COLUMNS` `int64`), NEVER a sentinel `0` (non-NaN → bypassed `pd.isna` routing → crashed the lakehouse opponent guard `_resolve_opponent_team_id`). ADR-001 forbids back-filling from duel/foul qualifiers; an ADR-001-legal canonical-player self-heal (`_resolve_team_ids`) fills team ONLY from a real same-match `player_id` (ambiguity raises). `convert_to_atomic` now preserves source id dtype (was force-casting to atomic `int64` — latent crash for GS NaN AND kloppy-family object strings). VAEP/tracking-retrain trigger. **GS dribble end-derivation (4.49.0, ADR-018 amendment, PR-S116):** every GS `OTB`+`BC` carry shipped a placeholder `end == start` (verified 850/850 zero-displacement on the live corpus) — GS initializes `end=start` for every event, never runs `_add_dribbles`, and the shared `_DERIVE_END_TYPE_IDS` excludes dribble. Fixed **GS-locally**: `_derive_end_coordinates` gains keyword-only `extra_type_ids` and ONLY gradientsports passes `{dribble}` (the shared set is untouched — its `placeholder_end` guard cannot distinguish statsbomb's ~11% genuine stationary carries from placeholders). Period-last carries honestly keep the placeholder; owner-gated e2e asserts >90% of real WC2022 dribbles derive an end. **GS-only retrain trigger** (xT/xtgk move-sets include dribbles; VAEP consumes dribble ends); precursor to TF-49 packing (PR-S117). **Provider data-quality fixes (4.20.1):** (1) **SkillCorner `time_seconds` is period-relative** — `_parse_time_start` parsed the continuous broadcast clock; `_to_period_relative` now subtracts period-start offsets `{1:0,2:2700,3:5400,4:6300,5:7200}` (2nd-half events were ~2700–5800s vs period-relative frames → linkage collapse; ADR-017). (2) **SkillCorner goalkick result** no longer hard-wired `success` → routed through `same_team_next`. (3) **sportec pass/set-piece result from native DFL `play_evaluation`** — it previously marked ALL passes/set-pieces/goalkicks success, ignoring the parsed `play_evaluation`; the punt-synth goalkick inherits its parent Play's eval. **4.21.3 generalized this to a kloppy-aligned success-ALLOWLIST** (`silly_kicks/spadl/sportec.py` `_extract_play_eval`/`_play_evaluation_is_fail`/`_warn_unexpected_play_eval`, single-sourced across the main + synth sites): `fail` iff the Evaluation is non-empty AND ∉ `{successfullyCompleted, successful}`, so unseen reason-coded failures (`unsuccessfulBecauseOfFoul`) fail by construction; empty/absent/NULL → success (no mass-fail on a missing column); unexpected tokens warn. Byte-identical on the 7 IDSSE matches (only non-success token is `unsuccessful`; owner-gated bronze e2e `tests/spadl/test_sportec_playeval_e2e.py`). Hyrum: a DFL stream with failure tokens beyond `unsuccessful` shifts its fail distribution. (4) **metrica left unchanged** (measured-correct: PASS = completed pass, loss = separate `BALL LOST` event). (5) **Frame-orientation `home_team_id` dtype bug (ADR-019)** — `gradientsports.py`/`sportec.py` tracking adapters set `team_attacking_direction` via a raw `team_id == home_team_id` that silently matched ZERO players for an int `home_team_id` vs object-string `team_id` → mislabeled → `play_left_to_right` double-flip → mis-oriented frames (the `structural_sgm` away-blow-up ROOT CAUSE + latent corruption of every frame-linked tracking feature on dtype-mismatch); both now use `ids_match` + **fail loud** on zero-match (kloppy gateway unaffected — derives home_team_id internally as string). SkillCorner + sportec + orientation changes are **VAEP/tracking-retrain triggers**. DFL goalkicks ~71% complete (not 100%). **Restart-coordinate enrichment (4.22.0, ADR-025):** post-conversion `spadl.add_restart_coordinates(actions, *, frames=None, links=None)` imputes NaN restart coords for Law-fixed-spot types (goalkick/penalty/corner/throw-in) as NEW provenance-tagged columns (`enriched_start_x/_y`/`enriched_end_x/_y` + `start_coord_source`/`end_coord_source` ∈ `{native, tracking_ball, tracking_gk, restart_prior, next_event, unresolved, tripwire_reverted}` + `start_coord_confidence`/`end_coord_confidence`), NEVER mutating canonical `start_x/end_x` (Phase 1, additive — **no retrain**; canonical promotion is deferred Phase 2). Single engine `tracking.resolve_restart_geometry` (`impute_types` gate); `tracking.resolve_gk_geometry` is now a byte-identical shim over it (`impute_types=(goalkick,)`, golden-snapshot-gated → xT-GK/completion unchanged). Geometry tripwire (ADR-018-style) lives at the `add_restart_coordinates` EDGE (engine is pure); reverts an imputed-out-of-region origin → `tripwire_reverted` (warns), native out-of-region warns only. `tracking.RestartCoordinateReport` aggregate QA. NaN coords are a GS set-piece phenomenon (StatsBomb/Wyscout/SkillCorner 0%; live-probe-grounded). C4-free (spadl helper, not a tracking aggregator — count stays 27).
- **Tracking** (`tracking/`): Per-frame namespace parallel to `spadl/`. 20-column long-form schema (`TRACKING_FRAMES_COLUMNS`); native Sportec + Gradient Sports adapters; kloppy gateway for Metrica + SkillCorner. Linkage primitive (`link_actions_to_frames` + `slice_around_event`) joins SPADL actions to frames; pointer-DataFrame return + `LinkReport` audit (ADR-004). **`time_seconds` is period-relative (resets to 0 each period — NOT absolute match-clock); ADR-017 documents + enforces this contract:** `link_actions_to_frames` gains a per-period `min_link_rate`/`on_low_coverage` (warn-default) coverage guard + `LinkReport.per_period_link_rate` (computed from the internal per-period merge, never laundered behind a healthy period); public `validate_time_base` + `TimeBaseDiagnosis` are the pre-link guard for time-filtering consumers (the linker can't see pre-filtered-out actions); `MISMATCH_OVERLAP_FLOOR=0.2` time-base-mismatch diagnostic decoupled from the link-rate symptom; convention pinned by Opta/StatsBomb lock tests (GS time-base guarded lakehouse-side). PR-S26 adds `is_goalkeeper_source` column (values `"native"` / `"derived"`) + `_gk_identification.derive_goalkeepers` B+ filtered algorithm (always-run with agreement-based source resolution) fixing Metrica/SkillCorner 21–50%→100% GK detection; `TrackingConversionReport` extended with `n_teams_gk_derived` + `derived_gk_picks` audit fields. **SkillCorner derived-GK is Tier-1 external-roster validated (PR-S86, 4.19.1: 20/20 team-GKs vs the pining `match.json` roster across 10 matches, `tests/tracking/test_gk_skillcorner_roster_e2e.py`); Metrica stays Tier-2 (anonymized public data, no roster).** Decision: ADR-007. Tracking-aware features ship in `tracking.features`: PR-S20's 4 `action_context` features + `add_action_context` aggregator + `tracking_default_xfns`; PR-S21's 4 `pre_shot_gk_*` features + `add_pre_shot_gk_position` aggregator + `pre_shot_gk_default_xfns`; PR-S24's 2 `pre_shot_gk_angle_*` features (`to_shot_trajectory` + `off_goal_line`, both signed) + `add_pre_shot_gk_angle` aggregator + `pre_shot_gk_angle_default_xfns` + `pre_shot_gk_full_default_xfns` union list. PR-S24 also ships `silly_kicks.tracking.preprocess` (Savitzky-Golay / EMA `smooth_frames` + linear `interpolate_frames` + SG-derivative `derive_velocities`; shared `PreprocessConfig` dataclass with flag-based `is_default()` + per-provider defaults from a codegen-from-JSON pipeline; tracking converters take optional `preprocess: PreprocessConfig | None = None` kwarg with auto-promotion to `for_provider(self.provider_name)`) and `silly_kicks.tracking.utils.sync_score` / `add_sync_score` / `LinkReport.sync_scores()` per-action sync-quality (TF-6). Events-side `silly_kicks.spadl.utils.add_pre_shot_gk_context(*, frames=None)` lazy-imports the GK position + angle aggregators when frames are supplied (per ADR-005 § 5; emits 6 cols when `frames=...`, 4 cols when `frames=None` — backcompat). Mirror atomic-SPADL surface in `atomic.tracking.features`; VAEP integration via composition (`compute_features(*, frames=None)` + `_frame_aware` xfn marker dispatch). Decision: ADR-005. PR-S25 adds 2 `actor_*_pre_window` features (`arc_length` + `displacement`, TF-3) + `add_actor_pre_window` + multi-flavor `pressure_on_actor` (TF-2; methods `andrienko_oval` (default) / `link_zones` / `bekkers_pi` from `silly_kicks.tracking.pressure`'s frozen-dataclass param surface) + `add_pressure_on_actor` umbrella + ADR-005 §8 amendment codifying `<feature>__<method>` column-naming convention. PR-S31 ships TF-7 pitch control subpackage (`tracking/pitch_control/`: Spearman/Fernandez-Bornn/Voronoi three-flavor dispatch, `PitchControlSurface` frozen return type, optional numba acceleration, action-coupled `pitch_control_at_target` (renamed from `pitch_control_at_action` in 4.32.0/ADR-033) + `add_pitch_control` + `pitch_control_xfns` factory). Decision: ADR-008. GKDV research program TF-15..TF-19 now unblocked. PR-S27 ships TF-13 `defending_gk_from_frames` (frame-based GK resolution fallback, ~120 LOC in `_gk_resolve.py`; **4.39.0/PR-S106 adds the mirror `acting_gk_from_frames`** — acting-team GK per action via a shared predicate-parameterized `_gk_from_frames_linked` body (defending byte-identical) + an **identity fallback** resolving the roster-stable `is_goalkeeper` identity when the keeper is undetected at the linked goal-kick frame + GK-sub nearest-in-time; pure `player_id` resolver, no action mutation, enables the lakehouse goal-kick actor-override; **4.43.0/PR-S110 adds the public `gk_distribution_mask(actions, frames=None, *, resolve_gk="robust")`** in `_gk_resolve.py` — per-action GK-distribution domain (goal-kick OR pass/throw-in by the acting GK), frame-optional (`frames=None`→goal-kicks-only), `resolve_gk` lever: `"native"` = global `frames[is_goalkeeper]` set-membership, `"robust"` (default) = `acting_gk_from_frames` time-accurate resolution (`robust ⊆ native` — tightens stale/substituted keepers, never broadens; the resolver the lakehouse pins for its goal-kick taker override). The frozen v1 `_gk_distribution_mask` (`_xt_gk.py`) is now a **byte-identical shim** over it (`resolve_gk="native"`, golden-gated). Lakehouse contract: `fct_action_context.is_gk_distribution = gk_distribution_mask(..., "robust")`; the ρ retention loader/trainer (`scripts/_loader_databricks.py`/`train_gk_retention.py`) drop the misused shot-scoped `gk_was_distributing` (the `add_pre_shot_gk_context` feature is unchanged) for a self-adapting, NULL-coalesced `is_gk_distribution` domain (ADR-036 amendment; additive, no retrain)) + TF-14 `compute_defensive_line` (per-frame 6-column back-line geometry in `_defensive_line.py`) + action-coupled 6 per-Series features + `add_defensive_line` aggregator + `defensive_line_xfns` factory (batch kernel via `_row_idx` positional pattern for gamestates compatibility). PR-S28 ships TF-5 `infer_ball_carrier` (per-frame ball-carrier inference with composite distance + velocity scoring + hysteresis in `_ball_carrier.py`) + `ball_carrier_at_action` action-coupled wrapper; also adds `game_id` to `compute_defensive_line` groupby/return schema for multi-game batch consistency. PR-S30 ships TF-4 off-ball runs + line-break detection (4 off-ball-run + 2 line-break action-coupled columns in `_off_ball_runs.py`) + `add_off_ball_runs` / `add_line_break` / `add_off_ball_context` aggregators + `off_ball_context_xfns` VAEP factory. PR-S32 ships TF-28 DAS adapter (`_das.py`: `get_das` / `get_individual_das` / `get_xc` wrappers over `accessible-space` PyPI package; `das_at_action` + `add_das` + `das_xfns` single-pass VAEP integration; `derive_team_in_possession` general helper in `_ball_carrier.py`). PR-S33 ships TF-31 team shape envelope (`_team_shape.py`: `compute_team_shape` per-frame 7-metric primitive; `add_team_shape` 14-column aggregator; `team_shape_xfns` 36-column VAEP factory) + TF-32 Ward-clustering line-breaking (`_line_breaking.py`: `detect_line_breaking` 3-column Ward clustering + straddle test; `add_line_break` extended with `method="ward"` dispatch; `line_breaking_ward_xfns` 9-column VAEP factory).
PR-S34 ships TF-15 GK influence primitives (GKDV Layer 1, `_gk_influence.py`: `compute_gk_influence` per-frame entry point with 3 primitives — threat-weighted pitch control share, uniquely reachable area, zone closing time; `Zone`/`GkInfluence`/`ZoneClosingTime` frozen dataclasses; `add_gk_influence` aggregator; `gk_influence_xfns` VAEP factory with frame-precomputation cache; 4 per-Series helpers; atomic mirror). Also ships prerequisite extractions (`compute_tti` public export, `select_back_line_players` extraction) and bundled TF-32 fixes (H1 joint dropna, H2 extension-poisoning, M4 non-pass filtering).
PR-S36 ships TF-30 cover shadow features (`_cover_shadows.py`: `CoverShadowParams`/`LaneControlResult`/`BlockingScoreResult` frozen dataclasses; `ball_drag_time` Spearman 2017 drag model; `player_tti` 3-phase TTI; `lane_control` corridor-discretized blocking; `compute_blocking_score` Voronoi counterfactual; `add_cover_shadows` 5-column aggregator; `cover_shadow_xfns` 15-column VAEP factory; atomic mirror).
PR-S51 ships TF-36 + TF-33 per-player influence primitives (`_player_influence.py`: `PlayerInfluence` frozen dataclass; `compute_player_influence` per-frame off-ball xT via PC share × xT + uniquely reachable area for all outfield; `add_player_influence` 7-column aggregator with `_team`/`_opponent`/`_diff` convention; `player_influence_xfns` 21-column VAEP factory; 5 per-Series helpers; atomic mirror).
PR-S52 ships TF-18 ghost-GK positioning model (GKDV Layer 2, `_ghost_gk.py`: `GhostGkModel` RFCDE density estimation via HistGradientBoostingRegressor leaf co-occurrence + weighted 2D KDE; `GhostGkDensity` frozen dataclass 60×64 grid; `extract_ghost_gk_features` 26-feature goal-relative extractor; `compute_ghost_gk` batched per-frame primitive; `add_ghost_gk` action-coupled aggregator (no provenance leak); `ghost_gk_xfns` 9-column VAEP factory; vectorized numpy tree traversal (no sklearn at inference); npz+JSON+SHA256 serialization (no pickle); two bundled model variants `"default"` (9 MB, 36k samples) / `"full"` (91 MB, 537k samples) via `GhostGkVariant` Literal type; `model="default"|"full"|GhostGkModel|None` on `compute_ghost_gk`/`add_ghost_gk`; atomic mirror). **[SUPERSEDED by 4.54.0/ADR-044:** distributed artifacts are now **parameters-only** — `ghost_gk_xfns` is a **6**-column factory, `default` is **~0.76 MB**, and `predict_density`/`ghost_gk_density_spread` are retired from the served surface — see the parameters-only bullet below.]
PR-S55 ships TF-18 training pipeline (`prepare_ghost_gk_training_data` public API with match context resolution via `_build_score_lookup`/`_build_phase_lookup`, shared `_extract_all_ghost_gk_features` batch helper, label domain filtering; `compute_ghost_gk` + `add_ghost_gk` now accept optional `actions`/`actions_for_context` for score+phase enrichment; `scripts/train_ghost_gk.py` StratifiedGroupKFold CV + permutation importance + metrics.json + feature extraction disk cache + `predict_mean()` fast path).
PR-S81 (4.10.0) ships the Ghost-GK serve-carrier fix + R3 + 4.7.0 re-fit: `compute_ghost_gk` now computes the ball-carrier on the FULL frames (fixing `team_in_possession` always-0-at-serve train/serve skew) with the model's recorded `carrier_params`; `GhostGkModel` records+consumes `carrier_params` in `metadata.json` (model version 1.1.0, R3 — mirrors xS) + `sklearn_version`/`training_commit`/`training_platform`; `compute_ghost_gk`/`add_ghost_gk`/`ghost_gk_xfns` gain an optional `carrier=` passthrough; `prepare_ghost_gk_training_data` gains additive `carrier_params=`. Bundled `default` + Hub `full` re-fit on 81 pining matches (887k samples) vs the 4.7.0 carrier defaults; serve-output change measured at 0.4% of frames / max ~4 m (long-tail, Hyrum-flagged for lakehouse).
PR-S57 ships library extraction TF-39..TF-44: TF-39 Shape Graph (`_shape_graph.py`: Sotudeh 2026 iterative Delaunay edge-removal + 5×5 face-center position decomposition; `compute_shape_graph`/`infer_positions`/`ShapeGraph`/`PositionLabel`; `add_shape_graph` aggregator; `shape_graph_xfns` 36-column VAEP factory); TF-40 OBSO (`_obso.py`: `compute_obso_surface`/`compute_pass_obso`/`ObsoSurface`/`ObsoParams`; `add_obso` aggregator; `obso_xfns` 9-column VAEP factory); TF-41 Space Creation (`_space_creation.py`: `compute_space_created`/`SpaceCreationParams` + `include_opponent_perspective` defender-side LOO on the opponent-attacking OBSO surface (4.23.0 lakehouse-mandated, ADR-026; **4.24.0 round-2 fix: the opponent surface is weighed by the x-MIRRORED transition/EPV artifacts** — the 4.23.0 shared-unmirrored multiplier made the complement LOO the exact pointwise negation of the team LOO (`opp_destroyed ≡ team_created`, informationally empty); complement decomposition = zero extra PC cost analytical / explicit opponent PC naive, BOTH on the mirrored multiplier; two-team loud guard, NaN-mask parity. **4.24.0 LEAN 2-COLUMN CONTRACT (owner-approved breaking): `space_created_m2` (attack) + `space_denied_m2_opponent` (rest-defense) ONLY — the monotone-LOO structural zeros (team destroyed, opponent created) and the redundant nets were RETIRED; guard test blocks resurrection**); `add_space_creation` 2-column aggregator; `space_creation_xfns` 6-column VAEP factory); TF-42 PAUSA (`_pausa.py`: `compute_pausa`/`compute_pausa_batch`; `add_pausa` aggregator; `pausa_xfns` 9-column VAEP factory); TF-43 ELASTIC (`_elastic_sync.py`: `align_events_to_frames`/`extract_ball_features`/`ElasticSyncParams`; `add_elastic_sync` aggregator; `elastic_sync_xfns` 6-column VAEP factory); TF-44 Ward inter-line gaps (`_team_shape.py` enhancement: `defensive_line_height`/`inter_line_gap_1`/`inter_line_gap_2` via Ward clustering; `team_shape_xfns` 36→54 columns).
PR-S75 ships TF-16 xShotOccurrence (xS, GKDV Layer 2, `_xshot_occurrence.py`: `XShotOccurrenceModel` deterministic-XGBoost shot-occurrence classifier — P(shot attempted by the in-possession team within ~1 s of a frame); paper-faithful 27-feature `extract_xshot_features` (ball r/θ/z/speed, `openGoal` goal-mouth obstruction, GK dist/bearing, 5 nearest defenders + 5 nearest attackers) in goal-relative coords via the shared `_geometry` helper; `build_xshot_labels` time-windowed labels; `prepare_xshot_training_data` shared train/serve entry point with alive-ball + attacking-third domain filter (always returns the faithful class distribution; negative subsampling lives in the standalone train-only `subsample_negatives` helper); `compute_xshot_occurrence` / `add_xshot_occurrence` / `xshot_occurrence_xfns` ADR-005 surfaces; pickle-free booster-JSON + SHA256 serialization; `XShotFeatureSet` Literal (`"extended"` reserved); ruthless HPO objective (`_xshot_occurrence_objective.py`) + `scripts/train_xshot_occurrence.py`; atomic mirror. **Trained weights bundled 4.9.0 (PR-S80):** a single `public` variant (skillcorner+idsse) trained on 81 matches against the 4.7.0 carrier defaults — the pre-registered `public`-vs-`full` two-candidate test found owner-tier Gradient Sports data degraded public-held-out PR-AUC in all 5 folds, so the reproducible public-only model shipped (`from_variant("default")`/`from_hub`; wired into `pre_shot_gk_full_default_xfns` only). Decision: ADR-011 (trained-model lifecycle); attribution arXiv:2512.00203.
PR-S82 ships TF-17 xCrossAttempt (xCross, GKDV Layer 2, `_xcross_attempt.py`: `XCrossAttemptModel` deterministic-XGBoost cross-attempt-propensity classifier — **STATE-anchored** P(the in-possession team attempts a cross within ~1 s of a frame), reframed from the Cao et al. (arXiv:2505.11841) sender-level event treatment; **7 of the paper's 8 confounders** (crosser-position #7 omitted — no faithful tracking-only proxy) **+ a novel, isolatable GK-position confounder block** (the paper's headline gap) in goal-relative coords via the shared `_geometry` helper; shared `_build_occurrence_labels` (`_occurrence_labels.py`; `build_xshot_labels` refactored onto it, bit-identical); `prepare_xcross_training_data` shared train/serve entry point with alive-ball + **wide-area** domain filter + optional `actions=`-supplied `score_differential` (reuses ghost-GK's `_build_score_lookup`, NaN-tolerant); `compute_xcross_attempt` / `add_xcross_attempt` / `xcross_attempt_xfns` ADR-005 surfaces; pickle-free booster-JSON + SHA256 serialization; `XCrossFeatureSet` Literal (`"extended"` reserved); ruthless HPO objective (`_xcross_attempt_objective.py`) + `scripts/train_xcross_attempt.py`; atomic mirror. **WEIGHTS shipped 4.18.0 (PR-B, PR-S85):** bundled `public` model (skillcorner+idsse) trained on the clean-4.13.0-GS 81-match pining corpus vs the 4.7.0 carrier defaults; pre-registered public-vs-full paired test → public-only (`full` degraded public held-out in all 5 folds, mirrors xS); `from_variant("default")`/`from_hub` live; `xcross_attempt_xfns` wired into `pre_shot_gk_full_default_xfns` (+atomic) only. Shipped-surface GK validation lives in the private `_xcross_eval.py` (`gk_block_ablation` + `gk_substitution_probe` + CV-held-out `permutation_importance_report` + shared `_cv_score` the trainer's `_cv_metrics` delegates to); the TF-19-viability gate is the substitution probe alone against a pre-registered `ratio>=2.0 AND abs-floor>=0.01` rule — result **`tf19_ready=False`** (GK carries relative signal, 2.6× the nearest-defender control, but below the absolute floor → TF-19 consumption gated on GK feature-engineering, never shipped silently as novelty); `score_differential` measured as the #2 feature (0.0216) on the clean GS stream. **PR-C (causal harness, ADR-015, 4.18.0) closes TF-17:** the private `silly_kicks/_causal/` port (pure numpy/sklearn, no R, no new dep) — `matching.py` (propensity-score matching: ATT/ATNT, 1:1 NN **with replacement**, no caliper, logistic PS on standardized covariates, **Abadie–Imbens 2006 matching SEs**) + `opportunities.py` (spell state-machine crosser-anchored rows; treatment `(entry, min(entry+T, spell_end)]` — fixed-`T` cap + possession-clamp; outcome strictly post-cross; 7 paper confounders, GK missing-indicator) + `scripts/validate_xcross_causal.py` (GK-vs-row-permuted-GK-placebo ablation, positivity guard, PS-overlap+SMD claim gate). REPORTED-not-gated (`docs/research/xcross_causal/`); only the known-truth `tests/causal/` method tests gate CI. **Clean all-provider result (`docs/research/xcross_causal/`): ATT_with_GK +0.0927 (SE 0.0156), 23966 opp / 669 treated, PS overlap 1.0, SMD 0.51→0.078; GK ablation shift 0.0179 < placebo p95 0.0239 → `gk_clears_placebo_band=False` — independently corroborates `tf19_ready=False`.** A latent GS bug fixed en route: `extract_xcross_features` stringified nullable-`Int64` ids via `.to_numpy().astype(str)` → `"366.0"≠"366"` → all-NaN GK/carrier features for ~83% of the corpus (all GS) + the shipped GS-inference path; fixed through the ADR-019 `canonical_id` contract (public weights train on string providers → no retrain; repairs GS inference). Decision: ADR-011 (trained-model lifecycle) + ADR-015 (causal-validation port); attribution arXiv:2505.11841. (PR-S82 = PR-A code; PR-B weights + PR-C causal harness ship together in 4.18.0 as PR-S85.)
PR-S83 (4.14.0) ships the TF-18 Ghost-GK served-estimator fix (ADR-016): `compute_ghost_gk`/`predict()`/`predict_mean` now serve the **exact sklearn HGBR boosted prediction**, reconstructed **pickle-free** as `baseline + Σ_trees leaf_value` via a new `_vectorized_leaf_values` (sibling of `_vectorized_leaf_indices`) — closing a pre-existing integrity gap (production served the KDE **mode** ~4.65 m while the card reported ~1.1 m for the `predict_mean` that `save()`/`load()` discarded → never served); clean re-fit serves **1.07 m** held-out MAE. `fit()` trains `phase` **numerically** (`categorical_features=None` — makes the numeric reconstruction match sklearn exactly AND closes a latent KDE categorical-routing capability gap) + re-adds the **gk_y** HGBR + stores both ensembles' tree nodes + `_baseline_x/y`; `predict_density` retained for `ghost_gk_density_spread` + the mode; `GhostGkDensity.central_*` + `_central_estimate` removed (the rejected Option-B leaf-weighted conditional mean — DGX-measured 7.0 m, *worse* than the mode, because the conditional density is broad/multimodal). **Breaking column rename `ghost_gk_spread` → `ghost_gk_density_spread`** (it is the conditional-**density** dispersion, NOT the served point's SE; lakehouse re-materializes). `save()/load()` serialize the gk_y trees + baselines (artifact version 1.2.0, `serve_estimator="boosted_mean"`, `load()` fail-closed on a conflicting tag AND on pre-Option-A artifacts → "re-fit required"); inference stays sklearn-free + **sklearn-version-independent** (sklearn couples only at fit/extract; a blocking parity-on-fresh-fit gate guards it). Both bundled `default` + Hub `full` **re-fit on the clean 4.13.0 GS events** (887k samples); atomic mirror. Decision: ADR-016; numpy-reconstruction chosen over skops/treelite/ONNX (no sklearn-at-inference, no new dep). **4.22.1 (ADR-016 amendment):** `compute_ghost_gk` clamps the served `ghost_gk_x/y` (goal-relative) to the physical pitch (x∈[0,105], y∈[0,68]; warns) — fires only on garbage input (e.g. upstream mis-flagged `is_goalkeeper` wrong-footing the goal-side flip → OOD extrapolation below the trained x∈[0,30] label hull); clamp lives at the serving seam, NOT in `predict_mean` (exact-boosted parity contract untouched); healthy sweeper-rush extrapolation past 30 m byte-unchanged. **[SUPERSEDED by 4.54.0/ADR-044:** `predict_density` is now available ONLY on a locally `fit()` model, NOT a loaded/distributed artifact (which is parameters-only); `ghost_gk_density_spread` is retired from `compute_ghost_gk`/`add_ghost_gk`/`ghost_gk_xfns`; artifact format is now **1.3.0** (`stores_training_data:false`). See the parameters-only bullet below.]
PR-S84 (4.16.0) ships **TF-45 structural-pass primitives** (`_structural_pass.py`: pure `_structural_pass_core` + `compute_structural_pass_metrics` per-frame + `StructuralPassParams`[`sigma=15.0` empirically tuned on 2,466 real WC2022 passes; the original "smallest σ where the inverse-density SGM is intrinsically pitch-bounded" claim was FALSIFIED on real byline-cross/fast-break frames (rho→underflow, 1/rho→~1e8) and fixed in 4.20.1 by an explicit eps-floor on rho at `exp(-4.5)≈0.0111` (3σ contribution), capping `1/rho`≈90] + `@nan_safe_enrichment add_structural_pass` emitting raw `structural_lbs`/`structural_sgm`/`structural_sdi` via the shared `_kernels._structural_pass_at_actions` + `structural_pass_xfns` VAEP factory [3×-not-9× call-count budget]; atomic mirror with `end=x+dx` synthesis; arXiv:2603.28916, ADR-005). **Library = RAW primitives only** — TIV z-norm composite + K-means(K=4) archetypes + ΔTIV/cumulative rankings stay consumer-side (frozen-exogenous, ADR-009). LBS = Line Bypass Score (defenders with `start_x < d_x ≤ end_x` in attack-positive coords; 1-D caveat), SGM = Space Gain Metric (Δ inverse-Gaussian defender density receiver−passer), SDI = Structural Disruption Index (Δ dist-from-full-team-2D-centroid); SDI uses a *fresh* centroid (NOT `compute_defensive_line`'s back-line mean), SGM a *direct* KDE (NOT `_cover_shadows`'). Receiver = pass destination `end_x/end_y` (no SPADL `receiver_player_id`). Owner-gated e2e validates real WC2022 GS. **Also fixes a systemic dup-`action_id` crash in 8 frame-aware xfns** (`pitch_control`/`obso`/`pausa`/`space_creation`/`pressure`/`cover_shadow`/`gk_influence`/`player_influence`): VAEP shifted gamestate slots repeat the boundary action → non-unique `action_id` → `set_index("action_id").at[aid]` returns a Series (raises) / provenance merges fan out. Fixed via shared `_kernels.resolve_frame_ids_by_position` (positional, byte-equivalent to the old `.at` on unique ids) + a red-first behavioral gate auto-enumerating every `*_xfns` (`tests/tracking/test_frame_aware_xfns_dup_action_id.py`, meta-asserts coverage, discriminates the bug from a fixture gap). Production `add_*` aggregator path (unique ids) unaffected; the xfns path now produces values vs crashing → **VAEP-xfns-path retrain trigger**. Decision: ADR-020 (driven by a behavioral gate, not a `.at` grep — which missed `pausa`/`pressure`).
PR-S88 (4.21.0) ships **xT-GK** (Eyestone, GKDV-distinct GK-distribution value, ADR-024): `_xt_gk.py` pure parametric compute feature (NOT trained — no ADR-011) — `compute_xt_gk`/`@nan_safe_enrichment add_xt_gk`/`xt_gk_xfns` + `XtGkParams.for_philosophy` (5 presets) + atomic mirror (`end=x+dx` synthesis). Emits raw `xt_gk_base`/`pev`/`rav`/`dzv`/`pressure` + composite `xt_gk` per GK-distribution action (goalkick / GK-actor pass / throw_in). **Tracking-required** (PEV needs `pressure_on_actor`; no pressure survives SPADL); **RAV P(success) = fitted `GkCompletionModel`** (bundled GS `default`; replaced the OOD-on-goalkicks open-play `get_xc`, ~31%→~100% coverage; `[das]` now OPTIONAL); **required injected pre-fitted `ExpectedThreat`** (no self-fit/leakage; unfitted-grid guard). **Option B (Jeffrey 2026-06-08): destination counted ONCE** — `base = −xT★(z)` origin-only, RAV owns `xT★(z')`; separate `progress` feeds PEV (else origin-only base zeroes it); composite `T·(−xT★(z) + γ·PEV + RAV) + φ·DZV`. Normative params (γ/δ/φ/η/`v_def`/`defensive_third_boundary`/`pressure_scale`) intent-set NEVER calibrated; `convolution_sigma` hand-set (no calibration phase). `_grid_value` reuses `xthreat._grid._get_cell_indexes` (pinned to `ExpectedThreat.rate`). `home_team_id` parity-only (LTR coords). In **no** default xfn list (additive — opt-in = self-triggered VAEP retrain). Construct-invariant gates (back-pass↑, def-third DZV>0, pressure→PEV). **Phase 2 (team γ/δ/φ/η estimation) deferred** (Jeffrey Q7). Presets provisional (exact values pending). Attribution: Eyestone (PtP-1); public-with-attribution + formula consent in NOTICE/ADR-024. **Goal-kick coverage (folded into 4.21.0, ADR-024 amendment):** the OOD e2e escalated (real goal-kick get_xc ~31%), so `resolve_gk_geometry` (`_gk_geometry.py`) derives the missing origin (conditional: native → in-area tracking-GK clamped `x≤16.5m` → rule-point `(5.5,34)`) + destination (native → in-period next-event, `(game_id,period_id)` boundary-guarded) WITHOUT mutating shared `actions`, emitting per-row `xt_gk_origin_source`/`xt_gk_dest_source`/`xt_gk_origin_confidence` provenance (+ aggregate `XtGkReport`); `GkCompletionModel` (`_gk_completion.py`; logistic, pure-numpy `sigmoid(Xβ)` serve, pickle-free JSON+SHA256, `from_variant("default")`/`completion=` override) bundled GS `default` trained on 30 WC2022 matches via `scripts/train_gk_completion.py` (native-origin POOLED out-of-fold green gate: AUC 0.838 CI95 [0.81,0.86], n_native 1395, Brier 0.122<0.171; density 96% finite). Density NaN→training-mean impute (neutral post-standardization); whole-row geometry-unscoreable→per-type base rate (standalone `compute_gk_completion` only — RAV NaNs unresolvable-dest rows honestly). **Train==serve parity** enforced at every producer (shared domain predicate `_gk_distribution_mask`, `resolve_gk_geometry` on FULL-then-mask, shared `_gk_completion_density`, shared `extract_gk_completion_features`). `compute_gk_completion` + `add_gk_completion` exported (`add_gk_completion` = the lakehouse wide-table aggregator: emits `gk_completion` per in-scope GK distribution, NaN out-of-scope, geom-on-full-then-mask reusing RAV's `_completion_p`; C4 action-coupled-aggregator count 26→27). **SkillCorner completion + provider-aware variants (folded into 4.21.0, ADR-024 amendment):** `spadl/skillcorner.py` `result_id` now routes through the **native** outcome — `pass_outcome`→`received==True`(success-only)→residual `same_team_next` — via `_native_completion_result`, emitting a dedicated **`result_source`** column (`native`/`inferred`/`stopgap`) in `SKILLCORNER_SPADL_COLUMNS` (the old bare `same_team_next` proxy agreed only ~0.72–0.79 with the native outcome + overstated goal-kick success ~16pp; `received==False` is NOT a fail — could be completion to a non-targeted teammate). **VAEP-retrain trigger** (SkillCorner scores/concedes shift; lakehouse re-materializes). `GkCompletionModel` gains a **provider-keyed variant registry**: pure `variant_key_for_provider` (`skillcorner`→own weights, else→`gs`) + auto-selection in `compute_xt_gk` from `frames["source_provider"]` (caller `completion=` wins; >1 real provider raises; `snapshot` excluded; missing-variant→`gs` fallback + warn; 4.22.1: `from_variant` aliases the `gs` KEY→bundled `default` DIR so `from_variant(variant_key_for_provider(p))` composes without `FileNotFoundError`). **Training is `result_source=="native"`-ONLY** (G1: `inferred` is positive-only → would bias the multiplicatively-consumed calibration; only `pass_outcome` gives both classes). Bundled **`skillcorner`** variant (10 matches): GS does NOT transfer on the corrected native label (GK-pass AUC 0.412 < chance → distinct weights), SkillCorner GK-pass **AUC 0.739 / ECE 0.036**; **goal-kicks chance (0.433) — model-scored in 4.21.0; base-rate-served as of 4.21.4** (per-type serve gate, SK-91 — see PR-S91 below). Provenance: `xt_gk_completion_variant`/`xt_gk_completion_source` + `XtGkReport.spans_multiple_variants`; cross-provider comparability gate (`scripts/_xtgk_comparability.py`, owner-run) → SC-vs-GS `xt_gk` **within tolerance** (pool directly, no re-scale; G2). No-pool-across-variants-without-comparability contract documented (ADR-024). C4: no enumeration change (a completion *variant* is not a new aggregator; count stays 27). **SkillCorner keeper-origin resolution (4.37.0, ADR-024 amendment, PR-S104):** SkillCorner broadcast tracking records a GK-distribution's native origin as the **ball-detection event location, not the keeper** (goal-kick `start_x` SD 23.2; own-box 51% vs ~100% full-tracking) — `resolve_gk_geometry` trusted it, corrupting `xt_gk` base/DZV + keeper pressure/PEV. Native-origin trust is now **provider-aware** via a FAIL-SAFE allowlist (`native_origin_is_trusted`: unknown/None/future → distrust; only `gradientsports`/`idsse`/`metrica`/`sportec`/`statsbomb`/`wyscout` trusted) decided at the `compute_xt_gk` edge and passed as the opt-in `distrust_native_origin` into `resolve_restart_geometry`/`resolve_gk_geometry` (default-off → full-tracking byte-identical). **Distrust scope = GOAL-KICKS ONLY** (narrowed after real-data validation: open-play GK passes' native origin IS the keeper — ball-at-release — measured 0.4 m vs the detected keeper, so they keep native; goal-kick native is the broadcast ball ~14–20 m downfield, so it's distrusted). A goal-kick resolves by: keeper **detected within ±1 s** (`visibility`-gated, nearest-in-time ties→at-or-before; `_tracking_gk_xy_detected`, window centred on the action clock per ADR-017, **re-projected to action-LTR via ADR-028 `acting_team_attacks_rtl`** — away-team detections else land at the wrong end of the pitch) + in-box clamp → `tracking_gk`; else rule-point `(5.5,34)` → `goalkick_prior`. `unresolved` is now a rare residual (NaN-native open-play pass + no detection). **Origin-only** (a dedicated `origin_eligible` mask leaves the destination cascade untouched — the broadcast ball *endpoint* is correct). S4 `flag_native_goalkick_out_of_region` warns + emits the per-row `xt_gk_native_goalkick_out_of_region` provenance flag (NOT in `_OUTPUT_COLS`) + `XtGkReport.n_native_goalkick_out_of_region`. S1 within-pitch invariant in `skillcorner.convert_to_frames` warns + counts `TrackingConversionReport.n_gross_off_pitch` (LAYERED: `derive_goalkeepers`'s `[-15,120]×[-15,83]` raise stays the catastrophic player backstop; S1 = a thin player band a margin INSIDE that shared bound + the SOLE off-pitch signal for the ball). C1: removed the mixed-provider `completion=` escape hatch (`_resolve_single_provider` enforces one-call-one-match uniformly across completion AND geometry). **`xt_gk` opt-in → not a forced VAEP retrain, but SkillCorner `xt_gk` serve output changes → lakehouse re-materializes.** Deferred (tracked): S1/S4 CI rate-gates + TOL calibration from the pining corpus; open-play own-half bound (validate-then-maybe); detection-window perf (measure-before-optimize). C4 count unchanged (28).
PR-S93 (4.28.0) ships **TF-48 post-shot goalmouth crossing geometry** (ADR-030, lakehouse-PSxG feed): `_shot_goalmouth.py` pure engine `compute_shot_goalmouth` + `@nan_safe_enrichment add_shot_goalmouth` + per-Series wrappers + atomic mirror ({shot, shot_penalty} — `shot_freekick` is a `freekick` atom). Per shot action, fits the post-contact ball trajectory (x/y constant-velocity LS; z by a `rolling`/`airborne`/`bounced`-with-hysteresis taxonomy, fixed-g ballistic; bounce supersedes to the LATEST sub-segment — what the GK faces) and intersects the attacked goal plane → `shot_crossing_y/z` (canonical attacked-goal-at-x=105, point reflection x→105−x+y→68−y), `shot_speed` (ALWAYS the contact sub-segment), `shot_time_to_goal_line`, `shot_on_target_derived` (posts/bar + ball-radius tolerance, width folded in BY DECISION) + provenance (`shot_crossing_source`/`confidence`/`fit_n_frames`/`fit_rmse`/`fit_end_reason`/`z_profile`) + `ShotGoalmouthReport`. Engine is orientation-AGNOSTIC (goal ends from the GK map — `defended_goal_x` extracted byte-identically from xS into `_gk_resolve.py`; never reads `team_attacking_direction`; both-conventions byte-identity gate); fit window ends at FIRST of observed plane straddle / trajectory break (LOCAL residual + ground-gated speed-drop + reversal-vs-own-initial-direction — own goals fall to `no_crossing` intentionally) / window cap. Kernel hardened over 7 real-WC2022 pilot cycles: sample-and-hold collapse (GS raw `balls` = ~15 Hz duplicated at 29.97 Hz stamps), flight-run + CONTACT anchoring (the GS event x/y is an exact ball-track point; contact-EXISTENCE: a window whose ball never comes contactably near the stamp at z≤2.6 m provably lacks the shot → `insufficient_frames`), z-aware flight (chips), 0.1 s-baseline velocities, extrapolation-leverage cap (t* ≤ 3× evidence span). **NO VAEP xfns** (post-contact outcome = HybridVAEP-class leakage; auto-discovering guard over every default xfn list) → no retrain trigger. **HOLDOUT-VALIDATED (one-shot protocol, ADR-030 floors ALL PASS: 48 held-out WC2022 matches / 999 matched shots — goals dy median 2.17 m ≤ 2.5, dz 0.48 ≤ 1.25, coverage 0.620 ≥ 0.60, on-target agreement 0.60 ≥ 0.45; y-handedness settled on SB-freeze-frame-GK-vs-tracked-GK GEOMETRY, 0.882 on 646 voters).** Owner-gated GS↔StatsBomb WC2022 acceptance harness (`scripts/validate_shot_goalmouth_sb.py`; SB y is inverted vs SPADL; GOALS-vs-SAVES stratified — SB save end_locations are save-points; SB↔GS clocks are BOTH cumulative — round-1 aborted on a period-relative conversion that had excluded ALL P2 shots, full failure record in ADR-030). Also fixed: the pining GS loader hardcoded `z=0.0` and dropped real ball z (present on 100% of raw GS ball records). C4 count 27→28. **Completion cycle (4.28.0):** two kernel refinements re-validated on the FULL 64-match GS corpus (post-holdout protocol) — (1) span-gated curve-aware y extrapolation (`_extrapolate_crossing_y`: quadratic-y when the producing segment supports curvature AND it markedly out-fits the line, capped tighter than the linear leverage; fixes a curling-chip's linear-extrapolation miss) + (2) earliest-reaching flight-run tie-break (`_find_flight_run`: among runs that REACH the plane prefer the EARLIEST — the bare nearest-plane rule anchored t0 PAST a real in-mouth crossing). Full-corpus floors all pass, goals dy 2.17→2.08 (no regression); `--sweep` extended to the contact/flight module constants confirms robustness. **Provider coverage:** GS validated; SkillCorner/IDSSE returned `insufficient_frames` at TF-48's time (4.28.0) due to a then-open upstream y-inversion (kloppy tracking frames inverted vs SPADL actions — the tracking gateway omitted the `to_coordinate_system` pin the event gateway used; `docs/research/bug_kloppy_tracking_y_inverted.md`). **FIXED in 4.29.0/4.30.0 (ADR-031):** the kloppy tracking gateway now pins the canonical SPADL coordinate system (CS-pin, T1/PR-S94) and the DFL parse-port retired the y-inverting dev loader (T3/PR-S95), so SC/IDSSE resolve on the corrected path — the coordinate-fix smoke-test had predicted SkillCorner 0.12→0.60 (GS baseline), proving the gap was upstream, not in TF-48.
PR-S98 (4.33.0) ships **TF-23 SkillCorner + Metrica bronze→frame builders** (ADR-034): pure bronze-consuming converters `tracking.skillcorner.convert_to_frames` / `tracking.metrica.convert_to_frames` (parallel to the native sportec/GS adapters) that single-source the SkillCorner/Metrica coordinate rescale + period-relative clock + id-namespacing + GK derivation + speed + LTR orientation the luxury-lakehouse previously duplicated **three** ways (the kloppy gateway oracle + two lakehouse builders `analytics/action_context/convert.py` + `ingestion/tracking_context.py`). Promotes the lakehouse ADR-053 flag-free **geometric frame-LTR net** to the library as `tracking.orient_frames_to_ltr_by_geometry` (per-period home-GK-median-x anchor → point-reflect mis-oriented periods; idempotent; silent on normal flips; reused by both builders; retained ALONGSIDE the flag-based `orient_frames_to_ltr`). Recovers SkillCorner `ball_z` (previously discarded → SkillCorner TF-48 PSxG features silently null). **Clock:** SkillCorner reuses the events converter's nominal `_PERIOD_START_SECONDS` (same-provider single-source); Metrica rebases per-`(period)`-min (its 3 public sample games have MIXED continuous/period-relative raw clocks — verified). **GK:** SkillCorner **trusts** its authoritative roster `is_goalkeeper` (4.38.0/PR-S105: use it as-is, `is_goalkeeper_source="native"`, SKIP `derive_goalkeepers`; derive only as fallback for a `(game,team)` with no native GK) — the prior unconditional re-derivation over-flagged ~15 "keepers"/team across the lakehouse's 250-frame per-batch builds since positional derivation is unstable on a 25 s window (real-bronze union 15/13→1/1); + S2 guard `warnings.warn`+`TrackingConversionReport.n_implausible_gk_teams` on an implausible per-team GK count (>2 or 0). **Metrica (no roster) is still contaminated the same way — derive-once-per-match is a tracked follow-up.** Metrica seeds NONE (`gk_jersey_numbers` is a flat, team-agnostic list; `derive_goalkeepers` ORs-never-clears → a mis-flag would reach the orientation anchor) and lets the validated positional algorithm derive it (Tier-2, ADR-007). **Metrica input CONTRACT:** bronze `y` must be SPADL bottom-to-top (kloppy's metrica NATIVE CS is top-to-bottom — a kloppy-`TrackingDataset` consumer MUST flip `1−y`; the lakehouse bronze already does). Gate C closes on the shipping path via a kloppy-independent **event-anchored ball-position y-identity gate** (SkillCorner, real Databricks `skillcorner_tracking ⋈ skillcorner_matches` + `spadl_actions` slice, both teams/periods); the Metrica builder is validated canonical-correct vs the kloppy gateway (dx=dy=0 on open-data game 1). Lakehouse delete-and-depends both builder copies + retires its orientation net (its re-materialize trigger, not silly-kicks'); GS-native-adapter ET orientation = TF-23b follow-on; a relayed lakehouse find: metrica `spadl_actions` event-y didn't co-locate with the canonical tracking ball (a lakehouse events issue, NOT the builder). C4-free (new converters + a geometric orienter, not action-coupled aggregators; count stays 28).
PR-S91 (4.21.4) ships the **xT-GK per-type base-rate serve switch** (ADR-024 amendment, SK-91): `compute_xt_gk` serves the per-type calibrated base rate (tagged `xt_gk_completion_source="base_rate"`) instead of the geometric `p` for any completion sub-domain whose held-out AUC LCB ≤ 0.5 (or degenerate / `n<_GATE_N_MIN`=50). One pure `serve_mode_from_lcb(lcb,n)` (`_gk_completion.py`) baked into `GkCompletionModel` (`_type_serve_mode`+`_type_gate_metrics`, artifact **VERSION 1.1.0**; `load()` **fail-opens** → a pre-gate 4.21.0 artifact serves all types `model` = prior behavior). `predict_proba` stays a pure scorer; the switch+tag live in `compute_xt_gk` (geometry-resolved RAV path only); atomic mirror inherits; `XtGkReport.completion_source_counts`. **Data-driven per variant, not a blanket goal-kick rule:** bundled **SkillCorner** goalkick→`base_rate` (AUC 0.433), GK-pass→`model` (0.737); bundled **GS `default`** goalkick→`model` (AUC **0.836**, LCB 0.798 — GS goal-kick completion IS geometry-predictable), degenerate throw-ins→`base_rate` both. **Coef byte-unchanged** — the re-bundle attaches the gate onto the *loaded committed* model (the fresh fit is only a corpus-identity probe); weight diff additive-only (`model.json` version+gate + `SHA256SUMS`). The corpus-identity guard relaxed `atol=1e-9`→`_CORPUS_IDENTITY_ATOL=0.05` in `train_gk_completion.py` (still aborts a real retrain ~0.47, ~9× the floor; tolerates unrecorded-`tracking_limit` density float noise ≤0.0056). **Training defaults to FULL-match frames** (`train_gk_completion.py` `--tracking-limit` default `200`→`None`, SK-91): a small cap starves the SkillCorner derived-GK → over-flags GKs in 2/10 matches, inflating the frame-derived GK-pass domain 461→538 (full frames reproduce 542 exactly; event-pinned goalkicks stay 81 — the tell; a small cap is dev-smoke-only, never to bundle). Not a VAEP retrain (xt_gk opt-in, in no default xfn list) but an `xt_gk` serve-output change → lakehouse re-materializes SkillCorner goalkicks (~15% of its GK-distribution rows) + degenerate throw-ins; GS goalkicks unchanged. C4-free (serve gate on an existing model — no new aggregator/model/backend; count stays 27).
PR-S100 (4.35.0) ships the **xT-GK PEV/DZV fidelity fix** (ADR-024 amendment, Eyestone Q1–Q3 answered 2026-06-27). **CHANGE 1 — PEV reads the GK-revalued surface:** `progress = V_GK*(z') − V_GK*(z)` where `V_GK = xT ⊙ φ(z,d)` (convolved like `xT*`), NOT raw `xT*` — raw xT flatlines in keeper zones (the measured PEV inertia is the surface, not the tournament); `PEV = ρ·max(0, progress)` kept exactly. **CHANGE 2 — DZV is the published revaluation multiplier** `M(z) = φ(z,d)·[1 − V_GK(z)/max V_GK]` applied as the **increment** `(M−1)·V_GK(z)` (Option A), def-third-gated — replaces the old additive `v_def − xT_raw(z)` floor (literal M≈2.5 would swamp the other terms → the increment revalues the small origin possession). **Magnitude (corrected PR-S103):** on the production grid (deep raw xT≈0.0085 → deep V_GK≈0.02) DZV lands **≈+0.02/action (live WC2022 +0.021)**; the earlier "O(0.01)" described the *understating* unit fixtures, and the 2× vs Jeff's ~0.009 La Liga anchor is grid amplitude (within his sanity band), form faithful — see `docs/research/xtgk_test_production_parity_audit.md`. **φ(z,d)** `= α·(1 − d/D_max)^(−β)` for `d < D_threshold` else 1; `α=2.1`/`β=0.8` **CANONICAL**, `D_max=105`/`D_threshold=35` (= `defensive_third_boundary`) provisional. `XtGkParams` gains `dzv_alpha`/`dzv_beta`/`dzv_d_max`, **retires `v_def`**; scalar `phi` stays the preset-modulated overall DZV weight (canonical shape in the φ grid). **Invariant (Eyestone constraint): φ enters value via PEV + DZV ONLY** — `base = −xT*(z)` and RAV (`xT*(z')`/`xT*_counter`) stay on raw `xT*`, so the destination is never revalued twice and **Option B is unchanged**; behaviorally guarded (`test_phi_shape_changes_only_pev_and_dzv_not_base_or_rav` + `test_pev_reads_revalued_surface_not_raw`). Not a forced VAEP retrain (xt_gk opt-in) but an `xt_gk` serve-output change → lakehouse re-materializes `fct_action_context` + re-runs the WC2022 cohort/report. C4-free (count stays 28). Atomic mirror inherits.
PR-S101 (4.36.0) ships the **xT-GK resolved-coordinate audit columns** (ADR-024 amendment, pre-Jeff-verification handoff): `compute_xt_gk` emits `xt_gk_origin_x`/`xt_gk_origin_y`/`xt_gk_dest_x`/`xt_gk_dest_y` — the EXACT coords the grid lookups used per in-scope GK distribution (NaN off-scope), emitted BEFORE the not-scoreable early return (an unresolvable-dest goalkick still shows its resolved origin + NaN dest). For goal-kicks ~67% are the imputed `resolve_gk_geometry` origin (rule-point `(5.5,34)` / in-area tracking-GK), NOT native `start_x`/`end_x` → every `xt_gk` row externally auditable. Parallel **`_COORD_COLS`** set, deliberately **NOT in `_OUTPUT_COLS`** (so `xt_gk_xfns` doesn't surface them as per-slot VAEP features — they're provenance, not a metric); ride through `add_xt_gk` + atomic mirror automatically. Tie-to-value gate: `base == −xT*(origin_x,origin_y)` on the convolved grid. Liveness gate: the 4 audit coords join the non-constant `provenance` exemption (legitimately constant across goalkicks sharing the rule-point origin; non-null check still applies). `XtGkReport` unchanged (counts report, not per-row coords). **Additive — no `xt_gk_*` value change, no retrain;** unblocks the held lakehouse persist-coords schema migration + external orientation verification. C4-free (count stays 28).
PR-S114 (4.47.0) ships the **TF-19 attempt-arm re-gate CODE** (ADR-037, GKDV Layer-3; spec `docs/superpowers/specs/2026-07-12-tf19-gkdv-regate-and-v1-design.md`). The shipped xS/xCross weights are **chirality-mis-served** (trained y-mirrored, served y-correct since ADR-031 — xS reads 12/27 features sign-inconsistently, xCross 3/16, on every y-correct provider for `pre_shot_gk_full_default_xfns` consumers; a correctness bug `load()` was structurally blind to), so PR-2 retrains all three on corrected frames; THIS PR is the code that makes the retrain measurable. New **PRIVATE** `tracking/_model_eval.py` = model-agnostic GK-substitution probe core (pure evaluator over pre-substituted ghost TARGETS-as-data → `tracking/` NEVER imports `gkdv/`) + the **registered xS probe rule** (constants locked before any owner run: `XS_PROBE_RATIO=2.0`, `XS_PROBE_DOSE_M=2.0`, band `n≥100`, trusted-stratum `n≥50`, 20 paired-vector placebo replicates, cluster-EXACT game-level dose-response) + `evaluate_xs_probe` (verdicts `pass`/`fail`/`unmeasurable_at_dose`/`no_valid_placebo`/`band_pass_flat_dose_response`; ratio prong strengthened to `≥ 2× max(nearest_def, placebo_p95)`; zero-inflation is a REPORTED diagnostic, NOT a gate — an all-zero GK band with live controls is a clean fail) + `PROBE_WRAPPERS` registry (xcross frozen byte-equivalent + xs) + `regate_verdict` (spec §3.5 as a pure function; the `no_valid_placebo`→`unmeasurable_at_dose` conflation is deliberate — the probe report preserves the distinction). `_causal/`→**public `causal/`** (ADR-015 promotion): `matching.py` estimators unchanged (fit/match/ATT/ATNT/AI-SE byte-identical) while `placebo_shift` gains the cluster-aware mode (`cluster_ids` + `_cluster_reassign` whole-cluster reassignment-with-recycling, `permutation_unit` reported); `opportunities.py` gains the full `OpportunityConfig` builder surface incl. a **result-conditioned, anchor-inclusive** outcome axis so the §3.3 shot arm is expressible purely as builder args (`shot_arm_config`); `config=None` is byte-identical to the legacy xCross path (regression-gated); cluster-aware placebo. **xS extractor ADR-019 hardening** (`extract_xshot_features` canonical-id compares — VAEP-invariant for matched dtypes; the ghost-GK extractor's raw `==`/`!=` at `_ghost_gk.py:488-490` is a recorded out-of-scope latent gap). **Behavioral chirality fingerprint** (`_chirality.py`): all three `save()` paths EMIT the model's outputs on a fixed y-ASYMMETRIC frame into `metadata.json`; `load()` fail-closed enforcement + legacy-override land PR-2. **gkdv→tracking rule: public seams only, never the reverse** (allowlist test lands PR-3). Weights land PR-2 → **NO retrain trigger from this code-only PR**. C4-free (count stays 28).
PR-S115 (4.48.0) ships the **SkillCorner corpus-expansion + visibility cycle** (ADR-038; spec `docs/superpowers/specs/2026-07-14-skillcorner-corpus-and-visibility-design.md`, evidence `docs/research/skillcorner_corpus/`). The pining SkillCorner listing grew 10→108; the 98 new matches are **owner-tier** (`visibility: "private"`, restricted, all Real Madrid LaLiga+UCL) → the public arm stays 17 (10 SC + 7 IDSSE; prior 4.9.0/4.18.0 paired verdicts intact), the 98 only expand the owner/full arm 81→179. **Native SkillCorner route (§5, supersedes the kloppy gateway for SkillCorner):** the pining path now builds SC frames via `tracking.skillcorner.convert_to_frames`, surfacing **`visibility`** (from `is_detected` — the kloppy gateway hard-codes `None`, discarding it) + **`ball_z`**, and single-sourcing the coordinate transform. **Clamp split (§3, the load-bearing fix):** `spadl/skillcorner.py::_transform_coords` scales THEN clamps — safe for events (on-pitch by construction), DESTRUCTIVE for tracking (measured 11.31% of ball rows snapped up to 9.00 m; a ball 9 m behind the goal → on the goal line, goal-vs-save ERASED). The affine part is extracted to `_scale_to_spadl` (no clamp); tracking calls **`_scale_to_spadl` ONLY, never `_transform_coords`**. **Pitch-dim scaling (§4):** the native builder scaled by a fixed 105×68 (goal line off by up to 2.0 m on the 4/10 public matches at 104/106 m + the private 101×67); now scales via the events converter's own affine transform (single-sourced); missing dims RAISE (fail-closed, `assume_standard_pitch=True` the explicit opt-in). The 104 m choice rests on **provenance** (SkillCorner's declared `pitch_length`), not agreement — kloppy's map is non-affine (~103.48 m effective, unexplained); open question → ask SkillCorner. **Visibility-keyed corpus taxonomy (`scripts/_corpus.py`, §2 compliance control):** `_PUBLIC_PROVIDERS = {"skillcorner","idsse"}` **DELETED** (6 sites — `provset <= _PUBLIC_PROVIDERS` at pre-PR `train_xshot_occurrence.py:306` labelled an sc_extended-shaped restricted run `"public"`, a non-redistributable model mislabelled reproducible); `is_public_row` fail-closed (unknown/missing visibility → restricted), `artifact_label` from the ship-mask composition, `assert_public_corpus` subset-gate; red-first `test_a_restricted_corpus_NEVER_ships_a_public_label`. **Registered-protocol machinery (locked before any fit sees data):** `scripts/_paired.py` = the fixed-sequence three-candidate (`public`/`sc_extended`/`full`) paired test with tuning **NESTED inside the outer CV** (M4 fix — `public` must not tune on the 17 matches that ARE the evaluation universe); `scripts/_ghost_domain.py` = ghost-GK detected-keeper targets + keeper-grouped CV + paired sign-consistency admission (interpolator-tell RETIRED — dead code under detected-only training → reported diagnostic); `scripts/_cache.py` = feature-cache schema guard (stale cache is a MISS). **S1 rate-gate (§6):** `_TOL_BALL` 30→15 m (real max ball excursion 9.00 m); the deferred per-match rate-gate implemented (`player_frac(>3 m) > 0.005` OR `ball_frac(>10 m) > 0.0005` → exclude). **PINNED limitation: it CANNOT detect a pitch-dim error** (0.00095 vs clean 0.00086, inside the clean band) — and neither can action↔frame co-location (events + tracking read the same metadata, move together); the only instruments are provenance + asking SkillCorner. **Detection finding: GKs detected in only 19.6% of frames** (~80% interpolated) → VINDICATES the GS-only GKDV measurement rule + ADR-024/PR-S104 SC keeper-origin distrust. **Cache constraint (deviation from spec §3.2):** the fingerprint is a CONSTANT schema token (invalidates pre-Task-11 caches, does NOT detect corpus drift) → **use a fresh `--output-dir` per corpus** (DGX runbook); live per-corpus fingerprint is a follow-up. **Code + tests only — NO weights** (they land after the owner Stage A re-baseline / Stage B expansion runs). Hyrum: lakehouse re-materializes SC frames (geometry moves ≤2.0 m on non-standard pitches, correctness fix); research-corpus SC frames change (native route) → owner Stage A re-baseline. C4-free (count stays 28).
PR-S117 (4.50.0) ships **TF-49 packing** (ADR-039): `_packing.py` (`PackingParams` frozen dataclass + pure `compute_packing_metrics` + `secured_reception`) + `_kernels._packing_at_actions` (GEOMETRY-ONLY: `packing_made`/`packing_net`/`packing_goal_threat`/internal `line_x`; domain = `params.action_types` ∧ result success; degenerate `start==end` → NaN for DRIBBLES only — placeholder-indistinguishable — pass-class → honest 0) + `@nan_safe_enrichment add_packing` (5 cols; made/goal_threat `Int64`, net float64, receiver source-dtype passthrough, secured `boolean`; `require_secured` gates receiver-bearing types only — dribbles/made==0 untouched) + `packing_xfns` (3 numeric cols × 3 slots, 3×-spied; **REJECTS `require_secured=True`**; **F4 result-leakage warning: gates on the action's OWN `result_id` — never into HybridVAEP-class consumers without a0 exclusion**; result-free-a0 variant = recorded fork, not built) + atomic mirror (numeric-only; SK-xT-2-style type-aware synthesized result — dribble intrinsic, pass-class next-atom-`receival`, name-mapped types; rejects `require_secured`). Defender extraction deliberately duplicated ×2 from the frozen `_structural_pass.py` (packing mirrors x ONLY; 3rd consumer triggers extraction) — pinned by the golden identity gate (`packing_made == structural_lbs` on completed pass/cross + failed-row discriminator + non-vacuity meta-asserts). NEW public `spadl.utils.resolve_next_touch_receiver` (packing-agnostic next-same-team-touch; skips `non_action` AND `foul` — neither is a touch; FULLY POSITIONAL per ADR-019/PR-S110; int64 AND NaN-coded float64 sources pre-convert to `Int64`, output never float64) with private `_resolve_next_touch_positions` as the STABLE INTERNAL SEAM feeding `secured_reception`'s RECEPTION-anchored window (retains() skeleton + REQUIRED foul-skip — heuristic possessions emit a boundary AT the foul row; reception-that-IS-a-shot → True; NaN-team rows skipped, ADR-027; empty window extends shot/boundary tests only; scan runs in `action_id` order — time-tied swapped rows resolve identically; an unattested NA `possession_id` never decides). **12-agent adversarial review confirmed + fixed six defects in-PR (ADR-039 §Execution-review: receiver foul-skip; atomic collapsed corner/freekick bridging; atomic caller-frame output purity; action_id-order secured scan; atomic same-team keeper receptions; NA-possession never decides).** In NO default xfn list → **no retrain trigger from packing itself**; C4 count 28→29. **Shipped alongside (owner-directed, ADR-018 amendment 2026-07-17): GS ball-carry RESULTS from the native `ballCarryOutcome`** — the GS result dispatch had no BC success condition, so every GS dribble fell to `fail` (structurally off-domain for the completion gate; statsbomb is 100% success). R (retained)→success, L (lost)→fail, unknown/absent→fail (exact-token allowlist style); probed on 4 WC2022 matches (field on 100% of BC rows; L ~86% opponent-next); **GS-only retrain trigger folding into the 4.49.0-queued GS re-fit**; the packing e2e gates the GS dribble in-domain share strictly interior. `retains()` shared the foul-row/raw-`!=` bias classes only LATENTLY (live solely on its unused `add_possessions` self-heal path): a 2026-07-17 probe over the LIVE ρ cohorts measured ZERO training-label flips (gold-mart possession ids stay continuous through fouls, no NA teams/possessions), so **the same hardening was applied to `retains()` in-PR with NO retrain** — post-fix gate: shipped == probed variant on all 223,718 cohort rows, 0/3451 GS + 0/5483 SC training-label changes; bundled ρ weights + metrics untouched (ADR-036 amendment 2026-07-17, ADR-039 relay 1).
PR-S118 (4.51.0) ships the **TF-19 PR-2 weights bundle** (ADR-040; completes ADR-037 §9's enforcement that PR-S114 deferred). The bundled default xS/xCross/ghost weights were **chirality-mis-served** (trained y-mirrored pre-ADR-031, served y-correct — a live correctness bug for `pre_shot_gk_full_default_xfns` VAEP consumers); the DGX y-correct retrains replace them (**xS/xCross defaults = the reproducible `public` arm** SkillCorner+IDSSE, GS-free; **ghost default = the Stage-B 179-match retrain**; wheel shrinks 12→7.2 MB). **`load()` fail-closed chirality enforcement** on all three models (`_chirality.verify_chirality`): re-runs the model's own `_chirality_block` on the canonical y-asymmetric probe (`canonical_probe_frame`) and compares to the `metadata.json` fingerprint — raises on a **MISMATCH** and on a **MISSING** one (every pre-PR-2 artifact = the mis-served class), with a `legacy_override=True` escape hatch (warns); cross-platform tol `atol=1e-3`/`rtol=1e-2`; finiteness guard in `chirality_fingerprint`. `verify_chirality` takes an `error_cls` so each `load()`'s chirality error shares that model's own `IntegrityError` taxonomy (ghost keeps its module-local type — a consumer catching `_ghost_gk.IntegrityError` catches this too). **`base_score` 2.x/3.x compatibility guard** (`load_xgb_booster_base_score_safe`, `_xshot_occurrence.py`, imported by `_xcross_attempt.py`): xgboost 3.x serializes `base_score` as a bracketed string `"[X]"` that 2.x silently drops to the `0.5` default (mis-served intercept) — the guard strips the brackets at load; reads `model.json` as UTF-8; the bundled weights are re-saved to clean 2.x scalar. **The chirality enforcement CAUGHT this base_score bug** (the golden `test_weights_bundle_golden.py` failed loud on the DGX retrains before any fix — the design validating itself). **HF-only `sc_extended` routing:** `from_variant("sc_extended")` → `from_hub` for BOTH xS and xCross; xS's `from_hub` is implemented (mirrors xCross) behind the new `[xshot]` extra; the Hub repos were **populated 2026-07-18** — `silly-kicks/xshot-occurrence-v1` + `silly-kicks/xcross-attempt-v1`, served at the repo ROOT to match `from_hub`, with model cards stating that `sc_extended` is the NON-shipped Stage-B variant (`tf19_ready=false`); `silly-kicks/ghost-gk-v1` still serves a **pre-chirality artifact that `load()` REJECTS** — uploading the 179-match retrain is the open follow-up. **Weights-bump CI fixture updates** (the new weights legitimately move outputs): the ghost KDE scipy-oracle golden is regenerated from the Stage-B model (cpu-numba/fft==scipy parity unchanged); the xCross directional liveness fixture was **degenerate** (held `ball_speed=0`, the model's #1 feature) → new `scripts/make_xcross_directional_fixture.py` + regenerated fixture (the new model is provably sound: held-out CV pr_auc 2.85× base rate, all acceptance gates green, AUC 1.0 on a realistic probe on BOTH the old and new models); the ghost mirror-invariance gate is **STRENGTHENED not loosened** (`test_ghost_gk_mirror_invariant` off-centre probe so a y-reprojection flip moves ghost_gk_y ~7 m — a real guard — vs the old vacuous ~0.1 m central probe; x stays the durable orientation check + an explicit non-vacuity assertion). Decision-table verdict recorded (`docs/research/tf19_pr2/decision_table.md`): the frozen xCross GK-substitution probe on the Stage-B `sc_extended` model **strengthened to ratio 2.21×** (clears the 2.0× prong) but `gk_median_abs_delta` 0.0097 still misses the 0.01 absolute floor → `tf19_ready=false`, `gated_clean_fail` (routes to GK feature-engineering, not "no signal"); the xS probe is PR-3-gated (needs the `gkdv/` ghost-substitution engine). **Hyrum/retrain trigger:** the xS/xCross columns of `pre_shot_gk_full_default_xfns` change for opted-in VAEP consumers → re-materialize; the public default arm is GS-free (unaffected by the 4.49/4.50 GS fixes). Ghost-GK model-card prose fixed (geometric goal-relative box, not "active defensive actions"). C4 count unchanged (29 — corrected weights + a load-guard are not a new aggregator/model/backend).
- **VAEP** (`vaep/`): Feature engineering + gradient boosting binary classifiers. `HybridVAEP` removes result leakage from a0 features. xG-targeted labels available via `xg_column` parameter. PR-S32 ships TF-29 design-space variants: `window` parameter on `scores()`/`concedes()` (`"action"` / `"possession"` / `"time"`); `xfns_default_no_goalscore` / `hybrid_xfns_default_no_goalscore` goalscore-bias-control lists. **4.13.0 (ADR-018): own goals are counted by RESULT (`result_id==owngoal`), not via a `type_name` shot-gate** — single-source `_is_goal`/`_is_owngoal` helpers in `vaep/labels.py` (all sites route through them; a guard test forbids the old shot-gated owngoal predicate). Fixes a codebase-wide undercount (every converter emits own goals as `bad_touch`, which the old `str.contains("shot")` gate zeroed out); the atomic label path already counted them. Hyrum: shifts scores/concedes/xG label distributions for all providers (VAEP-model retrain trigger).
- **xT** (`xthreat/`): Expected Threat. SK-xT-1 (4.17.0, ADR-021) refactored the `xthreat.py` module into a package with a house-style **pluggable transition family** (string-dispatch + frozen-dataclass params, NOT ABCs): `ExpectedThreat(method="singh_counts"|"kde_smoothed", params=, l=, w=)`. The `singh_counts` default is **byte-identical** to the classic socceraction grid (frozen-oracle parity gate at `tests/xthreat_legacy_reference.py`); `singh_transition_matrix` is vectorized (`np.add.at`, byte-identical). KDE-smoothed transition (`kde_smoothed_transition_matrix`, Silverman-1986 bandwidth, adaptive per-source-zone); `KDEParams.bandwidth=1.0` (pure Silverman) is a conservative default — the held-out-NLL optimum is **corpus-size-dependent** (~1 on a 64-match sample, ≥4 on an 8.9M-action mart), tune via the evaluator. `GridSpec` first-class variable resolution (pitch dims stay in `spadlconfig`). Standalone `value_iteration` (raw-diff convergence — monotone-from-below, do NOT "fix" to abs; optional `max_iter`). Held-out **transition-model** NLL evaluator (`holdout_split` `game_id`-keyed, `compute_holdout_nll` pure, `compute_holdout_nll_per_group`) — NOT an xT-quality metric. KNN/conditional deferred (pre-publication); lakehouse `XTGrid` declined (keep raw `.xT`). Additive — no Singh-path change, no retrain trigger (TF-24 calibration `FrozenXt` untouched). Lakehouse-proposed; attribution Singh 2018 / Silverman 1986 / Salimi 2026 (pre-pub). Decision: ADR-021. **SK-xT-2 (4.19.0, ADR-022)** wires xT into VAEP as an opt-in, frame-free feature: `vaep.features.xt_xfns(*, model)` + atomic mirror `atomic.vaep.features.xt_xfns` emit `xt__<model.method>` per gamestate slot (ADR-005 §8 naming), caller-supplies-the-fitted-model (fail-closed; `str`/`None`/unfitted raise; `ExpectedThreat` imported `TYPE_CHECKING`-only — duck-typed at runtime, **no new runtime edge**). In **no** default xfn list (additive — opting it in is a self-triggered VAEP retrain; guard test enforces absence). Atomic reuses `model.rate()` **unchanged** (no `_rate_cells` extraction — SK-xT-1 parity gate untouched) via a synthesized standard-SPADL frame + **type-aware** `result_id` (dribble intrinsic since dribbles are never followed by `receival`; pass/cross success iff next atom is `receival`); maps slots by composite `(game_id, period_id, action_id)`; period-last pass/cross → NaN (inherent atomic edge). Cross-representation symmetry verified by a geometry-keyed oracle (NOT `action_id` — `convert_to_atomic` renumbers it) + a dribble keystone gate on the committed WC2018 fixture. Bundled-grid variant reserved via a typed `str` door (ADR-011). C4-free.
PR-S119 (4.52.0) ships the **real-xT EPV wiring + three per-action orientation repairs** (ADR-041) and **TF-35 off-ball run valuation** (ADR-042). **Part A:** the OBSO family (`add_obso`/`add_pausa`/`add_space_creation`) had always multiplied pitch control by a SYNTHETIC placeholder EPV (a `linspace(0.01,0.3)` x-ramp) — a real `ExpectedThreat` has shipped since SK-xT-1 and was never connected. New **`xthreat/_physical.py`** is the ONE place the xT storage inversion is neutralized (`ExpectedThreat.xT` stores rows y-INVERTED, row 0 = TOP of pitch; `rate()` compensates, `interpolator()` does NOT): `physical_grid(model, grid_x, grid_y)` resamples onto a **CONSUMER-SUPPLIED** grid in ascending-y physical orientation (docstring rule: *pass the consumer's own grid, do not invent one*), `values_at_points` gives exact NaN-tolerant `rate(use_interpolation=False)` semantics, and `require_fitted_xt` collapses TWO divergent fitted-model guard copies into one. `xt=` threads through all three aggregators + their `*_xfns`, with per-row provenance in a new **`obso_epv_source`** column (`xt`/`synthetic`/`injected`) and a `SyntheticEPVWarning` when the placeholder is served. Three public warning categories live in one module (`tracking/_warnings.py`): `SyntheticEPVWarning` / `IgnoredSurfaceInputsWarning` / `RunValueCoverageWarning` — deliberately separate so silencing the routine notice doesn't silence genuine misuse. **Three orientation defects fixed, each RED-verified:** (A) `_precompute_obso_lookup` handled orientation NOWHERE (raw action-LTR target sampled against home-attacks-right surfaces + an EPV grid always increasing toward the HOME goal → away actions valued toward their OWN goal; `home_team_id` was a dead parameter — this **amends ADR-028**, which had wrongly listed obso as "self-reconciling"); (B) `_player_influence` multiplied the raw y-mirrored `interpolator()` output — and an in-PR adversarial review found `_cover_shadows` and `_gk_influence` carrying **byte-identical** defective code, so both were fixed in the same pass rather than leaving CLAUDE.md asserting a repair that did not exist; (C) **found during execution**: both repairs first flipped only `[:, ::-1]`, but ADR-028's relation is a 180° POINT reflection (`x→105−x` AND `y→68−y`) so the grid transform is `[::-1, ::-1]` — an x-only mirror is exact only for a y-symmetric grid, which the synthetic ramp IS and a fitted xT nearly is, so the same property that hid the original bug hid the incomplete repair (caught only by writing deliberately y-ASYMMETRIC oracles). Also: `_resolve_epv_grid` uses NODE registration matching the kernel's indexing (±0.505 m at the edges), the kernel's `floor` index became `round`, `PitchControlCache.__len__` is public, and **`validate_period_directions`** rejects ONLY a team that contradicts itself (both directions in one period) — all-null (`absolute_frame`), uniform-label (`snapshot_to_tracking_frames`) and period-5 PSO frames are ACCEPTED, and promotion into `acting_team_attacks_rtl` itself is **REJECTED ON EVIDENCE** (all three shapes are library-produced, so no consumer data could establish the precondition). **Part B (TF-35, ADR-042):** new `tracking/_run_values.py` = `RunValuationParams` + pure `detect_off_ball_runs` (geometry only, one row per qualifying (action,runner), positions emitted in **action-LTR**, shared TF-4/TF-35 candidacy via the 3rd-consumer-extracted `_prepare_run_candidates`) + `value_off_ball_runs` (domain = completed pass/cross with a resolvable receiver; `run_value` = MAX of `pitch_control × threat` over the cells the runner controls) + `@nan_safe_enrichment add_off_ball_run_values` (5 cols) + `off_ball_run_value_xfns` (4 numeric × 3 slots — `n_valued_disruptive_runs` EXCLUDED as a coverage denominator) + atomic mirror. `run_value` is a **max, not an influence-weighted mean** — that choice is the entire reason `region_influence_floor` exists (a max over a thresholded region needs a threshold; the weighted mean would delete the knob), recorded as a v2 fork; the 0.1 default is a **spec-time starting value, NOT calibrated**, and `resolved_region_floor()` fails loud for any method without a recorded floor (naming the `voronoi` binary-{0,1} hazard). `peak_speed_source` is emitted as DATA (the all-NaN-speed fallback under-states peak speed, so sparse providers LOSE runs); a runner absent from the linked frame keeps `run_value` NaN and its ROW (`RunValueCoverageWarning`), never a fabricated 0; **mean disruptive value must divide by `n_valued_disruptive_runs`, never `n_disruptive_runs`** (else tracking-coverage masquerades as tactics). **F4 result-leakage inherited from ADR-039**: the domain gates on the action's own `result_id`, so `off_ball_run_value_xfns` is in NO default xfn list, enforced by an auto-discovering guard with a transformer-name anchor. TF-4's `toward_goal` is re-keyed onto `acting_team_attacks_rtl` — a **BEHAVIOUR CHANGE** on unresolvable-direction rows, honestly framed in-code as buying CONSISTENCY not correctness (re-keying TF-4 did NOT remove identity-keying from the action-coupled geometry layer — other action-coupled aggregators still take `home_team_id` by design; the earlier "last identity-keyed module" phrasing was wrong, ADR-045 D6). **Hyrum / re-materialize:** every AWAY-team `obso_*`, `pausa_*`, `space_created_m2`/`space_denied_m2_opponent` changes (DEFECT A — **applies even without `xt=`**), all `player_influence_*`, `gk_*` (gk_influence) and `cover_shadow_*` values shift (B/C), TF-4's `n_off_ball_runners_toward_goal_pre_window` changes on unresolvable-direction rows. **Home rows are byte-identical ONLY for the away-keyed OBSO target/EPV reflection** — the y-inversion repair (`interpolator()`->`physical_grid`) is a y-mirror that moves home rows too for `player_influence`/`gk_influence`/`cover_shadows`, as does `space_creation`'s `axis=(0,1)` opponent mirror. Recorded latent gap, since FIXED (ADR-019): `PitchControlSurface.player_surface`/`.player_share` compared ids with a raw `==` which also made `_player_influence`'s `except ValueError: → 0.0` **silently zero players** on dtype-mismatched frames — both methods now route through `ids_match` (the same-source `player_team_ids` compare stays raw); byte-identical on matched dtypes, so no live-cohort change. New CI class: `tests/test_no_import_cycles.py` subprocess-imports each public subpackage standalone (the Fork-A cycle closed twice and is invisible to the ordinary suite). C4 count 29→30.
PR-S120 (4.53.0) ships the **DAS failure taxonomy + velocity-availability contract** (ADR-043 decisions 11–12) alongside the `gkdv/` package (own bullet below). **`add_das`/`das_at_action`/`das_xfns` no longer catch a broad `(ValueError, RuntimeError, ImportError, IndexError, TypeError)` and degrade to all-NaN** — that tuple swallowed silly-kicks' OWN bugs (missing `vx`/`vy`, the output-alignment breach, an accessible-space signature drift) and an all-NaN column is indistinguishable downstream from legitimately-absent DAS (`calibration/_features.py` carried a private `das_ok` flag + a full re-implementation of the DAS lookup purely to work around it; both now DELETED). New **`tracking.DasUnscoreableError(ValueError)`** is the ONLY degradable exception — everything else PROPAGATES; the `ValueError` subclassing keeps broad-catching consumers working, but a catch on `IndexError`/`TypeError` specifically does not (`_das._call_simulation` converts accessible-space's degenerate-Voronoi/NaN-coord raises at the library seam, and BINDS the call before entering its guard so a signature change raises loudly instead of masquerading as the NaN-coord `TypeError`). **`add_das` emits a new `das_source` column** over the closed **`tracking.DAS_SOURCE_VALUES`** vocabulary (`computed`/`unlinked`/`unscoreable_frame`/`team_unresolved`/`unscoreable_call`, each also exported as a `DAS_SOURCE_*` constant so consumer `CASE`/enums pin to the library's set) — additive, lakehouse re-materializes; `das_at_action` returns a bare Series and carries NO provenance (docstring routes to `add_das`); `das_xfns` deliberately does NOT emit the string column (VAEP matrices stay numeric, test-guarded). **`speed_source` gains a third token `tracking.SPEED_SOURCE_UNAVAILABLE` (`"unavailable"`) — a PUBLIC CONTRACT WIDENING of `TRACKING_CATEGORICAL_DOMAINS["speed_source"]`** (`{native, derived}` → `{native, derived, unavailable}`; a consumer validating against that domain must accept a third value): a frame builder DECLARES its source has no per-player temporal history, so `speed`/`vx`/`vy` can NEVER exist. Deliberately distinct from a NULL `speed_source` ("not derived YET") — without the distinction a velocity consumer cannot separate "structurally has no velocity" from "the caller forgot `derive_velocities()`", which demand opposite responses. `snapshot_to_tracking_frames` stamps it on every player AND ball row; **third-party builders may set it** (a public contract, not a snapshot backdoor). `_validate_das_inputs` reads it: ALL rows marked → `DasUnscoreableError` → NaN with `das_source="unscoreable_frame"`; UNMARKED or PARTIALLY-marked frames missing `vx`/`vy` still RAISE loud (fail-loud wins on a mixed frame set), and the marker never excuses a missing `team_in_possession`. Also: `das_xfns` gets the ADR-020 dup-`action_id` retrofit it was missed for (the auto-enumerating gate had probed it VACUOUSLY — the broad catch returned all-NaN before the bug could surface), `derive_team_in_possession` is now IDEMPOTENT (pre-existing columns REPLACED, not `_x`/`_y`-suffixed — the contract is "possession according to THIS `carrier`"), and `[das]` is installed on EVERY CI leg (it was on ZERO). Four new public tracking exports beyond the DAS set: `serve_ghost_gk_positions`, `compute_threat_pc`, `defended_goal_x` (the three `gkdv/` consumes, published so "public seams only" is true rather than restated) + `GhostClampWarning` (a filterable category for the ADR-016 pitch clamp). No DAS *value* change → no retrain. C4 count unchanged (30).
PR-S128 (4.57.0) ships **TF-51 per-event defensive credit/debit** (ADR-047): new `tracking/defensive_credit/` sub-package (`_params`/`_sizing`/`_resolution`/`_chaining`/`_rules`/`_bravery`/`_orchestration`) valuing individual defenders with proximity-gated signed credit, sized by shot **xG** or the attacker's **`xT(origin)`** at a turnover (the validated Bischofberger/Bauer/Baca arXiv:2606.19931 `xDT=−ΔxT→+xT(origin)` sizing — our TF-28 DAS group; reverse-xT "position won" is a deferred v2 pressing lens). **10 rules** (`DEFENSIVE_CREDIT_RULES` closed vocab, `DAS_SOURCE_VALUES` pattern): 3 shot rules as a **mutually-exclusive on-/off-target/blocked partition**, 4 `xT(origin)`-sized turnover rules (`pressure_pass_fail`/`recovery_double_credit`/`synchronized_final_third_pressure`/`forced_bad_touch` — each **anchor-type-gated**, a bug the untested plan omitted and execution caught), 3 resulting-shot-xG chained rules (`failed_cross_block`/`failed_marking_through_ball`/`beaten_1v1`). **`compute_defensive_credits`** (long-form, one row per (action, credited player, rule); `signed_value` NaN when fired-but-unsizable, no row when not fired, ADR-043) + **`add_defensive_credit`** (per-action aggregate `defensive_credit_net`/`_plus`/`_minus`/`n_defensive_credits`, scoped to the **defending team** via `ids_differ` so the acting-team `−passer` rows stay long-form only, R2-1 — the C4 **+1** action-coupled aggregator, **30→31**; a thin `@nan_safe_enrichment` wrapper in `features.py` that links ONCE and threads `links=` through the whole chain) + **`compute_bravery`** (event-only per-team: `bravery_shots`/`bravery_open_play_crosses`/`bravery_set_piece_crosses=NaN` + `n_set_piece_crosses_faced` + known-domain headline `bravery_pct_known_domain` — the set-piece gap is a v1 `cross_blocked`-column limitation **EXPOSED not dropped**, R2-2; source defines bravery over all crosses, arXiv/Sumpter 16.3). **On-/off-target is a tri-state** `_ensure_on_target` (goal→True; injected `on_target_column`; else the frame-based TF-48 `shot_on_target_derived` fallback reusing the single link; unknown→abstain — a pressured **saved** shot is never mis-signed as a miss, the one correctness bug the plan review caught). **No `*_xfns` factory** (F4 result-leakage, ADR-039/042; auto-discovering absence guard) → **in no default xfn list → no VAEP retrain**; **no atomic mirror in v1**. Consumes the shipped `shot_blocked`/`cross_blocked` (4.56.0); a cross-provider **`cross_blocked ⊆ cross-type`** invariant (CS-1) added to the block-detection contract (GS-synthetic teeth). Box-half-width picks `_xcross_attempt`'s 20.16 (FIFA 40.32/2) + flags the `_ghost_gk` 20.15 discrepancy — a canonical `spadlconfig` penalty-area constant is a tracked follow-up (ADR-021). Owner-gated GS e2e (real WC2022 match 10502) validates the family end-to-end. Registered with all four auto-gates (purity/nan-safety/liveness/id-dtype).
PR-S137 (4.69.0) ships **ADR-028 orientation defect class — PR 1 of 5: DETECTION** (ADR-051; spec `docs/superpowers/specs/2026-07-29-adr028-orientation-defect-class-design.md`). **Test-only + one public warning category: no shipped feature value changes, no retrain, C4 stays 32.** Specing TF-30 (b) surfaced a live ADR-028 defect in `add_cover_shadows` (`features.py:3698`/`:3861` feed a raw action-LTR `passer_xy` against frame-LTR positions), and verifying it surfaced FOUR more root causes. Measured on real matches with a physical-mirror instrument (chosen over patch-and-diff: it needs no "corrected" implementation, so a bug in the fix cannot masquerade as a defect magnitude): **cover_shadows is wrong on 78–85% of AWAY rows on BOTH valid providers** (GS 10502 + IDSSE; `n_blocked_receivers` off by a median of a whole receiver), `_gk_geometry` writes frame coords into action-LTR quantities (GS 19% / IDSSE 0%, the rate governed entirely by ADR-024 native-origin TRUST; restart origins to **101 m**), `_space_creation` applies an unrotated EPV multiplier so its two columns are EXCHANGED for away actions, and `_loader_pining` ships SkillCorner frames UNORIENTED (`team_attacking_direction` null on **100%** of rows — that leg's measurement was VOID and is recorded as such). **DETECTION LANDS FIRST, deliberately:** a gate landing after the fixes arrives green and is never observed failing — the green-by-construction trap TF-30 (a) existed to clean up. The anti-rot meta-assertion went RED listing all 33 aggregators before a single entry existed. **New public `OrientationUnresolvedWarning`** — `acting_team_attacks_rtl` returned an all-False flip SILENTLY (measured: `nearest_defender_distance` **7.6158 → 19.6977**, `receiver_zone_density` **1 → 0**); now specified by **OUTCOME, not enumerated condition**, which is load-bearing: the enumerated form missed the join-key branch, and a FIFTH branch (post-merge `.fillna(False)`, reachable when the acting team is absent or id spellings differ) was found only after the first implementation shipped, by two independent fixture groups. The signal is **NOTHING RESOLVED, not nothing flipped** (an all-home action set legitimately yields all-False; a PARTIAL miss stays silent because ADR-027 NaN-team rows never resolve). Period-5 PSO exempt. Warn not raise (ADR-029 consumers hold absolute frames; a raise has no reachable remedy inside a converter) — **the in-suite emitter arc was 62 → 8 → 2 → 0**. **CORRECTION (4.71.0): this category is NOT escalated in CI** — `pyproject.toml` `filterwarnings` escalates `SyntheticEPVWarning` / `IgnoredSurfaceInputsWarning` / `MissingFeatureContractWarning` only. The 4.69.0 claim of "an opt-out list of ZERO" described an intent, not a state, and is corrected here; escalating it remains a live option but is now a decision to take rather than a guarantee to rely on — and RC2 (4.71.0) makes `resolve_restart_geometry` a genuine emitter on unoriented frames. **`tests/tracking/test_mirror_registry.py` + `_mirror_entries/` = TWO gates over all 33 `add_*`**, meta-assertions pinning the registry to `tracking.__all__` both directions: **Gate A** (physical mirror; convention mixing) and **Gate B** (vary `home_team_id` over `{home, away, nonsense}` on FIXED frames; identity-keyed direction). Two gates because ONE INSTRUMENT CANNOT SEE BOTH CLASSES — swapping `home_team_id` restores the very invariant identity-keying assumes, so Gate A is **structurally blind** to it (a 5.68e-14 Gate A reading originally cited as evidence for `_cover_shadows.py:1030` is **WITHDRAWN**: it was the expected reading either way). **Both gates then found what the audit missed** — Gate B an EIGHTH D3 member (`add_gk_influence`, whose `:371-372` applies the correct both-axes reflection keyed on team IDENTITY), Gate A a **chiral goal-relative transform** (`_geometry.py` has NO `to_goal_relative_y`, so `goal_x=105` is an x-only mirror (det −1) and `goal_x=0` the identity (det +1) — opposite handedness, every BEARING negates: xS **12/27** features, xCross **3/16**, exactly ADR-037's recorded sign-inconsistent counts reached from the ADR-028 side ⇒ **PR 5**, a retrain trigger, atomic with its re-stamp because both artifacts carry fail-closed `chirality` AND `feature_contract`). **Fixture repair (D4/D5)** — `synthesize_actions` stamped coords from RAW FRAME positions, so an ADR-028 passer defect was **UNEXPRESSIBLE** (9/10 actions equal the raw frame position, **0/10** the point reflection) and a CORRECT implementation would have been wrong on it; now action-LTR unconditionally with `balance_teams` opt-in (correctness in the shared helper, sampling policy at the call site), GS direction DERIVED not hardcoded, metrica baseline regenerated (its `nearest_defender_distance` moves **1.029355 → 18.328738** — the wrong value looked MORE plausible than the right one, which is why it survived), `pff_expected.parquet` → `gradientsports_expected.parquet` (an orphan read by NOTHING, and creating the correctly-named file turns ON two tests that had been silently `pytest.skip`ping). **Also fixed:** `align_join_keys` treated every object-vs-object key pair as merge-safe while `ids_equal` content-probes the same pair (ADR-043) — a boxed `10.0` vs a string `"10"` merges and matches NOTHING; `_xshot_occurrence`'s raw `.astype(str)` merge made `add_xshot_occurrence` return ALL-NaN while `compute_xshot_occurrence` returned real values; `feature_column_names` is a NAME probe whose values are discarded, emitting ~12 warnings per `VAEP.fit` (suppression scoped to that loop ONLY). **CLAUDE.md's own ADR-028 repair table was wrong about TWO of the six aggregators it named** — corrected in place below. **14 STRICT xfails** encode every known defect (4 RC1/RC2/RC3 → PRs 2–3; 8 D3 re-key targets; 2 PR-5 chirality): strictness is the point — a fix cannot land without deleting its own marker. Decision: ADR-051 (supersedes **ADR-045 D6**; amends ADR-028 + ADR-041).
PR-S138 (4.70.0) ships **ADR-028 RC1 — PR 2 of 5: the cover-shadow passer reprojection** (ADR-051, no new ADR — RC1 is already recorded there). `add_cover_shadows` (`features.py:3698`) and `cover_shadow_xfns` (`:3861`) both built `passer_xy` from raw action-LTR `start_x`/`start_y` and differenced it against defenders, receivers and the ball — **all frame-LTR** — without ever calling `acting_team_attacks_rtl`, so an away passer entered the geometry a 180° point reflection away, at the WRONG END of the pitch. Fixed by reprojecting the **passer INTO frame coords** (not the frame into action-LTR): everything downstream of that tuple is frame-convention, and the one place that steps out — the xT lookup at `_cover_shadows.py:1164` — already reprojects itself. Flip computed once per call, and **per SLOT** in the xfns path (each gamestate slot is its own action frame, not the a0 slot). **In the xfns path the reprojection MUST precede `_get_cs`**, which rounds `passer_xy` into its cache key — reprojecting after would serve a home-oriented surface to an away action while looking correct at the call site. **Re-materialize trigger, no forced VAEP retrain** (`cover_shadow_xfns` is in no default xfn list): the two affected columns do NOT share a rate — on away rows `n_blocked_receivers` changed on **77.8%** (GS 10502) / **85.0%** (IDSSE DFL-MAT-J03WMX) and cheap-path `max_single_defender_blocking_score` on **90.7%** / **100%** (one-match point estimates per provider, NOT corpus rates); `blocking_score`/`blocked_threat_fraction`/`n_potential_receivers` are passer-independent and byte-identical, as is the `detailed=True` max-single (pitch-control counterfactual) and every home row. The atomic mirror (`atomic/tracking/features.py:1169`) is a rename-and-delegate adapter and INHERITS the fix — not a third site. Gate A's RC1 strict xfail is **deleted** (strictness forces that); **Gate B's stays** — `_cover_shadows.py:1030` still keys `attacking_toward_high_x` on team IDENTITY, which is D3, out of this cycle by decision and a different defect class, not a partial fix of the same one. C4 count unchanged (32).
PR-S139 (4.71.0, **NOT RELEASED — ships within 4.72.0 alongside PR 4**) ships **ADR-028 RC2 + RC3 — PR 3 of 5** (ADR-051, no new ADR). **RC2:** `_gk_geometry`'s `_tracking_gk_xy` + `_tracking_ball_xy` returned linked-frame coords UNREPROJECTED while their own sibling `_tracking_gk_xy_detected` had always applied the ADR-028 point reflection. The symptom was **not a wild coordinate but a systematic LOSS OF THE TRACKING TIER**: the goal-area clamp `gx <= 16.5` is an own-half predicate in ACTION-LTR coords, so applied to a raw away-team frame x it rejects a correctly-placed keeper (action-LTR x=5 IS frame x=100) and the goal kick falls through to the rule-point prior. The clamp now runs strictly AFTER the reflection, commented in place so it is not "tidied" back. **RC3:** see the ADR-028 bullet below — the two space-creation columns were EXCHANGED for away actions (measured: the SWAPPED pair agreed to 4.44e-16 while like-for-like disagreed by 1.207), fixed by reflecting the two attack-LTR GRIDS rather than the ball-anchored product. Three strict xfails deleted (13 → 10, no XPASS); their pre-fix magnitudes (0.125 / 7.0 m / 1.207) are RETAINED in the tolerance rationales as the signatures a regression must reproduce. **Re-materialize trigger** for `xt_gk*`, `gk_completion`, `enriched_*` restart coords and both space-creation columns (away rows only; home byte-identical); no forced VAEP retrain. Also fixes two carried non-RC defects: `SECURITY.md` advertised the long-dead `3.x` support line, and TODO's TF-19 row carried a 424-char verbatim duplication that added an extra table cell via one unescaped `|`. C4 count unchanged (32).

PR-S136 (4.67.0) ships the **TF-30 cover-shadow invariant repair + clamp verdict + a GATED per-defender identity**. Scope class: test repair + documentation correction + ONE additive aggregator-only column — **no shipped column's values change, no API change, no retrain, C4 count stays 32**. **(1) The monotonicity invariant could not fail:** `compute_blocking_score` clamps `max(threat_unblocked - threat_orig, 0.0)` and the invariant test asserted `>= -1e-9` on that clamped column — green by construction. Now asserted on the UNCLAMPED difference and **observed RED on a plant**; the superseded test was DELETED, not left beside it. **(2) A SECOND clamp** (`delta = np.maximum(new_recv - old_recv, 0.0)`) is now measured, not assumed: min **+1.62e-12** over 600 (receiver, blocker) deltas — it has never been observed to bind. Three tolerances at three scales (`TOL_INVARIANT` 1e-9 threat / `TOL_RECEPTION` 1e-12 summed reception probability / `TOL_ATTRIB` 1e-12 attribution), deliberately NOT one constant. **(3) `fernandez_bornn` settled empirically** (the structural argument covers only `spearman`/`voronoi`): min raw difference `spearman` +3.79 / `voronoi` +47.16 / `fernandez_bornn` +29.43 — keep both clamps, with **mixed provenance** stated in the glossary. **(4) `test_zero_blocked_implies_low_score` was renamed AND had never run:** the zero-blocked population does not exist on provider frames (~10 lane blockers ⇒ some lane always blocked; **0/9** actions under EVERY decision rule) and the old body `pytest.skip`ped on it. Reframed to the monotone form the data can answer — Spearman rho between `n_blocked_receivers` (lane classifier) and `blocking_score` (Voronoi threat integral), two INDEPENDENTLY computed quantities: measured **rho 0.935 / p 0.0002**, asserted floor 0.5 (gross-breakage catcher, honestly NOT pre-registered). **(5) NEW aggregator-only `max_single_defender_player_id`, GATED to `detailed=True`.** The cheap path CAN name a defender and deliberately returns `None`: measured on **970 qualifying actions**, cheap-vs-exact argmax agreement **0.157** (Wilson 95% [0.135, 0.181]) vs ~0.10 by chance, against a **pre-registered 0.90 floor** — and the disagreements are NOT near-ties (median nominee worth 1.6% of the true winner; at p90 its exact contribution is **exactly zero**). Not a defect: the cheap path is faithful to a LANE-based notion of "blocks most", the exact path to a pitch-control counterfactual, and the two rank the TOP of the list differently — the existing `TestDetailedVsLightweightCorrelation` (rho >= 0.7 on the VALUE) is untouched and near-silent about the argmax. Opting in costs a measured **2.3–3.2×** (39–42 ms/action vs 98–125 ms/action). The gate is itself guarded (`test_cheap_path_never_names_a_defender`, with a non-vacuity half pinning that the SAME actions DO yield identities under `detailed=True`); the column is **aggregator-only** via `_CS_AGGREGATOR_ONLY_COLS` (the `das_source` precedent) so a player id cannot reach a VAEP matrix; `_compute_cover_shadow_dict` returns a private `_CoverShadowDict` TypedDict (widening the old `dict[str, float | int]` to admit `None` would have made every NUMERIC consumer unsafe). `TOL_ATTRIB` is **confirmed by measurement**, not reasoned: 69 values exactly 0.0, smallest non-zero **3.64e-3** — nine orders of separation. The gate stays **RE-MEASURABLE** via a private `_ungated_cheap_identity` on `_compute_cover_shadow_dict` (single caller: the script; `features.py` never forwards it) — a gate nobody can re-measure is one nobody can revisit on evidence, and without the hatch the script would compare `None` against a real id on every row and report agreement 0.0, a number that measures the GATE rather than the cheap path. Guarded BOTH ways: `test_cheap_path_never_names_a_defender` (public default) + `test_ungated_cheap_identity_hatch_still_works` (the hatch is functional, not merely present — otherwise it could rot silently while the first guard stayed green). Verified end-to-end: the script reproduces **0.1992** on match 10502, the pre-gating pilot's exact figure. New `scripts/measure_cover_shadow_argmax_agreement.py` (provenance-wired, registered in the artifact-driver gate); evidence in `docs/research/cover_shadow_identity/`. **(6) Docs:** the three cover-shadow SCORING glossary entries state non-negativity is BY CONSTRUCTION and that this metric — unlike the paper's signed SoccerMap-CNN counterfactual — **cannot express a defender whose positioning made things WORSE**; the TF-30 design doc gains an appendix recording that RQ3's 789/822 headline is **circular** (positions optimised toward the Cone Corridor, then scored as "is it in the corridor?") and that the non-circular result reduces threat in only ~75% of snapshots, sign-only with no magnitude/CI/placebo. **No new ADR** — D1 held, and agreeing with a recorded decision does not need one.
PR-S135 (4.64.0) ships the **trained-model FEATURE CONTRACT + the canonical penalty-area constant** (ADR-050; supersedes the ADR-021 "canonical `spadlconfig` penalty-area constant" follow-up and closes ADR-038's deferred live corpus fingerprint). **The motivating finding: unifying the box constant is NOT behaviour-free.** `_ghost_gk.py:608` uses its 40.3 (half **20.15**) to compute `attackers_in_box` — one of the 26 `GHOST_GK_FEATURE_NAMES`, i.e. a real input to bundled trained weights — while `_xcross_attempt`/`defensive_credit/_params` use the Law's 40.32 (**20.16**). Measured on real WC2022: **70 / 175,969 frames (0.0398%)** can flip it (of 844 y-band rows only 70 were ALSO within the 16.5 m depth — the band alone is not the domain). The rate is beside the point; the MECHANISM is the risk, and no existing guard could see it (`chirality` fingerprints model OUTPUT, which a low-weight feature can shift without moving; `geometry_version` covers the transform, not the constants). So: guard first, constant second. **`tracking/_feature_contract.py`** = a `_chirality.py` sibling recording per artifact the feature VECTOR its extractor produces on a fixed probe frame + the geometry CONSTANTS it consumes. Policy (deliberately unlike chirality): a MISSING contract **warns** (pre-contract artifacts are undeclared, not known-bad); a PROBE change **warns and skips the fingerprint comparison ONLY**; a fingerprint or declared-constant mismatch **raises** with the model's own `IntegrityError`. **Constants compare FIRST and ALWAYS** — including when the probe changed (a sub-probe-resolution change 20.16→20.161 moves no feature, so only the declared constant catches it; the two nets must not cancel). `equal_nan=True` is load-bearing (`np.allclose(v,v)` is **False** for any NaN-bearing vector → a round trip on an unmodified artifact would fail), and the builder REFUSES a non-finite vector at save. **Tolerance is CHOSEN not inherited** (`atol=1e-6`/`rtol=0`; chirality's `rtol=1e-2` is a 0.17 m blind spot on a ~17 m feature, 17× the change this exists to catch); every fingerprinted artifact is x86-produced pending a measured DGX-vs-x86 delta. **Probe rule: a declared constant MUST be load-bearing on the probe** (else the guard fires when nothing changed → `legacy_override` becomes reflex); `contract_probe_frame()` yields `attackers_in_box` = **0 at 20.15, 1 at 20.16**, verified THROUGH the real extractor (not a re-implemented predicate). Every probe element is measured: 5 attackers + 5 defenders (4-and-3 gave xS **7 NaN features**), a ball row with `z` (else xS's `z` is NaN), ≥3 non-collinear defenders (ghost's ConvexHull). **Two independent public warning categories** — `MissingFeatureContractWarning` (escalatable) + `UnverifiableFeatureContractWarning` (probe changed / constant undeclared / `legacy_override`), neither subclassing the other (test-enforced): declaring a constant REQUIRES extending the probe, so one umbrella category would make escalating the missing case silently turn every probe extension into a hard failure. **ALL THREE bundled artifacts stamped** (the spec's "known gap" for xS/xCross was CLOSED, not counted — all three have identical structure and xS/xCross metadata already carried `chirality`/`geometry_version`/`pitch_length`/`pitch_width`) via the committed re-runnable `scripts/stamp_feature_contracts.py`, **metadata-only** (never `save()`, which rewrites the npz via `np.savez_compressed` — fresh-ZIP mtimes differ even when arrays are identical): verified **654 ghost arrays bit-identical**, both boosters byte-identical, all three metadata deltas **additive-only**. **What the stamp attests, precisely: FORWARD-looking** (from here, any extractor/constant change raises) — it does NOT retroactively prove these are the features each model was trained on; same limit the `_ghost_gk.py:1859-1860` load→save migration path already implied, supported by passing fail-closed chirality. Ghost also gains the `pitch_length`/`pitch_width` fail-closed guard xS/xCross always had. **CI escalates `MissingFeatureContractWarning`** (ADR-041's `SyntheticEPVWarning` mechanism) with an **EMPTY opt-out list**. **`tests/tracking/test_geometry_constant_enumeration.py`** = completeness by ENUMERATION (the ADR-043 idiom): AST-walks the four extractor modules, finds **14** geometry constants, requires each declared in `DECLARED_CONSTANT_SOURCES` or `_EXEMPT`-with-a-reason (derived-constant rule written down: derived from a declared constant → map it; derived from pitch dims → exempt), reads the **BUILT** contracts both directions, and pins the per-model sets. **It caught a real defect:** xS had been declaring `penalty_area_half_width` — VERIFIED it consumes no penalty-area constant at all (only the goal mouth, driving `openGoal`) — which would have made the canonical flip raise on every xS load with xS's features provably unchanged. Declarations: xS `goal_width`; xCross `penalty_area_half_width`+`penalty_area_depth`+`goal_width`; ghost `penalty_area_half_width`+`penalty_area_depth` (the frozen 40.3 pair). **Canonical `spadlconfig.penalty_area_half_width`/`penalty_area_depth`** + TWO **frame-explicit** `_geometry` predicates `in_penalty_area_absolute(x, y, *, attacked_goal_x)` / `in_penalty_area_goal_relative(gr_x, y)` — two entry points because the sites differ on FRAME not just strictness, and `goal_x` already means the *defended* goal in `_geometry` (same name, two readings = a box at the wrong end, an 88.5 m error); the goal-relative form takes NO goal argument so the ambiguity cannot re-enter. Migrated the two 20.16 sites **byte-identically** (grid-sweep proven; 159 xcross/defensive-credit tests unchanged): `_params` DELETES its box constants (its predicate moved to the helper, so aliases would be decoys satisfying the enumerator vacuously) while `_xcross_attempt` KEEPS them as aliases (its `:209` predicate is VECTORIZED — the scalar helper would need a per-element loop; single source = the CONSTANT, not the predicate). Rule: *a module-level constant exists iff something in that module reads it.* **`_ghost_gk` keeps 40.3** (weights fit on it; its depth test is also strict `<` vs the helper's `<=`) — the pin is now a **raise**, not a comment a contributor can delete. `in_penalty_area_absolute`'s `abs()` upper bound diverges from the old form past x>121.5, DOCUMENTED not claimed-unreachable (the nearest cap `_SPADL_X_MAX=120.0` guards `derive_goalkeepers`' TRACKING input; this helper's caller works on ACTION coords). **Trainer guards:** ghost's feature cache keys on the geometry constants (`cache_token()` — a hand-bumped literal goes stale INSIDE the re-fit cycle it protects: extract, flip, re-run → reuses 40.3 features under a 20.16 contract); xS/xCross cache fingerprint is now a **LIVE per-corpus hash** keyed on the **REQUESTED** corpus (not the extracted one — `load_matches` may drop a match, which would cause a permanent miss) via `_loader_pining.select_match_ids`/`_wanted_for_provider` **shared** with `load_matches` so the fingerprint can't describe a corpus the extraction never loaded; and `_ghost_gk.validate_provider` (shared with `keeper_detection_mask`) fails at trainer **startup** instead of after the full extraction. **No retrain, no value change** (no weights, no model output, no feature values change) — but three `metadata.json` gain `feature_contract` and their `SHA256SUMS` change, so a checksum-pinning consumer sees a diff. C4-free (count stays 32).
PR-S132 (4.61.0) ships **TF-51 v2 defensive-credit refinements** (ADR-049; supersedes the ADR-046/047 **Opta** status — permanent `pd.NA`, dropped from the roadmap). Four in-place refinements + one v1 bug fix, no VAEP retrain (no xfns): **B2** `recovery_after_pass` now scopes the forward opponent scan to the passer's `(game_id, period_id)` before the search (NOT possession — a recovery IS a possession change, which would make the scan vacuous); **Item 3** `rule_failed_marking_through_ball` gates on a genuine TF-32 ward `between_lines` line-break (not `ΔxT`), computed `home_team_id`-free in action-LTR via the SINGLE extracted `_line_breaking._straddle_core` (re-pointed `detect_line_breaking` + TF-32 tests) precomputed once on `RuleContext` candidate-gated (successful passes only — Ward never clusters other actions; perf-guarded), and `through_ball_delta_xt_min` is REMOVED (frozen dataclass → `TypeError`); **Item 2** `rule_shot_block` credits the in-corridor non-GK defender nearest the shot→goal lane (new `lane_blocker` `_resolution.py` mode: distance-scaled `_cover_shadows`-cone FLOORED so it doesn't pinch to 0 at the shooter, origin-threshold DROPPED, GK excluded by BOTH the `is_goalkeeper` flag AND a distance-along-lane `shot_lane_max_t` cap since the GS flag can be all-False, nearest-to-origin fallback) — long-form gains a generic **`resolution`** column (10→11: `nearest`/`all_within`/`all_within_beyond_nearest`/`lane`/`nearest_fallback`/`anchor_actor`, `RESOLUTION_VALUES` closed vocab); **Item 1** opt-in reverse-xT "position won" pressing lens (`DefensiveCreditParams(pressing_lens=True)` sizes the four turnover rules by `xT(105−x,68−y)` at each row's own point via the primitives-only `sized_xt`, gated PER-CALL-SITE not at the shared `_xt_at` seam; default byte-identical; `sizing="xt_pressing"`; new `SIZING_VALUES`/`ANCHOR_TYPE_VALUES`; DIVERGES from the validated `xT(origin)` + UNDER-VALUES last-ditch defending, in NOTICE). **Item 5** = new **`add_press_commitment`** aggregator (+ atomic pass-through mirror) over `tracking/_press_commitment.py` — a per-action DESCRIPTIVE cue (Role A, NOT signed credit → outside `defensive_credit/`, aggregator-only, no `*_xfns`) of whether the pressing defender COMMITS (drives in, +) vs CONTAINS (jockeys, −): the LEAST-SQUARES slope of the defender's closing-speed (projected onto the action-frame defender→actor axis, direction-agnostic) over a ≥0.1 s pre-action window, closed `press_commitment_source` provenance vocab; C4 action-coupled count **31→32**. Shared plumbing: **`tracking._opponent_resolution.opponents_within`** lifted as the ONE nearest-opponent core (dependency-inverted `threshold_m: float`; box-aware for defensive-credit, flat `press_max_distance_m` for the cue; `lane_blocker` bypasses it by design); **`tracking._velocity_availability.velocity_unavailable_by_design`** extracted from `_das.py`. `_xt_at` REMOVED (dead after the per-call-site swap; the plan said leave it but it retained zero callers → National Park; role subsumed by the lens-aware `_sized_xt`). Item 4 (atomic mirror of the credit family) SPLIT to a later spec. Three `press_commitment*` feature-glossary entries; owner-gated e2e extended with the §9 quantitative acceptance table.
PR-S129 (4.58.0) ships the **TF-19 xS-probe placebo v2** (ADR-037 amendment): a pre-registered `xs-dose-banded-v2` variant of the registered xS-arm GK-substitution probe (`tracking/_model_eval.py`) whose ONLY change from the frozen v1 is the placebo POOL. PR-3b Part A (4.55.4) returned `no_valid_placebo → unmeasurable_at_dose` — not for lack of a GK effect (dose-responsive, ~3.1× the nearest-defender control) but because v1's **random-outfielder** placebo was degenerate (`placebo_p95=0.0`, 66% zero) and that gate short-circuits BEFORE the clustered dose-response runs. New `substitution_deltas(..., placebo="random"|"model_relevant_def")` (default `"random"` = byte-identical v1) selects the pool: v2 draws the **ball-nearest defenders minus the carrier-nearest `nearest_def`** (mirroring `extract_xshot_features`'s 5-nearest-defender reference; private `_model_relevant_def_pool`), plus a distinct-role **`attacker_diag`** population (≤5 nearest attackers, carrier excluded; `_attacker_diag_pool`) emitted for reporting but NEVER banded by the unchanged `evaluate_xs_probe`. **The defender placebo is INERT in the ratio** (weaker than `nearest_def`, so `max()` pins to it) — its job is to clear the instrument-validity gate with a principled null, NOT to move the bar; **v2's real decider is the clustered dose-response permutation** (v1 never reached it), a framing baked into the driver's report generator. `xs_substitution_probe_v2` + `PROBE_WRAPPERS["xs_v2"]` (constants identical to `xs` + `placebo_pool`); driver `scripts/validate_xs_probe.py` gains `--variant {v1,v2,both}` + `--lock-commit` + a two-variant `metrics.json`/`report.md`. **v1 byte-identical** (frozen suite + a pre-refactor numeric pin; the placebo draw stays FIRST in the replicate loop → v1 rng untouched). Research instrument in NO xfn list → **C4-free (stays 31), no retrain**; the ~64-match GS `docs/research/tf19_pr3b_xs_v2/` deliverable is a **post-lock owner/DGX run** (blindness: `metrics.json` cites the lock-commit hash). **RESULT (4.60.0/PR-S131, `docs/research/tf19_pr3b_xs_v2/`, run from lock commit `78ffc70`): v1=`no_valid_placebo` (reproduces PR-3b), v2=`pass` → re-gate `joins_with_caveat` — `placebo_p95=0.00057` cleared the gate yet inert in the ratio (`gk_med 0.01548`/`nearest_def 0.00503`=`3.08×`), the clustered dose-response permutation SIGNIFICANT (ρ=0.436/p=0.001, 64 games); `joins_with_caveat` because the shot arm's `entanglement` input was a REGISTERED DEFAULT, not a measurement -- `regate_verdict` reads it only on a `pass`, which v2 returned, so a parameter documented as inert decided the verdict; `scripts/validate_xshot_causal.py` measures it and had never been run (TF-19 sign-off package F6) -- **RESOLVED 4.68.0: measured `inside_band`, which EQUALS the assumed default, so the verdict stands but is now earned; see `docs/research/tf19_entanglement/`**. Converts the xS arm from `unmeasurable_at_dose` → a citeable `pass`; research artifact only, wheel ≡ 4.59.0.**
PR-S130 (4.59.0) ships the **feature-column glossary + `describe_level`** (ADR-048) + the **TF-7 xfns pitch-control cache** (ADR-008 amendment). **Glossary:** new top-level public `silly_kicks/feature_glossary.py` — a pure Python registry of every DERIVED feature column silly-kicks emits (341 `FeatureColumn(name, definition, unit, emitting_module, attribution, higher_is_better)` records keyed by base column name; the `add_*`/`*_xfns`/atomic-mirror/spadl-enricher/vaep surface, EXCLUDING base schema `SPADL_COLUMNS`/`ATOMIC_SPADL_COLUMNS`/`TRACKING_FRAMES_COLUMNS` + linkage provenance). `unit` is a closed `Literal` vocab; `emitting_module` = the metric's **HOME compute module** (`_packing`/`_obso`/…), NOT the `features.py` monolith where the public producer is *defined* (`__module__` is uniformly `…features` → run-and-diff attribution impossible; the field is importable-+-not-`.features` gated, documentation beyond that — the honest resolution of the monolith surfaced in spec review). Pure `glossary_to_json` (+`GLOSSARY_SCHEMA_VERSION`) / thin `dump_glossary` writer / `glossary_entry` / `undocumented_columns`. The 171 combinatorial VAEP one-hots (`actiontype_*`/`result_*`/`bodypart_*`) are GENERATED from the spadlconfig vocabularies; ~170 unique features hand-authored with NOTICE citations. **`describe_level(z, *, higher_is_better=True)`** lives in a SEPARATE `silly_kicks/reporting.py` (generic z→verbal-band transform, not glossary metadata) — direction-aware, NaN-safe (ADR-003); direction lives on the glossary (`higher_is_better: bool|None`), the caller supplies it (a lower-is-better metric is never mislabelled). **Complete-by-construction CI gates:** coverage (`tests/test_feature_glossary_coverage.py`) enumerates producers **by INSPECTION with an `__all__`-less fallback** (the `_public_names` idiom from the id-scalar registry — NOT `__all__` alone, which reintroduces the ADR-019 blind spot), requires every emitted column documented (no-undocumented + no-stale); the load-bearing completeness dependency is the `add_*`/`*_xfns` NAME SHAPE (vaep `fs.*` = the recorded exception, list-enumerated) + a red-first `__all__`-less meta-test; a hard `attribution`↔NOTICE linkage gate (ADR-005); a both-directions dump→reload→`describe_level` roundtrip e2e. The harness's five run-and-diff legs each carry a non-vacuity anchor (catches a STUBBED leg, NOT a partial one — documented ceiling). **Additive documentation → no VAEP retrain.** **TF-7 xfns cache (ADR-008 amendment):** every pitch-control-consuming `*_xfns` factory (`pitch_control_xfns`/`obso_xfns`/`space_creation_xfns`/`pausa_xfns`/`cover_shadow_xfns`/`gk_influence_xfns`/`player_influence_xfns`/`off_ball_run_value_xfns`) + atomic mirrors gain a keyword-only `pitch_control_cache: PitchControlCache | None = None` threaded into whatever builds the `PitchControlSurface` (**caller-injection**; `compute_features` UNTOUCHED — framework threading rejected: it would extend the frame-aware contract for only the ad-hoc default-list user, not the perf-critical lakehouse path). `xshot_occurrence_xfns`/`xcross_attempt_xfns` out of scope (their `pitch_control_cache` param is reserved for the deferred `extended` variant, build no surface). `None` default ⇒ own local cache ⇒ **byte-identical to today** (default xfn lists stay cache-`None`) → **no value change, no VAEP retrain**. Guarded by an all-family value-identity test, a cross-family mis-keying test (pitch_control voronoi + obso spearman over one cache each get their own surface), a cross-family compute-once perf guard (spies `_cache.compute_pitch_control`: a second family over a pre-populated shared cache recomputes zero), and a wiring completeness gate (pins the wired set to the aggregators-that-accept-the-cache — caught `off_ball_run_value_xfns`, the 8th factor). C4-free (count stays 31).

PR-S`<NN>` (4.62.0) ships the **TF-19 §6.4 sign-off package** (ADR-037 amendment): the work that makes the GKDV discrimination harness SIGNABLE — it registers constants §6.4 forbade registering bare, builds the power simulator §6.1 declares a precondition, and splits a verdict from routing that hard-coded H1. Verifying the three known blockers against CODE surfaced three more. **F1: the plasmode simulator did not exist** — the derivation duty says "derive from the plasmode simulator, *it already generates* real strided frames with injected effects", and the string `plasmode` occurred in exactly ONE file, the spec. **F2: §6.1's power analysis was an undelivered PR-3 obligation** — `ICC_ANCHORS` shipped with a docstring promising "a power curve is reported at all three" that no code could produce, while §6.1 states the gate "is registered only if detection at the anchor ... is >= 0.8"; the PRIMARY criterion was registered with its power precondition undischarged. **F3: row 5 conflated two estimands** (an ICC variance share vs a spell-level ATT), so its `"at ICC 0.015–0.026"` clause is STRUCK and `N_min` carries its own `ATT_RELATIVE_ANCHORS = (0.10, 0.15, 0.20)` — RELATIVE per §1.3's own lesson. **F6: 4.60.0's `joins_with_caveat` rested on a registered DEFAULT, not a measurement** — `regate_verdict` reads `entanglement` only on a `pass`, the driver hard-coded it annotated *"inert unless the probe surprises with `pass`"*, and v2 surprised, so the parameter documented as inert DECIDED the verdict; `scripts/validate_xshot_causal.py` measures it and had never been run (nothing overclaimed — `joins_with_caveat` is the conservative branch — but the attribution was false). **F6 is now CLOSED (4.68.0, `docs/research/tf19_entanglement/`, run_commit `d1fc18d`, clean tree): MEASURED `inside_band`** on 179 matches / 98,789 opportunities — GK ablation shift −0.006999 vs a cluster placebo band of 0.004690 vs the registered 0.01 floor, so it exceeds the permutation null but not the absolute floor. **The measured value EQUALS the default that was assumed**, so F6 closes by CONFIRMATION, not reversal: `joins_with_caveat` is unchanged but now rests on two measured inputs instead of one measured and one defaulted. Alongside it the xS-v2 probe was re-run with verifiable provenance (`docs/research/tf19_pr3b_xs_v2/*_rerun_clean_provenance.*`, lock `78ffc70` / run `d1fc18d`) and REPRODUCES v1=`no_valid_placebo`, v2=`pass`. **F7: Layer 2's treatment is not expressible in the shipped builder** — `_label_treatment` assigns Z purely by ACTION-TYPE occurrence, while Layer 2's is a COVARIATE threshold at a fixed time, so the cross-session review's "just state the config" fix was unbuildable and Layer 2's DESIGN had to land (D5). Ships: `_group_metrics.icc_power_curve` (domain-free; a private numpy-only `_icc_fast` serves the permutation loop with its equivalence to the shipped consumer-tested `icc_one_way` GATED not assumed, 35.5s→0.34s) + NEW PUBLIC `causal/power.py` + `causal/_confounders.py` + `OpportunityConfig`'s covariate-treatment axis / entry-anchor rule / `outcome_max_distance_m` / outcome PARTITION (one labelling pass ⇒ identical row masks BY CONSTRUCTION) + `layer2_config()` + `regate_routing` (`gated_clean_fail`→`pending_layer2`; `regate_verdict` byte-identical, golden-pinned over every input) + two drivers. **THE FIREWALL is the constraint the package rests on:** once Layer 2's design exists in code the same machinery could RUN it, answering TF-19's open question before the sign-off meant to authorise it — so `att_power_curve` accepts **no outcome vector at all**, only an `InjectionSpec` recipe it draws from per replicate, making the observed outcome unrepresentable rather than merely refused. A call-count spy on `estimate_att` would be VACUOUS (the harness always calls it); the guard is provenance and its non-vacuity is DEMONSTRATED (guard removed + a fully duck-typed fake ⇒ the call succeeds silently). Treatment depth is `GK_r·cos(GK_theta)` NOT `GK_r` (they agree only on the goal centre line; the wide case is the discriminating test). Confounder provenance is REGISTERED frames-computed and REFUSES a `fct_action_context` source (its `bekkers_pi` is pre-ADR-045 for away teams until the lakehouse re-materializes). **BEHAVIOUR CHANGE (shipped tracking): `compute_threat_pc` now REFUSES an unfitted xT.** Its `xt` is typed as a required `ExpectedThreat` but nothing enforced it — `None` did not raise, it returned **0.0**, so any caller persisting a threat column persisted structural zeros and an ICC/power curve on them is degenerate while looking like a measurement. Now routed through the single shipped `require_fitted_xt` (ADR-041). **Hyrum: a consumer passing `None` now gets a `ValueError`.** Also ships `scripts/build_gkdv_arm_values.py` (the §6.1 ICC leg's expensive arm-value pass: **per-match shards with resume**, ~5 GB RSS and 2,224 scored frames from 175,969 on one WC2022 match; credits **only the DEFENDING keeper** — the serving seam emits a row per team's keeper and only the defender is substituted, so a naive pass-through doubles every frame and attributes half the deltas to a keeper who never moved, which is keeper-INDEPENDENT noise that compresses between-keeper variance toward zero exactly as fabricated origins did in ADR-036/PR-S113; the threat arm is REFUSED not defaulted, since `ExpectedThreat` has NO serialization anywhere and needs an in-process fit = a leakage decision for its own cycle). **C4: adds the missing `silly_kicks.causal` container** (public since 4.47.0, never modelled) + a **completeness gate** deriving subpackages from the package tree — nothing previously pinned the diagram to the code, which is why the gap survived every release. All shipped configs byte-identical (guarded per-config, not by the default alone); no new `add_*` → **C4 count unchanged (32)**; in no xfn list → **no VAEP retrain**. **RESULT (4.68.0, `docs/research/tf19_signoff_power/`, run_commit `6b242cf`, clean tree): the two legs SPLIT.** The **ICC** leg DISCHARGES §6.1's precondition — power **1.0 at all three anchors** (`mean_observed_icc_at_zero = −0.00034`, i.e. ~zero with no injected effect, so it detects rather than manufactures; 41 keepers, 8 single-match and reported as such) — closing **F2**. The **ATT** leg does NOT: `N_MIN_MATCHED` stays `None`, but its meaning changes from *the run has not happened* to *the run happened and NO bin reaches 0.80* (max power **0.055** at n=8000 vs a required 0.80, every anchor, both outcomes; 151 treated spells, prevalence 0.0041). The degenerate counts make that readable — **0/200 at n≥4000**, so this is an estimable design with no power, NOT a positivity failure wearing its clothes. Per §6.1 the response is adjust floors/sampling FIRST; the 16.5 m Layer 2 threshold is NOT retuned to raise prevalence (Law-defined so the decider stays untuned — changing it is a re-registration decision). **This vindicates F3:** the two estimands it split answer OPPOSITELY, so a merged criterion would have had one wrongly veto the other.

PR-S122 (4.55.0) ships **vector-quantity consistency under coordinate reflection** (ADR-045). A 180° point reflection (ADR-028: `x→105−x` AND `y→68−y` for away actions) must point-reflect POINTS, **negate VECTORS** (`vx`/`vy`/`dx`/`dy`), leave MAGNITUDES (`speed`) alone, and swap DIRECTION LABELS — but every reflection helper enumerated an explicit column list, so any column not on it (crucially `vx`/`vy`/`x_smoothed`/`y_smoothed`, **none in `TRACKING_FRAMES_COLUMNS`** — preprocess adds them) rode through untransformed and silently wrong. New PUBLIC **`silly_kicks/reflection.py`** = one seam: `reflect()` (registry-driven, for schema-bearing tables) + `reflect_columns()` (explicit, kind-aware, for derived/pre-canonical frames) over `TRACKING`/`SPADL`/`ATOMIC_SPADL_REFLECTION_KINDS` (each column declares a `ReflectionKind`). **`on_unknown` defaults to `"warn"`, NOT `"raise"` (do not rebuild raise):** an undeclared column is treated as `invariant` and warns (`UndeclaredGeometricColumnWarning`) only on a geometry-shaped name — fail-closed lives in the CI registry-completeness meta-assertion, not the runtime call, because `to_spadl_ltr` runs INSIDE nine converters on a frame carrying the caller's unbounded `preserve_native` columns (a raise has no reachable remedy), 0 of the 8 catalogued defects involved a caller-owned column, and a per-site policy split would recreate D3. Consumers who control their column universe escalate via `filterwarnings("error", category=UndeclaredGeometricColumnWarning)`. **Eleven reflection sites migrated onto the seam; two more were defective by omission.** **D1 + D2 are the only LIVE defects, both in `pressure_on_actor__bekkers_pi` (away actions):** D1 (`_reproject_rows` re-projected positions not velocities → away defenders modelled running backwards, −38.9%) + D2 (`_build_ball_xy_v_per_action` never re-projected the ball at all). D3/D3b (`play_left_to_right` vx/vy+smoothed), D4 (`finalize_orientation` both legs — the "both or neither" slogan is FALSE, only "both" is zero-error), D5 (shape-graph lateral label settled PITCH-ABSOLUTE by default — no consumer), D7 (the two VAEP `play_left_to_right` helpers — migrated in-place-preserving via assign-back), D8 (`spadl/orientation.py` `to_spadl_ltr` enrichment) are LATENT; D6 corrected a false ADR-042 "last identity-keyed module" claim. Guards are **PER-ROW, never aggregate** (D2 mean bias −1.1%, D3 −0.002 pass a mean check vacuously) with non-vacuity partners; the `_reproject_team_shape` gate is BEHAVIOURAL (`GEOMETRIC_NAME` can't see its infix names, measured blind spots), closing a y-axis-vacuous pre-existing test. `ActionFrameContext` gains a required `flip_by_action` field (empty = "reflect nothing" = D2 restored). **Hyrum/retrain: `bekkers_pi` away-team values change (home byte-identical) → re-materialize `fct_action_context.pressure_on_actor__bekkers_pi` → retrain ρ (both variants, must clear `ece≤0.10`/`|slope−1|≤0.25`) → re-run the xT-GK v2 deep-zone gate (its GO-leaning verdict was measured on broken pressure) — ONE ordered pass, bundle with the 4.52.0 TF-35 recompute.** No converter output changes; SPADL/atomic byte-identical. C4 count unchanged (30).

- **xT-GK v2 possession value** (`xtgk/`): PR-S107 (4.40.0, ADR-036) — the honest value surface `V(z,p)` that replaces xT-GK v1's flat destination-only raw-xT inside the v2 metric `ρ·[V(s′)−V(s)] − (1−ρ)·[V(s)+κ·V_opp]` (v1 `tracking/_xt_gk.py` is **frozen**, coexists until the lakehouse migrates its `xt_gk*` columns; two "xt-gk" homes documented here deliberately). Hexagonal: a `PossessionValue` Protocol + two adapters. **`MarkovPossessionValue`** (production) = pressure-stratified value iteration reusing `xthreat.value_iteration` **verbatim**, with (i) an **xG-calibrated first-shot immediate reward** `E[xG\|shot]·P(shot)` (`_xg_reward.xg_scoring_prob` — the xG analogue of `_grid._scoring_prob`; NOT the goal-gated `vaep.labels` possession surface, which reproduces the v1 flat deep zone), (ii) a **goal-kick-inclusive move-set** (`_moves.MOVE_TYPE_IDS` = pass∪dribble∪cross∪goalkick∪throw_in — the metric scores keeper distributions, which classic xT's move-set excludes), (iii) pressure terciles (`PressureLevels`). `V(z,p) = E[xG of the possession's FIRST shot | ball at z under pressure p]` (the shoot branch is terminal; deep-zone value is pure forward propagation). **xtgk-local builders** (`_moves.xtgk_transition_matrix`/`xtgk_action_prob`) reuse `xthreat`'s low-level seams (`_kde_transition_from_grouped`, `_zone_centres`, `_get_flat_indexes`, `_count`) and modify **NO** `xthreat` source — Singh consumes all-results moves, KDE the success-filtered set (the two need different populations, so a single injected `move_actions=` on the public builders would be wrong); classic xT stays **byte-identical** (parity-gated over random cohorts in `tests/test_xtgk_builder_parity.py` + the frozen oracle). **`EmpiricalPossessionValue`** (model-free cross-check, NOT shipped) = per-action first-shot empirical surface (O(n) reverse scan; independent of the Markov estimator so §8.5 disagreement is diagnostic — graded on BUILD-UP cells, never the ≈0 deep cells). **Pre-registered occupied-cell deep-zone gate** (`run_deep_zone_gate`/`GateConfig`/`DeepZoneGateReport`, `_diagnostics.py`) — the make-or-break go/no-go; operates on deep cells with ≥`n_min` support in ALL THREE terciles, effect floor + `expected_direction` + cross-check tolerance owner/Eyestone-locked before fitting (selection≠certification). **`delta_v`** = two-factor Shapley split (`ΔV_pressure + ΔV_position = ΔV`). **Injected `xg_column`** (silly-kicks ships NO xG model; sourced from the lakehouse `fct_shot_xg` mart on `(match_key, action_id)`); `V` fittable only where a calibrated per-shot xG exists. Pickle-free `save`/`load` (npz+JSON+SHA256, `_serialize.py`). Attack-orientation + column guard via `validate_possession_value_input`. In **NO** default xfn list (opt-in). Owner-run real-data gate `scripts/validate_xtgk_possession_value.py` (blocked on the locked gate numbers; WC2022/gradientsports cohort — SkillCorner is xG-`ood_flag`-uncertified in `fct_shot_xg`). C4-free (no action-coupled aggregator; count stays 28). Attribution: Singh 2018 (xT lineage), Eyestone (xT-GK v2). **PR-S109 (4.42.0, ADR-036 amendment) completes v2 in one release:** the metric assembler `xtgk._metric.compute_xt_gk_v2(actions, *, possession_value, retention, turnover_cost, kappa=1.0)` depending only on the three ports; four coherent additive decomposition columns **`xt_gk_v2_position`/`_pev`/`_retention_loss`/`_dzv`/`xt_gk_v2`** (namespaced away from v1's frozen `xt_gk_pev/rav/dzv`; PEV≡0 while `p′=p`, dormant pending receiver-pressure `q`; RAV=the total, not a bar). **SP2 `V_opp`** (`_turnover.py`): `MirroredTurnoverCost` = `V(mirror_zone(z), policy(p))` on the fitted V (zero new fitting; `mirror_zone`=180° reflection; injectable `p_opp=p` default) + `EmpiricalTurnoverValue` bounded-window cross-check + `_is_turnover`. **SP3 `ρ`** (`_retention.py`): `RetentionModel` port + `GkRetentionModel` (logistic pure-numpy serve, JSON+SHA256, provider variants) mirroring `GkCompletionModel`; new `retains(actions, *, window_seconds=10.0)` label (`_retention_labels.py`; retain iff team keeps ball-no-opponent-boundary OR shoots within the window; **truncated-window→NaN** = excluded, not falsely-retained; **4.50.0/PR-S117 hardening**: non_action/foul/NaN-team rows never decide + possession boundary requires both ids attested + canonical `(time_seconds, action_id)` scan order (input-row-order-insensitive at time ties; bare-`action_id` sort RULED OUT — the GS mart's action_id order disagrees with time_seconds) — all probe/gate-verified byte-identical on both live ρ cohorts → weights untouched, ADR-036 amendment 2026-07-17); **marts-native `extract_retention_features`** — 8 features (pass geometry + `pressure_on_actor__bekkers_pi`, the pinned pressure measure) sourced from the gold action marts (`fct_action_values`+`fct_action_context`), **NOT tracking frames (deprecated as an active source)** — so the frames-only receiver-density feature is absent and the GK-distribution domain is **goal-kicks** (`gk_was_distributing` is unpopulated on GS/SC — lakehouse handoff F1; the acting-GK-pass resolution is frames-based → out of scope); calibration gate `ece≤0.10 AND |reliability_slope−1|≤0.25` on every variant via the extra-free `silly_kicks/_calibration_metrics.py` (`ece`/`reliability_slope` extracted from the completion trainer so SP3/SP5 don't pull the optuna `[calibration]` extra); **`default` weights BUNDLED** under `xtgk/_retention_weights/default/` (GS 64-match goal-kicks OOF AUC 0.776/ECE 0.090/slope 1.01, PASS) via the marts-native `scripts/train_gk_retention.py` `__main__` + `_loader_databricks.load_retention_cohort`; **SkillCorner variant NOT shipped** (near-chance AUC 0.54 + fails the calibration gate at slope 0.63 under bekkers_pi → every provider falls back to `default`, `_PROVIDER_VARIANT={}`); ADR-011 does NOT govern this trained-light class (ADR-024 precedent). **Gate (Part 1)**: `GateConfig.relative_effect_floor` (primary, gate-enforced `|v_lo−v_hi|/mean≥0.25`); **zone-conditional terciles now REAL** (`PressureLevels.mode="zone_conditional"` per-band cutpoints, deep band xi∈{0,1}; `to_meta`/`from_meta` keep global on-disk byte-identical, absent `pressure_mode`⇒global); pre-registered three-rung ladder `run_gate_with_ladder` (global→zone-conditional→STOP, winning rung reported) + `run_gate_both_orientations` (mirror_y equivariance / mirror_x rejection); locked Q4 (`effect_floor=0.005`/`relative=0.25`/`n_min=30`/`decreasing`); RM included PROVISIONAL (100% OOD), not dropped. **SP5** owner-run `scripts/validate_xtgk_v2.py` (out-of-sample construct-validity vs completion/destination-V/v1, transfer, calibration; WC2018/Neuer stubbed). v1 `tracking/_xt_gk.py` FROZEN alongside v2 (M5; removed ≥1 release after lakehouse migrates); no v2↔v1 imports; xthreat+v1 byte-stability regression-gated. In **no** default xfn list (opt-in). **Gate RUN on real data (Databricks gold, `_loader_databricks.load_xtgk_cohort` = `spadl_actions⋈dim_matches⋈fct_action_context⋈fct_shot_xg`; pressure pinned to `pressure_on_actor__bekkers_pi`): GO-leaning — a real, decreasing, monotone deep-zone pressure gradient on BOTH cohorts** (WC2022 relative effect **0.86**, 8 occupied cells; RM **1.05**, 17 cells; `GATE_FINDINGS.md`). Not a clean PASS: WC2022's *absolute* effect (0.0027) sits under the pre-registered 0.005 floor (deep-zone first-shot xG is intrinsically tiny) and the empirical cross-check diverges — both Eyestone-review items (floor recalibration + cross-check audit), NOT the degenerate STOP. **The pressure measure was pinned to `bekkers_pi` after the lakehouse 3-method audit** (handoff F2): `andrienko_oval` floors to exactly 0 for ~47% of actions (link_zones ~80%, bekkers_pi ~5%) — a parameterization artifact; the initial andrienko run STOPped, superseded. Two real bugs surfaced+fixed en route: `prepare_cohort` now DROPS frame-absent tracking-gap nulls (was: `apply` raised); NaN-safe `flat_zones` at the four zone-binning seams (real coords have NaN; synthetic fixtures didn't). Per the owner build-ahead directive, SP2–SP5 shipped regardless. **PR-S111 (4.44.0, ADR-036 amendment) retrains ρ on the now-live broadened `is_gk_distribution` domain** (lakehouse F1 shipped it): the ρ domain is no longer goal-kicks-only but goal-kicks ∪ acting-GK open-play passes. `default` (GS) re-bundled at **AUC 0.781/ECE 0.031/slope 0.998** (n=2923, improved from the goal-kicks-only 0.776/0.090); **the SkillCorner variant NOW SHIPS** — the broadened domain (5477 rows) makes it viable at **AUC 0.650/ECE 0.020/slope 0.923, GATE=PASS** (vs the old near-chance 0.54), so `_PROVIDER_VARIANT={"skillcorner":"skillcorner"}` (other providers still fall back to `default`). New **F1 CI guard** (`tests/xtgk/test_retention_bundle_calibration.py`) certifies every bundled variant's recorded `metrics.json` clears the canonical `_ECE_MAX`/`_SLOPE_TOL` (+ tamper-pin). The transitional self-adapting loader probe is **collapsed** — `is_gk_distribution` is a HARD dependency (unconditional `SELECT`); MODEL_CARD pressure doc-bug fixed (`andrienko_oval`→`bekkers_pi`). **`compute_xt_gk_v2` serve output changes → xT-GK v2 retrain trigger** (opt-in; not a forced VAEP retrain). **Part C (ADR-019):** the shared `_gk_from_frames_linked` team predicate is now dtype-safe (`ids_equal` acting / `ids_differ` defending) — `acting_gk_from_frames` was already fallback-protected; the real fix is `defending_gk_from_frames` returning the true opponent (not the acting team's own keeper) on a cross-dtype team mismatch, byte-identical on matched/NA paths. **PR-S112 (4.45.0, ADR-036 amendment) ships the faithful V_opp + the honest construct-validity verdict.** `EmpiricalTurnoverValue` (`_turnover.py`) was rewritten from a bounded-window cross-check into the **faithful** turnover cost (Jeff §2.3): **possession-bound** by default (`window_seconds=None`, scan to the match boundary — a `game_id` fail-loud guard) + **support-gated hierarchical bin-widening** (native cell → coarse `coarsen×coarsen` block → global-per-pressure; `min_support=30`=gate `n_min`) so a 1–2-turnover deep cell is not a noise estimate (`resolution_level` + module `surface_divergence` audit). It replaces the mirror proxy `MirroredTurnoverCost = V(mirror_zone(z))`, which over-stated deep opponent threat ~10–50× at real support; `_metric.py` unchanged (`turnover_cost` stays injected). **The faithful V_opp genuinely un-swamped the metric** (deep `dzv` share 87–89%→**29%**; `ρ·ΔV` 8%→**36–42%**), but **out-of-sample on real gold, xT-GK v2 is NOT construct-validated**: outcome-AUC lift over `max(baselines)` **GS −0.139 / SC −0.072**, and action-level keeper-discrimination ICC (grouped by resolved `player_key`, R2-not-CV) **v2 −0.002/0.011 vs v1 0.019/0.018 — both near-zero (still keeper-flat)**. Reported honestly per §3 (a-priori params fixed, not retuned; κ=1 headline, κ-sweep for Jeff); open forks flagged not patched (V=`E[first-shot xG]` vs Jeff §2.1 remainder-of-possession; PEV dormant). Faithful adapter ships regardless (it is the correct turnover cost); the deep-zone gate (reads V, not V_opp) untouched + still GO-leaning. Reports in `docs/research/xtgk_v2_construct_validity/`. C4 count stays 28. **PR-S113 (4.46.0, ADR-036 amendment) fixes the fabricated-origin defect the 4.45.0 verdict was measured on.** `flat_zones` maps a NaN coord to `(0,0)` → **zone 176** (own-corner cell) — a **FIT-PATH contract** (safe only because every fit seam `dropna`s: `_moves`/`_xg_reward`/`_markov`/`_empirical`/`_turnover` drop *before* calling in; `_markov.py:65`/`_empirical.py:83`/`_diagnostics.py:123` pass NaN *through* for tercile binning then drop) that was **FALSE at the one SCORING seam** (`_metric.py`), which dropped nothing → a NaN-origin goal-kick scored as a **real number at a location it never had**. Compounding: `load_xtgk_cohort` read **raw** `bronze.spadl_actions.start_x` while the **resolved** keeper origins already sat in `fct_action_context.xt_gk_origin_x/_y` (PR-S101/4.36.0) — in the table it already `LEFT JOIN`s, never `SELECT`ed — and the v1 comparator **did** use them, so the 4.45.0 v2-vs-v1 head-to-head was **never apples-to-apples**. **Measured live:** **946/3874 GS rows (24.4%; 60.2% of its goal-kicks)** scored at zone 176 (530 rescuable + 416 now-honest-NaN); **971 SkillCorner goal-kicks** carried a *present-and-wrong* origin (the broadcast **BALL**, not the keeper — ADR-024/PR-S104) → a `fillna` **coalesce** would fix GS and silently miss SC, so the rule is **OVERRIDE, not coalesce**. New public pure **`apply_resolved_gk_geometry`** (`_resolved_geometry.py`; no I/O, returns a NEW frame; stamps a 7-value **`gk_geometry_source`** — `off_domain`/`native`/`resolved_origin`/`resolved_dest`/`resolved_both`/`unresolved`/`unattested`, where **`unresolved` wins** on any still-non-finite coord and an absent resolved-column set → warn + no-op + **`unattested`** never `native`; missing `domain_column` → `ValueError`) + **`finite_coord_mask`** (adjacent to `flat_zones`, whose docstring now states the fit-path-only contract). `compute_xt_gk_v2` gains a **NaN guard** (non-finite rows → NaN across all 5 outputs, never a fabricated zone; **ρ is not scored on them**, closing `_retention.py:81`'s silent training-mean imputation *without* changing `predict_proba`), a **coordinate-coherence check** (recomputes the coordinate-derived ρ features from `actions` and **raises** on divergence — catches resolved-actions/raw-features, its mirror, AND mart-vintage divergence with one rule; must span the **origin**-derived features since the dest override is a **measured no-op**), and a **warn-once attestation** (`.any()` unattested, so a mixed-vintage frame can't score raw rows silently). Both loaders apply the helper → all **four** consumers inherit it (incl. `validate_xtgk_possession_value.py`, the gate script — so a *future* gate re-run would fit V on different coords than 4.42.0 did). ρ **retrained**, both variants PASS: `default` 2923→**3451** rows (the goal-kicks NaN origins had excluded from training entirely), AUC 0.781→**0.798**; `skillcorner` **5477 rows identical** (nothing added/dropped — only the *geometry* corrected), AUC 0.650→**0.662**. **Construct-validity re-run (2 legs, everything else frozen): the "keeper-flat" leg does NOT survive** — GS v2 ICC **−0.0020 → +0.0256**, now **above v1's 0.0193** (a fabricated origin is keeper-INDEPENDENT, so it compresses between-keeper variance toward zero; removing it restored the signal) — **but the outcome-AUC leg stands** (GS −0.1474, SC −0.0268; v2 still loses to `raw_completion`). **Deep-zone gate NOT re-run** (fit seams `dropna`; regression-gated by `test_deep_zone_gate_nan_invariance.py`, which carries a **non-vacuity meta-assertion** — its first draft compared an all-zero surface to an all-zero surface). **ADR-025 is NOT breached**: the override is a **transient scoring-time view on a copy**, never written back to a mart; the side-band idiom was rejected because it would push provenance policy into the metric engine. **Lakehouse handoff:** call `apply_resolved_gk_geometry` before `compute_xt_gk_v2` **and keep `is_gk_distribution` (or the stamp) on the frame** — the warn-once needs a domain column, so a pre-filtered slice with the flag dropped scores raw origins silently (the coherence check does NOT cover a uniformly-raw pair). **xT-GK v2 re-materialize trigger** (opt-in; not a forced VAEP retrain). C4 count stays 28.
- **GKDV — GK Deterrent Value** (`gkdv/`): PR-3 of the TF-19 cycle (ADR-043; spec `docs/superpowers/specs/2026-07-12-tf19-gkdv-regate-and-v1-design.md`). Counterfactual valuation of keeper POSITIONING: how much does the actual keeper's position change the attacking team's space and threat versus a league-average "ghost" keeper in the same frame state? **Depends on `silly_kicks.tracking` PUBLIC seams ONLY, never the reverse** — `tracking/` must never import `gkdv/` (pinned by `tests/gkdv/test_import_allowlist.py`; the same rule ADR-037 set for `tracking/_model_eval.py`). **Engine** (`_engine.py`): `build_ghost_frames(frames, *, home_team_id, model=, carrier=, params=)` → `(counterfactual_frames, provenance, report)`, PURE (never mutates `frames`); domain = alive ball ∧ in-possession team attacking ∧ ball within `domain_ball_to_goal_m` of the attacked goal ∧ defending-GK row present; the ball carrier is pinned ONCE and shared by the domain filter and the serving seam. **A NaN/missing ghost is dropped-and-COUNTED, never scored as Δ=0** — a zero delta from a missing keeper reads as "no deterrence" and biases keeper aggregates toward the null (`GkdvReport` conserves exactly: `n_frames_scored + Σ drop_reasons == n_frames_in`); a non-finite served ghost RAISES rather than making the keeper vanish. `counterfactual_frames` is the FULL input with only the defending keeper substituted, so consumers MUST restrict to the scored set via `provenance_to_targets` / `provenance["drop_reason"].isna()` before differencing. **Arms** (`_arms.py`), both in ATTACKER-VALUE units as `actual − ghost` so **negative = deterrent** uniformly: `delta_das` (accessible space; direction is pinned ONCE on the FACTUAL frames and the SAME pinned column feeds BOTH legs — accessible-space otherwise infers direction per period from a team-x mean the ghost displacement perturbs, so the two legs could infer OPPOSITE directions and the difference would not be a counterfactual at all; routed through `get_individual_das` summed per team via the narrow `_das_port.py`, since `get_das` hardcodes `infer_attacking_direction=True`) and `delta_threat_suppression` (`compute_threat_pc(actual) − compute_threat_pc(ghost)`). **Asymmetric keeper modelling, by construction:** the threat arm carries `lambda_gk` (a keeper GAIN applied after the influence field, so keeper position enters via TTI) while ΔDAS is **keeper-BLIND** (accessible-space's column map has no `is_goalkeeper`), i.e. ΔDAS measures relocating a GENERIC player. **`method="spearman"` is a HARD API constraint** enforced in `GkdvParams.__post_init__` — a GK-blind pitch-control configuration is *unrepresentable*, not merely discouraged, and the check lives in ONE place (the arms deliberately do not re-check). **Neither arm accepts a `pitch_control_cache`, and that is CORRECTNESS not performance:** `PitchControlCache` keys on frame IDENTITY (`game_id, period_id, frame_id, team, method, params, ball_position, decompose`) and excludes player positions, so a ghost frame — which carries its twin's identity — would be served the factual leg's surface and every delta would collapse to exactly zero with no warning. Ships **no `add_*` aggregator** (C4 action-coupled count unchanged) and is in **no** default xfn list. §6.4 Layers 0–3 and the xS-arm probe (PR-3b) are NOT in this PR. Attribution: Le et al. 2017 (ghosting); DEFCON-GNN arXiv:2512.10355 (comparator only, not implemented).
- **Atomic-SPADL** (`atomic/`): Variant where actions are decomposed into atomic sub-actions (receival, interception, out, etc.).
- **Providers** (`providers/`): Per-provider raw-data **parse ports** (bytes → provider-canonical bronze → converter input). **SK-S95 (4.30.0, ADR-031 T3):** `providers/sportec/parse.py` single-sources the IDSSE/Sportec **DFL parser** as a **verbatim lift** of the luxury-lakehouse parser (`ingestion/idsse.py` + `finalize_bronze_df` + `idsse_native_match_id` + the `convert.py`/`spadl_adapter.py` shapers, pinned @ `0efac60`): `parse_dfl_match_info`/`parse_dfl_tracking`/`parse_dfl_events` (DFL XML → RAW bronze) + `shape_tracking_to_native`/`shape_events_to_native` (bronze → `tracking.sportec`/`spadl.sportec` `EXPECTED_INPUT_COLUMNS`) + `derive_idsse_home_team_start_left{,_extratime}`; typed returns `MatchInfo`/`SportecTrackingBronze`/`SportecEventBronze` (silly-kicks' own domain names — a versioned cross-repo bronze contract, ADR-031 N1). Behind the `[parse-dfl]` extra (pure stdlib `xml.etree` + pandas/numpy — no new runtime dep). **Emits RAW bronze — data-quality (Savitzky-Golay smoothing, velocities) stays consumer-side** (the harness applies `_preprocess` after shaping; a delete-and-depend lakehouse keeps its own `_smooth_tracking` after the parse). The dev harness `scripts/_loader_pining.py::_build_idsse` is re-routed onto the port → native converters, retiring the y-inverting loader-local kloppy `_kloppy_tracking_to_frames`; the action `team_id` is remapped `"home"/"away"`→DFL-CLU so the ADR-028 action↔frame join aligns with the CLU-keyed frames. Golden parity (`tests/providers/sportec/test_parse_port_parity.py`) pins the lift to real lakehouse output on a reduced WC2022 IDSSE slice (`tests/datasets/sportec/idsse_slice/`, `SOURCE_SHA`). **VAEP/tracking-retrain trigger** for IDSSE calibration/pining consumers (N6 — action-anchored feature values change; the new path aligns action↔frame y to ~0.2 m vs ~11.8 m on the old kloppy path). C4-free (no new action-coupled aggregator; count stays 28).
- **Calibration** (`calibration/`): TF-24 Optuna calibration harness (optional `[calibration]` extra; lazy-import-guarded, NOT imported by `silly_kicks/__init__`). Pure provider-agnostic objectives/CV/gates: `CarrierAccuracyObjective` (Stage 1 carrier accuracy, maximize) + `AugmentedVaepBrierObjective` (Stage 2 augmented-VAEP held-out Brier, minimize) — a `ruthless` `CachedObjective` with invariant-prepare + per-trial-patch and a deterministic-XGBoost cache-equivalence guarantee (`assert_cache_equivalence`, 1e-9); match-stratified CV (`GroupKFold(5)` for >7 matches / leave-one-match-out ≤7); `FrozenXt` frozen **exogenous** xT artifact (fit once on a corpus disjoint from the calibration matches, sha256 + fail-closed exclusion, train–serve-consistent); H1 stateless default-Brier-anchored penalty + per-provider signal-sanity + DAS-degradation surfacing + TF-25 provider-specific-defaults gate. I/O lives in `scripts/calibrate_tracking_defaults.py` (CLI) + `scripts/_loader_pining.py` (SkillCorner/IDSSE public, Gradient Sports owner-tier) + `scripts/_loader_databricks.py` (bronze). The harness **recommends** values + an auditable manifest; it does NOT change the library default constants (separate "apply" PR). Decision: ADR-009. **SK-xT-3 (4.20.0)** adds `XtBandwidthObjective` + `xt_bandwidth_config` + `scripts/calibrate_xt_bandwidth.py` — a per-corpus `bandwidth × GridSpec-resolution × adaptive` sweep minimizing K-fold held-out xT **transition-NLL** (Singh baseline reported alongside; recommendation scoped to destination-likelihood with a reported Spearman-vs-goals cross-check, ADR-009 amendment + ADR-021). Plain duck-typed objective (resolution-keyed lazy cache, NOT a `CachedObjective`); caches the small per-`(grid,fold)` grouped destinations and shares the vectorized gaussian `_gaussian_transition_from_grouped` seam with `xthreat` (gaussian KDE re-pinned to the vectorized core — Chesterton-verified single caller; sklearn retained for non-gaussian kernels). Does not change library defaults.

## Key conventions

- No pandera — schemas are plain Python dicts (`SPADL_COLUMNS`, `ATOMIC_SPADL_COLUMNS`).
- Config DataFrames (`actiontypes_df()`, etc.) are cached with `@functools.cache`.
- Vectorized dispatch: converters use `np.select` over pre-flattened columns, not `apply(axis=1)`.
- All `warnings.warn()` calls include `stacklevel=2`.
- ML naming conventions (uppercase `X`, `Y`, `Pscores`) are allowed in `vaep/` and `xthreat/*.py` per ruff per-file-ignores.
- **Converter identifier conventions are sacred.** SPADL DataFrame converters never override the caller's `team_id` / `player_id` columns from provider-specific qualifiers. Qualifier-derived facts surface as dedicated output columns (see `tackle_winner_*` / `tackle_loser_*` on sportec). Decision: ADR-001.
- **Block-detection columns are cross-provider + nullable (ADR-046, 4.56.0).** Every converter emits `shot_blocked` + `cross_blocked` (in `SPADL_COLUMNS`, pandas `"boolean"`) via the shared `spadl/utils._blocked_flag` helper with 3-valued semantics: `True`/`False` on shot/cross rows the provider encodes a block, `pd.NA` on non-shot/non-cross rows AND on providers that cannot encode it (Opta unverified; SkillCorner absent — real-data verified both tiers; StatsBomb `cross_blocked` deferred at n=1; Sportec/Metrica/kloppy `cross_blocked` infeasible). `cross_blocked` = open-play `cross` only (set-piece crosses `pd.NA` in v1). Additive → **no retrain**; declared `"invariant"` in the ADR-045 reflection registry; atomic-SPADL unaffected (projects to `ATOMIC_SPADL_COLUMNS`). TF-51 prerequisite. GS `crossOutcomeType=="B"` pining-probed on real WC2022.
- **Public enrichment helpers (post-conversion `add_*` family) tolerate NaN in caller-supplied identifier columns.** NaN identifiers route to the documented per-row default (typically NaN-output / False / 0); helpers never crash on NaN input. Decoration with `@nan_safe_enrichment` from `silly_kicks._nan_safety` is the formal opt-in; the CI gate at `tests/test_enrichment_nan_safety.py` auto-discovers decorated helpers. Decision: ADR-003.
- **Public `add_*` enrichers are PURE — input-mutation is CI-gated (ADR-033, 4.32.0).** Every registered `add_*` (the full public surface across `spadl`/`atomic.spadl`/`tracking`/`atomic.tracking` — `__all__` UNION `.features.__all__`, since the 15 `atomic.tracking.features` mirrors export via the submodule, not the package) must NOT mutate any caller-supplied DataFrame/Series/ndarray and MUST return a NEW object. Gate: `tests/test_add_star_purity.py` — one canonical `PURITY_ENTRIES` registry (key `"<package>:<add_name>"`), build-fresh-owned-inputs-once + snapshot-every-array-arg + value-equality + `out is not input`; two meta-assertions pin the surface to the public export (a new `add_*` must register or CI fails). A best-effort AST heuristic NUDGES toward per-branch coverage. **Contributor contract:** any `add_*` that *conditionally* adds columns (a present/absent branch) MUST register ≥2 purity variants (both branches) — the heuristic is a nudge for the one known shape, NOT a guarantee, so the contract is the real backstop. Born from the `add_gk_distribution_metrics` gk_role-present in-place-mutation defect (the gk_role-absent path always copied, so the old column-list guard never caught it). Same PR renamed `pitch_control_at_action`→`pitch_control_at_target` (function aligns with its emitted `pitch_control_at_target__<method>` column base, unchanged since 4.31.0) — note the lakehouse keeps its own DEFCON `pitch_control_at_action` mart column (different semantics; not silly-kicks').
- **GK-domain keeper identity: use the RESOLVED `player_key`, never raw `player_id` (4.45.0/PR-S112).** SPADL `player_id` is **null for goal-kicks by design** (the taker isn't in the raw event — the reason `acting_gk_from_frames`/the lakehouse goal-kick-taker override exist), and it was 100% null across the whole GK-distribution domain in the gold marts. The resolved Kimball surrogate `fct_action_context.player_key` (99.9% populated on `is_gk_distribution` rows) is the keeper identity. Analysis loaders source resolved fields from GOLD marts, NOT raw `bronze.spadl_actions`. CI guards: `tests/xtgk/test_player_key_contract.py` (pure SQL-source guard + an `@e2e` `player_key ≥99%` data-contract that catches both a wrong-column read and a resolver regression).
- **Every band needs a test from BOTH sides, and every counterfactual needs a non-vacuity assertion that it actually moved something.** A gate that only asserts "value is inside the band" passes identically when the value is inside the band *and* when the computation silently produced nothing; a counterfactual test that only asserts `delta == expected` passes when both legs are secretly the same object. Assert the failing side too (a mutation that SHOULD move the number out of the band), and assert the counterfactual measurably differs from its factual twin. **Four silent-null defects in this codebase share exactly one shape** — the kloppy tracking y-inversion (ADR-031), the fabricated `flat_zones` grid origin (ADR-036/PR-S113), the identity-keyed `PitchControlCache` served to a moved-GK counterfactual (ADR-043), and a mirrored external-provider event frame — each produced a plausible number from a computation that had not happened. None was caught by a one-sided assertion; each was caught by a test that checked the other side. Precedents: `test_deep_zone_gate_nan_invariance.py`'s non-vacuity meta-assertion (its first draft compared an all-zero surface to an all-zero surface), the `test_ghost_gk_mirror_invariant` off-centre probe (ADR-040 strengthened it because the central probe moved ~0.1 m and was vacuous), and `tests/gkdv/test_arms.py::test_unpinned_implementation_would_measurably_differ`.
- **Private modules can have downstream consumers.** Before renaming, splitting or relocating any `silly_kicks/**/_*.py`, check `docs/PRIVATE_CONSUMERS.md`. Path pins fail **silently** — a renamed module degrades a consumer's guard with no `ImportError`.
- **Artifact-writing drivers REFUSE a dirty tree (4.63.0, `scripts/_provenance.py`).** Any `scripts/` driver that writes a registered artifact calls `require_clean_tree(git_provenance(), allow_dirty=...)` FIRST — before paying for any corpus work — and stamps `run_commit` + `run_tree_dirty` into its output. The reason is measured, not hypothetical: `git rev-parse HEAD` returns the same SHA whether or not the tree is modified, so a driver stamping the bare SHA records a commit that does **not** describe the code that ran. A corpus pass was launched exactly that way (HEAD clean, three drivers modified); the resulting numbers would have carried a verifiable-looking false provenance, which is strictly worse than none. Absent git counts as dirty, never clean. `--allow-dirty` permits a dev run but the artifact still records `dirty: true` — the escape hatch must never launder the fact. **Enforcement belongs in `main()`, not in the work function** (a `run()` that refuses to execute on a dirty checkout cannot be tested without mocking git; the CLI refuses, `run()` records the truth). **CI-gated as of 4.65.0** (`tests/scripts/test_provenance_wiring.py`): the rule was enforced by memory and memory had already failed twice — `validate_xshot_causal.py` wrote the §3.3 entanglement artifact with no provenance at all, and `validate_xs_probe.py` stamped a bare `git rev-parse HEAD` into the cited 4.60.0 result. The gate requires every artifact driver to import the helper, offer `--allow-dirty`, never shell out to `rev-parse` (AST-matched on CALLS, so prose describing the defect is not mistaken for committing it), and call `require_clean_tree` from `main()`. An artifact whose inputs came from another driver (e.g. the arm-values table feeding the §6.1 ICC number) needs provenance on BOTH, or the clean SHA on the downstream metrics launders the dirty upstream input.
- **Every corpus driver adopts the shared seam `scripts/_driver.py` -- CI-gated, not prose (ADR-052, 4.72.0).** This rule was prose for two releases and prose was not enough: of 21 in-population drivers, **three** survived a crash and **fourteen** held every result in memory and wrote once at the end. `for_each(items, key=, work=, shard_root=, token_inputs=)` owns the loop -- it STREAMS its corpus (never `list()`s it: `load_matches` parses a full tracking frame per match inside the generator), writes one shard per item into a generation DIRECTORY named by a `ruthless.fingerprint` digest of the DECLARED inputs, skips an item whose shard exists, prints a FLUSHED `[i/n]` line, records failures rather than losing the pass, and asserts conservation over its OWN keys. **An empty result still writes a shard** (absent = "not yet run", present-and-empty = "ran, produced nothing"; conflating them recomputes every barren item forever). The primitives are the escape hatch and a driver on that path must call `assert_conservation` **AND** `_require_injective` -- conservation alone is SATISFIABLE BY A LOSSY RUN, because a colliding key makes two items share one shard and `present` counts it once per duplicate key (measured: 2 items -> 1 processed -> conservation returns `(2,2)` and PASSES). **`reconcile` requires a partition surface**; selectors are deliberately outside `token_inputs` so narrowing reuses shards, which makes the generation a SUPERSET of any one run, so every other adopter combines from `res.keys`. Corpus-scoped counters survive resume via a `<key>.counters.json` sidecar (pass-scoped `n_attempted` deliberately does NOT -- a resumed pass must be able to declare it built nothing); use `CorpusPassResult.manifest()`, since a hand-written `manifest_fields(...)` silently took the old `counters_unrecorded=0` default and reported a complete corpus of nothing. **What it does NOT prove:** token completeness (a wrong declaration digests stably), and a SECOND uncounted loop -- that writes no shards and lists no keys, so nothing sees it; a fan-in check is the recorded follow-up. Two rules the migrations produced: `for_each` resumes WORK, never the PRODUCTION of its items, so invert onto an id list only where `work` is unambiguously trivial next to the load; and the `work -> tidy frame` contract carries no per-item METADATA (use the documented `counters(item, frame)` closure) and no non-tabular side state at all.
- **An expensive corpus pass is its OWN shardable driver, and an inestimable replicate is COUNTED (4.65.0, `scripts/build_layer2_spells.py` + `causal/power.py`).** **MECHANISM SUPERSEDED by ADR-052 (4.72.0) — the POLICY below still holds in full (per-match shards on completion, `--match-ids-json` partitionability, atomic writes, counted degenerate replicates), but shard writing and combination now live behind `scripts/_driver.py`: `build_layer2_spells` no longer calls `write_table_atomically` at all (measured: zero references), it calls `for_each` + `reconcile`, and `_partition.py` is retained for MANIFEST aggregation and partitioning. Read this bullet for the WHY, the seam bullet above for the HOW.** Any `scripts/` pass whose per-match work costs minutes writes **per-match shards on completion** (an existing shard is skipped, so a crash resumes) and accepts `--match-ids-json`/`--list-matches` so N processes share one `--out`. Measured: the power driver built its spells INLINE, spent **8.7h** walking 64 matches, then raised in the cheap analysis step that followed and lost every one of them — while the arm-values pass, which had shards, survived its own 64-match run twice. The consumer takes the persisted table (`--spells`, mirroring `--arm-values`), so a crash downstream costs seconds. Reconciliation lives in the shared `scripts/_partition.py` because it has already been wrong once (N workers writing one manifest let the last writer win, so a 64-match artifact reported one partition's `n_matches: 8`) and must not be fixable in one producer while live in the other; it also checks **commit consistency** across workers, since nothing stops one being launched from a different checkout. **A partition that names NO ids for a provider must DROP it** (`providers_for_slice`): the shared loader reads `(match_ids.get(p) if match_ids else None) or list(manifest_ids)`, so an empty list (falsy) and an absent key (None) BOTH expand to the entire manifest — correct for an unpartitioned caller, exactly inverted for a worker, and measured to make every worker of a multi-provider driver load the unsliced providers in full. Two corollaries of running N writers against one output dir: tables are written via `write_table_atomically` (private temp + `os.replace`), per-match shards included because N workers rebuilding it from the shared shard dir means N concurrent writers on one path, and the CONSUMER (`run_signoff_power.py --spells/--arm-values`) reads each input's sibling manifest and REFUSES a dirty upstream, a **missing** manifest (unprovenanced == dirty), or one whose workers ran at different commits — extending the ADR-037 rule above from "this run's tree" to "every artifact this number derives from". Statistically: `att_power_curve` scores a single-treatment-class resample as a **non-detection** and reports it in `n_degenerate_by_size` — never a crash (it used to raise out of sklearn) and never a silent skip. Counting is load-bearing in the same way conservation is: a size whose degenerate count approaches `n_replicates` reports an **inestimable design at that n**, which is not a weak effect, and excluding those replicates from the DENOMINATOR would condition on estimability and inflate the curve exactly where the design is weakest.
- **Academic attribution discipline.** Every new feature implementing a published methodology gets an entry in the `NOTICE` file's "Mathematical / Methodological References" section. Cross-link from per-feature docstrings via `See NOTICE for full bibliographic citations.` Mirrors the lakehouse pattern. Decision: ADR-005.
- **Aggregator column liveness is CI-gated (non-NaN AND non-constant).** Every registered `tracking` `add_*` (the full `__all__` surface, jersey helper included) must produce non-null values for every column it ADDS, and every float METRIC column with ≥2 observed values must carry >1 distinct value, on the multi-domain fixture in `tests/tracking/test_aggregator_column_liveness.py` (5 action windows: pass / shot / GK goalkick / attacking-third / wide-area; actor carries the ball through a final-step kick; per-window velocity/layout/GK/kick variation; event clocks jittered off the frame grid). A meta-assertion pins the gate surface to `tracking.__all__`, so a NEW `add_*` must wire a liveness entry or CI fails. Constants BY DESIGN go in the `STRUCTURAL_CONSTANTS` registry with a justification + dedicated invariant test — never silent exclusions. The registry is currently EMPTY: the space-creation structural zeros were RETIRED from the contract (4.24.0), and the dead `pitch_control_at_ball__spearman` near-ball-PPCF degeneracy was RETIRED + replaced by the LIVE `pitch_control_at_target__spearman` (sampled at the action destination + ADR-028-reprojected; 4.31.0, ADR-032). Born from the TF-41 hard-coded-NaN `_opponent` defect (ADR-026; non-constant added 4.24.0 after `space_destroyed_m2 ≡ 0` showed non-NaN alone is insufficient).
- **Linkage-provenance columns are idempotent.** All tracking `add_*` aggregators that merge provenance columns (`frame_id`, `time_offset_seconds`, `link_quality_score`, `n_candidate_frames`) must skip the merge when those columns already exist on the input DataFrame. This allows callers to chain multiple `add_*` calls without producing `_x`/`_y` suffixed duplicates. Pattern: `if not any(c in out.columns for c in provenance_cols):`. CI gate: `tests/tracking/test_provenance_skip_guard.py`.
- **Pre-linking optimization via `links` kwarg.** All tracking `add_*` aggregators accept an optional `links: pd.DataFrame | None = None` kwarg. When provided, the function skips its internal `link_actions_to_frames` call and uses the caller-supplied pointers. Pipeline callers (e.g. lakehouse) pre-link once and pass `links` to all enrichment steps, reducing N × 2-5s to 1 × 2-5s per match. The `links` DataFrame must be the pointers output of `link_actions_to_frames` (columns: `action_id`, `frame_id`, `time_offset_seconds`, `n_candidate_frames`, `link_quality_score`). Internal helpers (`_resolve_action_frame_context`, kernel functions) also accept `links` for full thread-through. `infer_ball_carrier` additionally accepts `pre` (a precomputed `_pre_index_frames(frames)`) and `ball_carrier_at_action` accepts both `pre` and `links`; the pre-index dominates carrier cost (~99%) yet is param-invariant, so the TF-24 Stage-1 calibration objective caches `pre`+`links` per match (invariant-prepare) and re-runs only the carrier kernel per Optuna trial (~100× faster sweep; bit-identical).
- **Shared pitch-control surface via `pitch_control_cache` kwarg.** All pitch-control-consuming aggregators (`add_obso`, `add_cover_shadows`, `add_gk_influence`, `add_player_influence`, `add_space_creation`, `add_pitch_control`) accept an optional `pitch_control_cache: PitchControlCache | None = None` kwarg (mirrors `links`). `PitchControlCache` (in `tracking/pitch_control/`) memoizes **canonical** per-frame surfaces keyed on `(game_id, period_id, frame_id, team, method, params, ball_position, decompose)`, so families that need pitch control on overlapping frames compute each surface once instead of once per family. Each aggregator uses a fresh local cache by default (within-pass reuse); pipeline callers pre-build one cache and pass it to all steps for cross-family reuse (like `links`). Only canonical-frame surfaces are cached — **counterfactual** (player-removed) surfaces in `cover_shadows`/`space_creation` are never routed through the cache. Zero global state. Decision: ADR-008 (TF-7).
- **Dtype-safe id comparisons are a REPO-WIDE contract (ADR-019, 4.15.0; scope widened 4.53.0/ADR-043).** No consumer anywhere in the package compares ids (action↔frame, action↔action, or against a scalar `home_team_id`/`player_id`) with raw `==`/`!=`, and none merges on id keys without alignment — those silently mis-resolve across dtypes (`Int64(366) == "366"` → False) or raise on a mixed merge key. Originally written as a *tracking-feature-seam* rule; it never was one, and 4.53.0 stopped pretending — the seam is the **public `silly_kicks.id_compat`**, imported by **39 modules across 6 packages** (`spadl/`, `vaep/`, `atomic/`, `causal/`, `gkdv/`, `tracking/`; measured by AST over `silly_kicks/`, excluding the module itself). ADR-043 promoted it out of `tracking/_id_compat.py`: five packages were reaching into a private *tracking* submodule, two via function-local imports in `spadl/utils.py` written to dodge a circular import, and a seam ADR-019 makes MANDATORY is public API by definition. **Breaking import-path change, no shim** — the pin fails loud at import, and a shim would have made the promotion cosmetic. Surface: `ids_equal`/`ids_differ` (column-vs-column, NA-safe both-present), `ids_match` (Series/array vs scalar), `same_id` (scalar↔scalar groupby loops), `align_join_keys` (pre-merge, numeric-vs-object only), `restore_id_dtype` (put a kernel-built id column back on its SOURCE dtype where that dtype can represent the result — numpy ints can't hold NA, so a result with unmatched rows stays float). One `_canonical` truth (integral-float collapse; genuine strings pass through). **The both-object fast path is CONTENT-probed, not free** (`_raw_comparable`): an object column of boxed floats raw-compares False against the same id as a string, so dtype alone cannot license a raw `==`. Probe cost ≈15% of the comparison per side, ≈30% for the guard. Opt-in loud guard: public `validate_id_dtypes` + `IdDtypeDiagnosis` (mirrors ADR-017's `validate_time_base`). CI gates: red-first **asymmetric** dtype-invariance gate over every registered `add_*` (`tests/tracking/test_id_dtype_invariance.py`; numeric actions × string frames + reverse, `home_team_id` independent axis) + meta-assertion (gate surface == registered surface) + the **`PUBLIC_ID_SCALAR_ENTRIES` registry** (`tests/invariants/conftest_id_scalar.py`, exercised by `tests/invariants/test_public_id_scalar_registry.py`) + structural de-dup perf guard. **The AST lint is DELETED as of 4.53.0/ADR-043 — deleted, NOT widened, and do not reinstate it.** Widening was rejected on principle, not cost: it was a NAME heuristic (it missed ADR-027's `t != action_team`, and `_ghost_gk`'s `str(t) == home_team_id_norm` because the scalar was renamed), but the unfixable part is that **a safe same-source compare and an unsafe cross-source one are the IDENTICAL AST** — only the scalar's *provenance* separates them, and no syntactic rule sees provenance, so any widening either flags correct code (breeding the exemptions that grew `ALLOW_MODULES` in the first place) or keeps missing real defects. **Complete by ENUMERATION where the lint was incomplete by HEURISTIC** (same idiom as ADR-003's NaN-safety registry and ADR-033's `PURITY_ENTRIES`): the registry derives its surface from `inspect.signature` over the `__all__` exports of `spadl`/`atomic`/`vaep`/`causal`/`tracking`, keyed by defining qualname — **102 functions: 77 exercised directly, 22 delegated** to the `add_*` gate (machine-checked against that gate's registered surface, not asserted in prose), **3 justified non-invariant** — each invoked on a matched scalar, a mismatched-but-value-equal one (`5` vs `"5"`), and a **float** one (`5.0`; not redundant — a naive `str(x)==str(s)` renders identically for ints, so only the float axis exposes it). Two meta-assertions pin it to the public surface in BOTH directions, so a newly-exported id-scalar function fails CI until registered, delegated or justified — the anti-rot property the lint's glob lacked. It caught two live `str()`-comparison defects on its first run that the lint structurally could not have seen. Fixed three latent bugs (object-`is_ball` `~`-no-op ball-as-opponent; `str(5.0)` iterrows-upcast player-influence/cover-shadow mislabel; object-path opponent join-miss) → **VAEP retrain trigger**. ADR-001 preserved (fix lives at consumer seams, not converters). **4.25.0 (ADR-027), retained as the historical case that killed the lint:** it missed a raw `t != action_team` in `_line_breaking.py` ward (a NaN-team crash + an Int64-vs-string silent opponent miscompute on GS) because the operands are not named `*_id`; the behavioral NaN-safety gate (`tests/test_enrichment_nan_safety.py`, NaN-team fixture) caught it. Fixed via `same_id` + a `pd.isna` NaN-route. **Behavioral gates, never a lint, are the backstop** — ADR-043 acted on exactly this by replacing the lint with the enumeration registry above. One site ships DELIBERATELY ungated (the velocity-state defending split, ADR-043 decision 6): its `gk_team` scalar comes from the same frame it is compared against, so no input can distinguish `ids_match` from `==` and no lint could flag it without flagging safe code — recorded so a future reader meets the reasoning instead of "fixing" an untested line.
- **Per-action geometry is emitted in the SPADL action-LTR frame (ADR-028, 4.26.0).** Actions are per-acting-team LTR (acting team attacks x=105); `convert_to_frames` is home-attacks-right (home attacks x=105 every period). They are a 180° point reflection apart for **away-team actions**, and the geometry layer historically sampled frame positions WITHOUT re-projecting → ~50% of tracking action rows wrong: visibly-bimodal absolute positions (`pre_shot_gk_x`, `defensive_line_x`, `team_shape_centroid_*`) AND silently-wrong mixed-frame scalars (`nearest_defender_distance`, `receiver_zone_density`, `defenders_in_triangle_to_goal`, all `pressure_on_actor__*`, `pre_shot_gk_distance_to_shot`/`_angle_*` — action anchor combined with frame positions). One frame serves BOTH teams' actions, so the caller cannot pre-orient; re-projection is per-action + library-owned. Fixed by ONE helper `tracking/_action_orientation.py` (`acting_team_attacks_rtl` keyed on the frame's `team_attacking_direction`, `align_join_keys` for the id join; `reproject_to_action_ltr`) at THREE seams: the shared `_resolve_action_frame_context` (fixes all 8 context kernels at once + makes their hardcoded goal-at-(105,34) correct), `_kernels._defensive_line_at_actions`, and `add_team_shape`/`_team_shape_at_actions`. `add_ghost_gk` emits action-LTR (`x→105−gr_x` uniform; `y→68−gr_y` away only) — model stays goal-relative; `compute_team_shape` made **orientation-aware** (deepest line nearest the defended goal; ltr/home byte-identical, only rtl/away changes). Self-reconciling features (`structural_pass`/`shape_graph`/`das`/`pitch_control`/`xt_gk`) DON'T use the context and are untouched (no double-flip). **CORRECTED in 4.52.0 (ADR-041): `obso`, `space_creation`, `pausa`, `player_influence`, `cover_shadows` and `gk_influence` were listed here in error** — they don't use the shared context, but they were NOT self-reconciling either; `obso` simply never handled orientation at all (`home_team_id` was a dead parameter), and `player_influence` multiplied a y-mirrored raw `interpolator()` grid. They are now reprojected AT THEIR OWN QUERY SEAM: the action-LTR target is point-reflected into frame coords and the attack-LTR EPV/threat grid is point-reflected (`[::-1, ::-1]`, BOTH axes) whenever `acting_team_attacks_rtl`. **CORRECTED AGAIN 2026-07-29 — that sentence is TRUE of `obso`/`pausa`/`player_influence` and FALSE of two of the six** (spec `docs/superpowers/specs/2026-07-29-adr028-orientation-defect-class-design.md`): `space_creation` was NEVER reprojected — `_compute_space_creation_for_action`'s `home_team_id` is a DEAD parameter (verified: identical output for `{HOME, AWAY, 999}`) and ADR-041 gave it only an unconditional `axis=(0,1)` opponent mirror, so its two emitted columns were EXCHANGED for every away action (**RC3, FIXED 4.71.0/PR-S139**: `compute_space_created` gains a keyword-only `attacks_rtl` threaded from the FRAMES via `acting_team_attacks_rtl`, and point-reflects the attack-LTR `transition_grid`/`epv_grid` — **the two GRIDS, never the finished `obso_multiplier`**, which also carries a ball-anchored `distance_weight` computed in frame coords that must never be mirrored, the rule the opponent branch already followed; reflecting at the grid seam corrects the opponent multiplier for free since it is built as a flip of the same artifacts. `home_team_id` stays in the signature and stays UNREAD — D3 retires it by disuse, not removal); and `cover_shadows` was only HALF reprojected — the RECEIVER at `_cover_shadows.py:1164`, never the PASSER at `features.py:3698`/`:3861` (**RC1, FIXED 4.70.0/PR-S138**: the passer is now reprojected INTO frame coords at both seams — everything downstream of that tuple is frame-convention and the xT lookup already reprojects itself; in the xfns path the reprojection precedes `_get_cs`, which rounds `passer_xy` into its cache key). A third, `gk_influence`, reprojects the right thing keyed on the WRONG thing: `_gk_influence.py:371-372` applies the both-axes point reflection but gates it on `same_id(attacking_team_id, home_team_id)` — team IDENTITY, not attacking DIRECTION — so it is correct only while frames are home-attacks-right (D3 re-key target). Durable guard: `tests/tracking/test_action_ltr_mirror_invariance.py` (hand-listed, 5 aggregators) PLUS `tests/tracking/test_mirror_registry.py` — a registry-driven pair of gates over all 33 registered `add_*`: **Gate A** (physical mirror; detects convention mixing) and **Gate B** (vary `home_team_id` on FIXED frames; detects identity-keyed direction, which Gate A is structurally BLIND to because swapping `home_team_id` restores the very invariant identity-keying assumes). **VAEP/tracking-retrain trigger** (away-team values change; home-team byte-identical). C4-free (no new aggregator/backend/model). Atomic mirror inherits.
- **Frame-LTR orientation is single-sourced for consumer-built frames (ADR-029, 4.27.0).** ADR-028's per-action re-projection only works on frames already in the canonical **home-attacks-right** convention; it no-ops on absolute frames (`team_attacking_direction` all-null) — filtering to `.notna()` rows yields an empty direction lookup → no flip → ~50% mirror-wrong geometry. Providers converted via `convert_to_frames(output_convention="ltr")` (sportec/GS) or the kloppy gateway (metrica/skillcorner) are already oriented, but a consumer holding **bronze DataFrames** (not a kloppy `TrackingDataset`) builds frames itself and must orient them. Public `tracking.orient_frames_to_ltr(frames, *, home_team_id, home_team_start_left, home_team_start_left_extratime=None)` is the **unlabeled-input** sibling of `play_left_to_right` (which serves already-labeled frames): pure composition of `compute_attacking_direction` (private) + `play_left_to_right` with fail-loud guards (missing-schema; already-labeled → raise + route to `play_left_to_right`; zero home-match (ADR-019); ET-without-flag). Two entry points by input state — labeled → `play_left_to_right`, unlabeled → `orient_frames_to_ltr`. **Consumer contract:** any consumer building frames from a non-kloppy source MUST orient before the geometry layer; the helper is only as correct as the caller-derived `home_team_start_left` (validate per game — e.g. defending GK lands near the attacked goal post-orient). Additive — no provider behaviour change, no silly-kicks retrain; the lakehouse re-materializes metrica/skillcorner after adopting it. The sportec/GS adapters are NOT refactored through the helper (primitives already shared; Chesterton's Fence). Positive ET orientation is regression-guarded on both the helper and the native adapters (`tests/tracking/test_adapter_extra_time_orientation.py`), prompted by a live GS-ET flip that was a consumer-side `home_team_start_left_extratime` placeholder bug, not a silly-kicks bug.
- **The kloppy TRACKING gateway pins the canonical SPADL coordinate system (ADR-031, 4.29.0).** `tracking/kloppy.py` previously emitted frames **y-mirrored** vs SPADL actions (`action_y == 68 − frame_y`) for SkillCorner / Metrica / IDSSE-dev: the EVENT gateway (`spadl/kloppy.py`) pinned `_SoccerActionCoordinateSystem` (now extracted to the shared `spadl/_kloppy_coordinates.py` + `socceraction_coordinate_system` helper, imported by both gateways), the tracking gateway never did, so frames kept each provider's kloppy-native vertical. Single-axis y mirror (NOT orientation — orthogonal to ADR-028/029; error `|68−2y|` → 0 at centre, ~full width at touchlines, which hid it). Fix: `convert_to_frames` pins the CS via a **CS-only `transform()` signature** — dropping `to_pitch_dimensions`, which otherwise silently OVERRIDES the CS vertical and leaves y inverted (Gate 0); the CS carries its own standardized 0–105/0–68 dims, so x is byte-identical. **NOT** a blanket `y=68−y` flip (which would double-invert an already-canonical provider; no-op-on-canonical guarded). DGX-verified: SkillCorner + Metrica both inverted → fixed (**calibration retrain trigger** — but the lakehouse builds SC/Metrica via its OWN bronze builders, so PR-S94 fixes the calibration/pining path + external gateway consumers, NOT lakehouse prod); the **native** sportec/IDSSE path is y-CORRECT (Gate D — no fix, no retrain). First of a sequence (PR-S94 / T1); the IDSSE/Sportec DFL **parse-port** single-sourcing (T3, PR-S95) + the lakehouse's own SC/Metrica builder y-check follow. C4-free (no new aggregator; count stays 28).
- **TF-23b geometric frame-LTR backstop on the native tracking adapters (ADR-035, 4.34.0).** `tracking.{gradientsports,sportec}.convert_to_frames` now self-correct a wrong/absent `home_team_start_left_extratime` from GK geometry: a shared `direction.finalize_orientation` tail (ET guard → per-period flag flip → period-gated label → the idempotent `orient_frames_to_ltr_by_geometry` backstop) replaces the duplicated inline orientation block in both adapters (**supersedes the ADR-029 no-refactor fence for the native adapters ONLY** — kloppy gateway stays intentionally un-routed). The net gains `on_missing_home` (default `"raise"`; adapters pass `"warn"` to keep their warn-don't-raise contract) + `copy` (default `True`; `finalize` passes `False` to drop the redundant 3rd full-frame copy on tracking-scale data) and **no longer orients period-5/PSO frames for ANY caller** (incl. the TF-23 SC/Metrica builders; PSO excluded from geometric analysis). **Byte-identical no-op on the correct-flag path** (DGX G1, strict-dtype: 69/71 corpus matches byte-identical; the only 2 changed are GS WC2022 ET-tracking matches **10506 + 10517** whose constant placeholder flag was geometrically wrong → **VAEP/tracking-retrain trigger for those 2 ET-frame sets only**). Closes ADR-031 **Gate D** (sportec/IDSSE-ET handedness; gated on synthetic sportec ET + the provider-agnostic mechanism — 0 real IDSSE-ET in the pining corpus, G3 re-scoped). C4-free (count stays 28).

## Testing

```bash
python -m pytest tests/ -m "not e2e" -v --tb=short
```

e2e tests require dataset fixtures not committed to the repo. Tests with
fixtures committed to the repo should not be marked e2e — they run in
the regular suite.

CI runs a bulk suite step serial (`--benchmark-skip`) on every matrix leg and a benchmark
*measurements* step single-threaded (`--benchmark-only`) on the primary leg only (see the
slow-test-gating note below for the per-leg split). There are no wall-clock
`assert ms < budget` perf tests — performance regressions are guarded by
**deterministic structural guards** (call-count spies on each function's dominant
primitive; see `tests/_perf_structural.py` + the `*_perf_budget.py` files), which
never flake on shared runners. (An xdist parallelization was tried and reverted: it
regressed py3.12 on the 4-core/7GB CI runners — memory/JIT pressure.)

**Doctests: CI executes `--doctest-modules` on the PUBLIC surface only** (`pytest --doctest-modules
silly_kicks/ --ignore-glob="*/_[!_]*.py"`, every leg; PR-S124, wiring guarded by
`tests/test_ci_doctest_wired.py`). The glob skips single-underscore
private modules while KEEPING dunder `__init__.py`; private-module examples are kept CORRECT (the
whole-package sweep is clean) but are NOT executed in CI, to bound wall-clock — the initial
enforcement scope. Most public examples are the canonical indented RST **literal block** (the honest
form for anything needing a real match's `actions`/`frames`/`xt` — no docstring can conjure them);
`tests/test_public_api_examples.py::_has_real_example` accepts a ≥4-space-indented non-`>>>` line in
the Examples section as a REAL example, so converting a failing `>>> f(actions, ...)` doctest to a
literal block satisfies BOTH the doctest run (nothing to execute) and the Examples gate. Genuinely
self-contained examples stay executable `>>>` doctests. Do NOT reintroduce `>>> f(x)  # doctest: +SKIP`
filler — the gate's `_demonstrates_something` rejects it (the 4.53.0 tightening).

**`@pytest.mark.slow` = expensive AND platform/interpreter-INVARIANT** (does-it-run train-script
smokes, same-run internal-consistency/parity, calibration cache-equivalence). These run **once on the
CI primary leg (`ubuntu-3.12`, matrix `primary: true`)**; every other leg runs `-m "not e2e and not
slow"`. Do **NOT** mark version-sensitive tests `slow` (golden-hash / snapshot / absolute-numeric — e.g.
`test_golden_*`) — they must stay on all legs (OS + interpreter axes), as must cheap behavioral-contract
guards (dup-`action_id`, id-dtype-invariance, orientation/roster). The matrix partition is guarded by
`tests/test_ci_slow_gating_wired.py`. Decision: ADR-023.

**Claims about a gate's behaviour must quote the assertion body, not its registration.**
A gate's registry (`ENTRIES`, `PURITY_ENTRIES`, `AGGREGATORS`, `_PUBLIC_MODULE_FILES`,
`_EXHAUSTIVE_EMITTED`) tells you *whether* a helper is exercised — never *what is asserted
about it*. Two PR-S119 review errors came from this, one on each side: a reviewer read the
liveness gate's registration and concluded a partially-`pd.NA` column would fail, when
`tests/tracking/test_aggregator_column_liveness.py:416` asserts only
`not out[c].notna().any()` (100%-null), and `:448`'s non-constant check is
float-dtype-gated so non-numeric columns are exempt entirely; and a plan asserted the
synthetic reachability grid was exactly y-symmetric and that symmetrizing it would cost
"≤ 1 ulp", when neither had been run (measured: 4.44e-16 across 3752/6400 cells). Same
failure mode both times — adjacent evidence substituted for the executed thing. **Rule: any
claim that a test passes or fails, or that a numeric property holds, carries either the
quoted assertion body or a pasted measurement.** Registration proves coverage; only the
assertion proves behaviour. Corollary for tolerances: measure at the scale you assert at —
grid-level float noise does not transfer to a derived area (a 3.3e-16 relative grid
asymmetry is not a 1e-12 absolute bound on an m² quantity).

## Open Items

See [TODO.md](TODO.md) for tracked work.

## Dependencies

- Runtime: pandas, numpy, scipy, scikit-learn (no pandera, no multimethod)
- Optional: kloppy (>= 3.18 for tracking parsers), xgboost, catboost, lightgbm, accessible-space (for DAS features), ruthless-efficiency[optuna]>=0.4.0 + xgboost>=2.0,<4.0 (for the `[calibration]` harness AND the `[train]` extra — shared trained-model HPO substrate; the ruthless floor was RAISED to 0.4.0 by ADR-052 because `ruthless.fingerprint` -- the structural, fail-closed digest `scripts/_driver.py` keys shard generations on -- only became public in 0.4.0, and the old 0.2.1 floor permitted a resolve where `from ruthless import fingerprint` raises ImportError (measured: 73 test failures on a stale `.venv312`); it also fixes a `warm_start` n_trials off-by-one, the xgboost bound protects the 1e-9 cache-equivalence gate's `tree_method="hist"` determinism). xS inference gates on the `[xgboost]` extra. numba (>=0.59) accelerates the pitch-control `@njit` kernels (ADR-008) and the ghost-GK `cpu-numba` KDE backend (ADR-013) via the `[numba]` extra (also bundled in `[test]` so CI exercises the `@njit` parity). `statsbombpy` is an OPTIONAL, `importorskip`-guarded dep used only by the network-gated xT open-data e2e (`tests/test_xthreat_statsbomb_e2e.py`, `@pytest.mark.e2e`); not in any committed extra — the test self-skips without it.
- numpy>=2.0 compatible
