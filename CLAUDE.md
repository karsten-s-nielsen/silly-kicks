# silly-kicks

Maintained fork of socceraction — SPADL event conversion + VAEP action valuation for soccer analytics.

## Architecture

- **Hexagonal**: All core functions are pure (pandas in, pandas out). Zero I/O, zero global state mutation.
- **Converters** (`spadl/`): Each provider (StatsBomb, Opta, Wyscout, Sportec, Metrica, Gradient Sports, SkillCorner, plus the Kloppy gateway) has its own module. All return `tuple[pd.DataFrame, ConversionReport]` with guaranteed columns/dtypes via `_finalize_output()`. Live per-provider contracts:
  - **Gradient Sports goal capture (ADR-018).** Own goals = `RE`+`shotOutcomeType=="G"` -> `bad_touch`+`owngoal` (conceding team/rebounder kept; a post-LTR own-half geometry tripwire warns+reverts a mis-located one). Cross-goals = `CR`+`G` -> keep the cross + synthesize a `shot`/`shot_freekick`+`success` by the crosser; synthesized rows carry `is_synthetic=True` (they share the parent's `original_event_id`). Voided `possessionEvents.nonEvent==True` events are excluded (optional column -- absent is an observable no-op warn, tallied in `excluded_counts`). `"O"` shot-outcome is off-target -> `fail`, NOT own-goal.
  - **GS null-actor rows carry NaN, never a sentinel `0` (ADR-027).** The null-actor duel/foul events (`OTB`+`CH`, `FOUL`+`FO`) have both `gameEvents.teamId` AND `playerId` NULL; GS schema ids are nullable `Int64`, not the `SPADL_COLUMNS` `int64`. A non-NaN sentinel bypasses `pd.isna` routing and crashes downstream opponent guards. ADR-001 forbids back-filling from duel/foul qualifiers; `_resolve_team_ids` fills team ONLY from a real same-match `player_id` (ambiguity raises). `convert_to_atomic` preserves source id dtype.
  - **GS dribble ends are derived GS-locally (ADR-018 amendment).** GS initializes `end=start` for every event and never runs `_add_dribbles`, so every `OTB`+`BC` shipped a placeholder. `_derive_end_coordinates` takes keyword-only `extra_type_ids` and ONLY gradientsports passes `{dribble}` -- the shared `_DERIVE_END_TYPE_IDS` stays untouched, because its `placeholder_end` guard cannot distinguish statsbomb's genuine stationary carries from placeholders. Period-last carries honestly keep the placeholder.
  - **GS ball-carry results come from the native `ballCarryOutcome`**: `R` (retained) -> success, `L` (lost) -> fail, unknown/absent -> fail. Without it every GS dribble fell to `fail`.
  - **SkillCorner `time_seconds` is period-relative** -- `_to_period_relative` subtracts the period-start offsets `{1:0, 2:2700, 3:5400, 4:6300, 5:7200}` (ADR-017).
  - **SkillCorner `result_id` routes through the native outcome** -- `pass_outcome` -> `received==True` (success-only) -> residual `same_team_next` -- and emits a dedicated `result_source` column (`native`/`inferred`/`stopgap`) in `SKILLCORNER_SPADL_COLUMNS`. `received==False` is NOT a fail (it can be completion to a non-targeted teammate).
  - **SkillCorner `_transform_coords` scales THEN clamps -- never call it on tracking (ADR-038).** The clamp is safe for events (on-pitch by construction) and destructive for tracking: a ball 9 m behind the goal snaps onto the goal line, erasing goal-vs-save. Tracking calls `_scale_to_spadl` (affine only) and nothing else. Pitch dims come from the events converter's own affine transform; missing dims RAISE, and `assume_standard_pitch=True` is the explicit opt-in.
  - **sportec pass/set-piece results use a kloppy-aligned success-ALLOWLIST** on the native DFL `play_evaluation` (`_extract_play_eval`/`_play_evaluation_is_fail`/`_warn_unexpected_play_eval`, single-sourced across the main and synth sites): `fail` iff the Evaluation is non-empty AND not in `{successfullyCompleted, successful}`, so unseen reason-coded failures fail by construction; empty/absent/NULL -> success (no mass-fail on a missing column); unexpected tokens warn. The punt-synth goalkick inherits its parent Play's eval.
  - **metrica is deliberately unchanged** -- measured-correct (PASS = completed pass; a loss is a separate `BALL LOST` event).
  - **Restart-coordinate enrichment is additive (ADR-025).** `spadl.add_restart_coordinates(actions, *, frames=None, links=None)` imputes NaN restart coords for the Law-fixed-spot types as NEW provenance-tagged columns (`enriched_start_x/_y`, `enriched_end_x/_y`, `start_coord_source`/`end_coord_source`, `*_coord_confidence`) and NEVER mutates canonical `start_x`/`end_x`. Single engine `tracking.resolve_restart_geometry`; `tracking.resolve_gk_geometry` is a byte-identical shim over it.
- **Tracking** (`tracking/`): Per-frame namespace parallel to `spadl/`. 20-column long-form schema (`TRACKING_FRAMES_COLUMNS`); native Sportec + Gradient Sports adapters; native SkillCorner + Metrica bronze->frame builders (ADR-034); kloppy gateway. The linkage primitive (`link_actions_to_frames` + `slice_around_event`) joins SPADL actions to frames -- pointer-DataFrame return plus a `LinkReport` audit (ADR-004). Tracking-aware features compose into VAEP via `compute_features(*, frames=None)` and the `_frame_aware` xfn marker (ADR-005); every family has an `atomic.tracking.features` mirror.
  - **`time_seconds` is period-relative** (it resets each period -- NOT an absolute match clock). ADR-017 enforces it with a per-period `min_link_rate`/`on_low_coverage` guard plus `LinkReport.per_period_link_rate`, and public `validate_time_base`/`TimeBaseDiagnosis` as the pre-link guard for time-filtering consumers (the linker cannot see pre-filtered-out actions).
  - **GK identity:** `is_goalkeeper_source` is `native` or `derived`; `_gk_identification.derive_goalkeepers` is the B+ filtered algorithm with agreement-based source resolution. SkillCorner **trusts its authoritative roster** (`native`, derivation skipped; derive only for a `(game, team)` with no native GK) -- positional derivation is unstable over the short windows a batched builder sees. Metrica has no roster and stays Tier-2 (ADR-007); it seeds NONE, because `gk_jersey_numbers` is a flat team-agnostic list and `derive_goalkeepers` ORs-never-clears.
  - **Metrica input CONTRACT:** bronze `y` must be SPADL bottom-to-top. Kloppy's metrica NATIVE coordinate system is top-to-bottom, so a `TrackingDataset` consumer MUST flip `1-y`.
  - `tracking.preprocess` ships Savitzky-Golay / EMA `smooth_frames`, linear `interpolate_frames`, SG-derivative `derive_velocities`, and the shared `PreprocessConfig` (flag-based `is_default()`, per-provider defaults auto-promoted via `for_provider`).
  - Feature families -- purity, liveness, NaN-safety, dtype and orientation rules for every `add_*` are in **Key conventions**:

| TF | Module | Public surface |
|---|---|---|
| -- | `features.py` | `add_action_context` / `tracking_default_xfns` |
| -- | `features.py` | `add_pre_shot_gk_position`, `add_pre_shot_gk_angle` / `pre_shot_gk_default_xfns`, `pre_shot_gk_angle_default_xfns`, `pre_shot_gk_full_default_xfns` |
| TF-2 | `_pressure.py` | `add_pressure_on_actor` (`andrienko_oval` default, `link_zones`, `bekkers_pi`) |
| TF-3 | `_kernels.py` | `add_actor_pre_window` |
| TF-4 | `_off_ball_runs.py` | `add_off_ball_runs`, `add_line_break`, `add_off_ball_context` / `off_ball_context_xfns` |
| TF-5 | `_ball_carrier.py` | `infer_ball_carrier`, `ball_carrier_at_action`, `derive_team_in_possession` |
| TF-6 | `utils.py` | `sync_score`, `add_sync_score`, `LinkReport.sync_scores()` |
| TF-7 | `pitch_control/` | `add_pitch_control`, `pitch_control_at_target` / `pitch_control_xfns`, `PitchControlCache` |
| TF-13 | `_gk_resolve.py` | `defending_gk_from_frames`, `acting_gk_from_frames`, `gk_distribution_mask`, `resolve_defended_goals` / `GoalMap` / `GoalEndUnresolvedError` |
| TF-14 | `_defensive_line.py` | `compute_defensive_line`, `add_defensive_line` / `defensive_line_xfns` |
| TF-15 | `_gk_influence.py` | `compute_gk_influence`, `add_gk_influence` / `gk_influence_xfns` |
| TF-16 | `_xshot_occurrence.py` | `XShotOccurrenceModel`, `add_xshot_occurrence` / `xshot_occurrence_xfns` |
| TF-17 | `_xcross_attempt.py` | `XCrossAttemptModel`, `add_xcross_attempt` / `xcross_attempt_xfns` |
| TF-18 | `_ghost_gk.py` | `GhostGkModel`, `compute_ghost_gk`, `add_ghost_gk` / `ghost_gk_xfns` |
| TF-28 | `_das.py` | `get_das`, `get_individual_das`, `get_xc`, `add_das` / `das_xfns` |
| TF-30 | `_cover_shadows.py` | `add_cover_shadows` / `cover_shadow_xfns` |
| TF-31, TF-44 | `_team_shape.py` | `compute_team_shape`, `add_team_shape` / `team_shape_xfns` |
| TF-32 | `_line_breaking.py` | `detect_line_breaking`, `add_line_break(method="ward")` / `line_breaking_ward_xfns` |
| TF-33, TF-36 | `_player_influence.py` | `compute_player_influence`, `add_player_influence` / `player_influence_xfns` |
| TF-35 | `_run_values.py` | `detect_off_ball_runs`, `value_off_ball_runs`, `add_off_ball_run_values` / `off_ball_run_value_xfns` |
| TF-39 | `_shape_graph.py` | `compute_shape_graph`, `infer_positions`, `add_shape_graph` / `shape_graph_xfns` |
| TF-40 | `_obso.py` | `compute_obso_surface`, `compute_pass_obso`, `add_obso` / `obso_xfns` |
| TF-41 | `_space_creation.py` | `compute_space_created`, `add_space_creation` / `space_creation_xfns` |
| TF-42 | `_pausa.py` | `compute_pausa`, `add_pausa` / `pausa_xfns` |
| TF-43 | `_elastic_sync.py` | `align_events_to_frames`, `add_elastic_sync` / `elastic_sync_xfns` |
| TF-45 | `_structural_pass.py` | `compute_structural_pass_metrics`, `add_structural_pass` / `structural_pass_xfns` |
| TF-48 | `_shot_goalmouth.py` | `compute_shot_goalmouth`, `add_shot_goalmouth` -- **no xfns** (post-contact leakage) |
| TF-49 | `_packing.py` | `compute_packing_metrics`, `add_packing` / `packing_xfns` (in no default list) |
| TF-51 | `defensive_credit/`, `_press_commitment.py` | `compute_defensive_credits`, `add_defensive_credit`, `compute_bravery`, `add_press_commitment` -- **no xfns** |
| -- | `_visibility.py` | `point_observed`, `region_observed_fraction`, `add_visible_area_coverage` -- **no xfns** (a coverage denominator is not a VAEP feature; ADR-055) |
- **VAEP** (`vaep/`): Feature engineering + gradient boosting binary classifiers. `HybridVAEP` removes result leakage from a0 features. xG-targeted labels available via `xg_column` parameter. PR-S32 ships TF-29 design-space variants: `window` parameter on `scores()`/`concedes()` (`"action"` / `"possession"` / `"time"`); `xfns_default_no_goalscore` / `hybrid_xfns_default_no_goalscore` goalscore-bias-control lists. **4.13.0 (ADR-018): own goals are counted by RESULT (`result_id==owngoal`), not via a `type_name` shot-gate** — single-source `_is_goal`/`_is_owngoal` helpers in `vaep/labels.py` (all sites route through them; a guard test forbids the old shot-gated owngoal predicate). Fixes a codebase-wide undercount (every converter emits own goals as `bad_touch`, which the old `str.contains("shot")` gate zeroed out); the atomic label path already counted them. Hyrum: shifts scores/concedes/xG label distributions for all providers (VAEP-model retrain trigger).
- **xT** (`xthreat/`): Expected Threat. SK-xT-1 (4.17.0, ADR-021) refactored the `xthreat.py` module into a package with a house-style **pluggable transition family** (string-dispatch + frozen-dataclass params, NOT ABCs): `ExpectedThreat(method="singh_counts"|"kde_smoothed", params=, l=, w=)`. The `singh_counts` default is **byte-identical** to the classic socceraction grid (frozen-oracle parity gate at `tests/xthreat_legacy_reference.py`); `singh_transition_matrix` is vectorized (`np.add.at`, byte-identical). KDE-smoothed transition (`kde_smoothed_transition_matrix`, Silverman-1986 bandwidth, adaptive per-source-zone); `KDEParams.bandwidth=1.0` (pure Silverman) is a conservative default — the held-out-NLL optimum is **corpus-size-dependent** (~1 on a 64-match sample, ≥4 on an 8.9M-action mart), tune via the evaluator. `GridSpec` first-class variable resolution (pitch dims stay in `spadlconfig`). Standalone `value_iteration` (raw-diff convergence — monotone-from-below, do NOT "fix" to abs; optional `max_iter`). Held-out **transition-model** NLL evaluator (`holdout_split` `game_id`-keyed, `compute_holdout_nll` pure, `compute_holdout_nll_per_group`) — NOT an xT-quality metric. KNN/conditional deferred (pre-publication); lakehouse `XTGrid` declined (keep raw `.xT`). Additive — no Singh-path change, no retrain trigger (TF-24 calibration `FrozenXt` untouched). Lakehouse-proposed; attribution Singh 2018 / Silverman 1986 / Salimi 2026 (pre-pub). Decision: ADR-021. **SK-xT-2 (4.19.0, ADR-022)** wires xT into VAEP as an opt-in, frame-free feature: `vaep.features.xt_xfns(*, model)` + atomic mirror `atomic.vaep.features.xt_xfns` emit `xt__<model.method>` per gamestate slot (ADR-005 §8 naming), caller-supplies-the-fitted-model (fail-closed; `str`/`None`/unfitted raise; `ExpectedThreat` imported `TYPE_CHECKING`-only — duck-typed at runtime, **no new runtime edge**). In **no** default xfn list (additive — opting it in is a self-triggered VAEP retrain; guard test enforces absence). Atomic reuses `model.rate()` **unchanged** (no `_rate_cells` extraction — SK-xT-1 parity gate untouched) via a synthesized standard-SPADL frame + **type-aware** `result_id` (dribble intrinsic since dribbles are never followed by `receival`; pass/cross success iff next atom is `receival`); maps slots by composite `(game_id, period_id, action_id)`; period-last pass/cross → NaN (inherent atomic edge). Cross-representation symmetry verified by a geometry-keyed oracle (NOT `action_id` — `convert_to_atomic` renumbers it) + a dribble keystone gate on the committed WC2018 fixture. Bundled-grid variant reserved via a typed `str` door (ADR-011). C4-free.

- **xT-GK v1** (`tracking/_xt_gk.py`, `_gk_geometry.py`, `_gk_completion.py`): FROZEN parametric GK-distribution value (ADR-024) -- `compute_xt_gk` / `add_xt_gk` / `xt_gk_xfns` plus `XtGkParams.for_philosophy`. Tracking-required; in no default xfn list. Live contracts:
  - Normative params (gamma/delta/phi/eta, `defensive_third_boundary`, `pressure_scale`, `convolution_sigma`) are **intent-set, NEVER calibrated**.
  - **Invariant: the phi surface enters value via PEV and DZV ONLY** -- `base = -xT*(z)` and RAV stay on raw `xT*`, so the destination is never revalued twice. Behaviourally guarded.
  - `resolve_gk_geometry` derives a missing goal-kick origin/destination WITHOUT mutating shared `actions`, emitting per-row `xt_gk_origin_source`/`xt_gk_dest_source`/`xt_gk_origin_confidence` plus an aggregate `XtGkReport`. The `x <= 16.5` goal-area clamp is an **action-LTR own-half predicate, so it must run AFTER the ADR-028 reflection**, never before -- applied to a raw away-team frame x it rejects a correctly-placed keeper and silently loses the tracking tier.
  - **Native-origin trust is a FAIL-SAFE provider allowlist** (`native_origin_is_trusted`; unknown/None/future -> distrust). Distrust scope is **goal-kicks ONLY**: SkillCorner records a goal-kick origin as the broadcast ball detection ~14-20 m downfield, but its open-play GK-pass origin IS the keeper (measured 0.4 m).
  - `GkCompletionModel` (logistic, pure-numpy `sigmoid(Xb)` serve, pickle-free JSON + SHA256) backs RAV and the public `compute_gk_completion` / `add_gk_completion`. **Provider-keyed variants** via `variant_key_for_provider` (`skillcorner` -> its own weights, else `gs`); one call = one match (`_resolve_single_provider`). **Training is `result_source=="native"`-ONLY** -- `inferred` is positive-only and would bias a multiplicatively-consumed calibration -- and defaults to FULL-match frames, because a small `--tracking-limit` starves derived-GK detection and inflates the domain. A capped run is dev-smoke only, never used to bundle.
  - **Per-type serve gate:** any completion sub-domain whose held-out AUC lower confidence bound is <= 0.5 (or degenerate, or `n < 50`) serves the calibrated per-type base rate, tagged `xt_gk_completion_source="base_rate"` (`serve_mode_from_lcb`). Data-driven per variant, not a blanket goal-kick rule.
  - **Do not pool `xt_gk` across completion variants without running the comparability gate** (`scripts/_xtgk_comparability.py`).
  - The resolved-coordinate audit columns (`xt_gk_origin_x/_y`, `xt_gk_dest_x/_y`) are provenance and deliberately NOT in `_OUTPUT_COLS`, so `xt_gk_xfns` never surfaces them as VAEP features.

- **xT-GK v2 possession value** (`xtgk/`): the honest value surface `V(z,p)` behind the v2 metric `rho*[V(s') - V(s)] - (1-rho)*[V(s) + kappa*V_opp]` (ADR-036). Hexagonal -- a `PossessionValue` Protocol plus adapters; `compute_xt_gk_v2` depends only on its three ports and emits the coherent additive decomposition `xt_gk_v2_position` / `_pev` / `_retention_loss` / `_dzv` / `xt_gk_v2`. In no default xfn list.
  - `MarkovPossessionValue` (production) = pressure-stratified value iteration reusing `xthreat.value_iteration` verbatim, with an xG-calibrated first-shot reward, a goal-kick-inclusive move-set (`_moves.MOVE_TYPE_IDS` = pass, dribble, cross, goalkick, throw_in) and pressure terciles. `V(z,p) = E[xG of the possession's FIRST shot | ball at z under pressure p]`. The xtgk-local builders reuse `xthreat`'s low-level seams and modify NO `xthreat` source -- classic xT stays byte-identical, parity-gated.
  - **silly-kicks ships NO xG model.** `V` is fittable only where a calibrated per-shot xG exists; the column is injected (`xg_column`).
  - **rho** = `GkRetentionModel` (logistic, pure-numpy serve, JSON + SHA256, provider variants); labels come from `retains(actions, *, window_seconds=10.0)`, where a truncated window is NaN -- excluded, never falsely-retained. Features are marts-native (gold `fct_action_values` + `fct_action_context`), NOT tracking frames, and pressure is pinned to `pressure_on_actor__bekkers_pi`. Every bundled variant must clear `ece <= 0.10 AND |reliability_slope - 1| <= 0.25`, CI-certified against its recorded `metrics.json`.
  - **`V_opp`** = `EmpiricalTurnoverValue`: possession-bound by default, with support-gated hierarchical bin-widening (native cell -> coarse block -> global-per-pressure, `min_support=30`) so a one-turnover deep cell is not a noise estimate.
  - **Consumer contract: call `apply_resolved_gk_geometry` BEFORE `compute_xt_gk_v2`, and keep `is_gk_distribution` (or the `gk_geometry_source` stamp) on the frame.** Resolved keeper origins live in `fct_action_context.xt_gk_origin_x/_y`, not raw `bronze.spadl_actions`, and the rule is **OVERRIDE, not coalesce** -- SkillCorner's native goal-kick origin is present-and-wrong (the broadcast ball), so a `fillna` would fix GS and silently miss SkillCorner. The warn-once attestation needs a domain column, so a pre-filtered slice with the flag dropped scores raw origins silently.
  - **`flat_zones` maps a NaN coord to zone `(0,0)` -- a FIT-PATH-only contract.** Every fit seam `dropna`s first; use `finite_coord_mask` at any scoring seam. `compute_xt_gk_v2` NaN-guards non-finite rows across all five outputs (never a fabricated zone) and raises on coordinate incoherence between `actions` and the rho features.
  - **Status: NOT construct-validated.** Out-of-sample on real gold, v2 loses to `raw_completion` on outcome-AUC (GS -0.147, SC -0.027); the deep-zone pressure gate is GO-leaning (a real, decreasing, monotone gradient on both cohorts) but WC2022's absolute effect sits under the pre-registered floor. Reports in `docs/research/xtgk_v2_construct_validity/`.
  - v1 is **FROZEN** and coexists until the lakehouse migrates its `xt_gk*` columns; there are no v2<->v1 imports.
- **GKDV — GK Deterrent Value** (`gkdv/`): PR-3 of the TF-19 cycle (ADR-043; spec `docs/superpowers/specs/2026-07-12-tf19-gkdv-regate-and-v1-design.md`). Counterfactual valuation of keeper POSITIONING: how much does the actual keeper's position change the attacking team's space and threat versus a league-average "ghost" keeper in the same frame state? **Depends on `silly_kicks.tracking` PUBLIC seams ONLY, never the reverse** — `tracking/` must never import `gkdv/` (pinned by `tests/gkdv/test_import_allowlist.py`; the same rule ADR-037 set for `tracking/_model_eval.py`). **Engine** (`_engine.py`): `build_ghost_frames(frames, *, home_team_id, model=, carrier=, params=)` → `(counterfactual_frames, provenance, report)`, PURE (never mutates `frames`); domain = alive ball ∧ in-possession team attacking ∧ ball within `domain_ball_to_goal_m` of the attacked goal ∧ defending-GK row present; the ball carrier is pinned ONCE and shared by the domain filter and the serving seam. **A NaN/missing ghost is dropped-and-COUNTED, never scored as Δ=0** — a zero delta from a missing keeper reads as "no deterrence" and biases keeper aggregates toward the null (`GkdvReport` conserves exactly: `n_frames_scored + Σ drop_reasons == n_frames_in`); a non-finite served ghost RAISES rather than making the keeper vanish. `counterfactual_frames` is the FULL input with only the defending keeper substituted, so consumers MUST restrict to the scored set via `provenance_to_targets` / `provenance["drop_reason"].isna()` before differencing. **Arms** (`_arms.py`), both in ATTACKER-VALUE units as `actual − ghost` so **negative = deterrent** uniformly: `delta_das` (accessible space; direction is pinned ONCE on the FACTUAL frames and the SAME pinned column feeds BOTH legs — accessible-space otherwise infers direction per period from a team-x mean the ghost displacement perturbs, so the two legs could infer OPPOSITE directions and the difference would not be a counterfactual at all; routed through `get_individual_das` summed per team via the narrow `_das_port.py`, since `get_das` hardcodes `infer_attacking_direction=True`) and `delta_threat_suppression` (`compute_threat_pc(actual) − compute_threat_pc(ghost)`). **Asymmetric keeper modelling, by construction:** the threat arm carries `lambda_gk` (a keeper GAIN applied after the influence field, so keeper position enters via TTI) while ΔDAS is **keeper-BLIND** (accessible-space's column map has no `is_goalkeeper`), i.e. ΔDAS measures relocating a GENERIC player. **`method="spearman"` is a HARD API constraint** enforced in `GkdvParams.__post_init__` — a GK-blind pitch-control configuration is *unrepresentable*, not merely discouraged, and the check lives in ONE place (the arms deliberately do not re-check). **Neither arm accepts a `pitch_control_cache`, and that is CORRECTNESS not performance:** `PitchControlCache` keys on frame IDENTITY (`game_id, period_id, frame_id, team, method, params, ball_position, decompose`) and excludes player positions, so a ghost frame — which carries its twin's identity — would be served the factual leg's surface and every delta would collapse to exactly zero with no warning. Ships **no `add_*` aggregator** (C4 action-coupled count unchanged) and is in **no** default xfn list. §6.4 Layers 0–3 and the xS-arm probe (PR-3b) are NOT in this PR. Attribution: Le et al. 2017 (ghosting); DEFCON-GNN arXiv:2512.10355 (comparator only, not implemented).
- **Causal** (`causal/`): Public since 4.47.0, promoted out of `_causal/` (ADR-015) -- pure numpy/sklearn, no R, no new runtime dependency. `matching.py` (propensity-score matching; ATT/ATNT; 1:1 nearest-neighbour **with replacement**, no caliper; logistic PS on standardized covariates; Abadie-Imbens 2006 matching SEs; cluster-aware `placebo_shift` with whole-cluster reassignment), `opportunities.py` (spell state-machine opportunity rows behind an `OpportunityConfig` builder surface), `power.py` (`att_power_curve`, `icc_power_curve`) and `_confounders.py`. The known-truth method tests in `tests/causal/` gate CI; applied results are REPORTED, never gated.
- **Atomic-SPADL** (`atomic/`): Variant where actions are decomposed into atomic sub-actions (receival, interception, out, etc.).
- **Providers** (`providers/`): Per-provider raw-data **parse ports** (bytes → provider-canonical bronze → converter input). **SK-S95 (4.30.0, ADR-031 T3):** `providers/sportec/parse.py` single-sources the IDSSE/Sportec **DFL parser** as a **verbatim lift** of the luxury-lakehouse parser (`ingestion/idsse.py` + `finalize_bronze_df` + `idsse_native_match_id` + the `convert.py`/`spadl_adapter.py` shapers, pinned @ `0efac60`): `parse_dfl_match_info`/`parse_dfl_tracking`/`parse_dfl_events` (DFL XML → RAW bronze) + `shape_tracking_to_native`/`shape_events_to_native` (bronze → `tracking.sportec`/`spadl.sportec` `EXPECTED_INPUT_COLUMNS`) + `derive_idsse_home_team_start_left{,_extratime}`; typed returns `MatchInfo`/`SportecTrackingBronze`/`SportecEventBronze` (silly-kicks' own domain names — a versioned cross-repo bronze contract, ADR-031 N1). Behind the `[parse-dfl]` extra (pure stdlib `xml.etree` + pandas/numpy — no new runtime dep). **Emits RAW bronze — data-quality (Savitzky-Golay smoothing, velocities) stays consumer-side** (the harness applies `_preprocess` after shaping; a delete-and-depend lakehouse keeps its own `_smooth_tracking` after the parse). The dev harness `scripts/_loader_pining.py::_build_idsse` is re-routed onto the port → native converters, retiring the y-inverting loader-local kloppy `_kloppy_tracking_to_frames`; the action `team_id` is remapped `"home"/"away"`→DFL-CLU so the ADR-028 action↔frame join aligns with the CLU-keyed frames. Golden parity (`tests/providers/sportec/test_parse_port_parity.py`) pins the lift to real lakehouse output on a reduced WC2022 IDSSE slice (`tests/datasets/sportec/idsse_slice/`, `SOURCE_SHA`). **VAEP/tracking-retrain trigger** for IDSSE calibration/pining consumers (N6 — action-anchored feature values change; the new path aligns action↔frame y to ~0.2 m vs ~11.8 m on the old kloppy path). C4-free (no new action-coupled aggregator; count stays 28). **`providers/statsbomb` (4.76.0, ADR-054)** is the second port: SB360 freeze-frames -> the `snapshot_to_tracking_frames` contract plus `visible_area` as raw per-action polygons. **Shape, never fetch** -- no extra and NO new runtime dependency (`statsbombpy` is a `scripts/` dep only), so the `[parse-dfl]` extra is per-PORT, not package-wide. EXTRACTED from `scripts/build_sb360_coverage.py` and the script re-pointed at it (identity-verified), so the published `coverage.md` numbers cannot drift from the library. The scalar affine lives in `spadl/_sb_coordinates.py` while the CLIP and the 3-element shot `y_offset` stay in `_convert_locations` as EVENT semantics (ADR-038's split) -- which is what lets a `visible_area` polygon extend past the touchline instead of being silently shrunk. **`player_id` does NOT recur across freeze-frames** (SB360 carries no identity; `snapshot_to_tracking_frames` numbers rows), so per-player aggregation is foreclosed -- in the port's contract, not a downstream surprise.
- **Calibration** (`calibration/`): TF-24 Optuna calibration harness (optional `[calibration]` extra; lazy-import-guarded, NOT imported by `silly_kicks/__init__`). Pure provider-agnostic objectives/CV/gates: `CarrierAccuracyObjective` (Stage 1 carrier accuracy, maximize) + `AugmentedVaepBrierObjective` (Stage 2 augmented-VAEP held-out Brier, minimize) — a `ruthless` `CachedObjective` with invariant-prepare + per-trial-patch and a deterministic-XGBoost cache-equivalence guarantee (`assert_cache_equivalence`, 1e-9); match-stratified CV (`GroupKFold(5)` for >7 matches / leave-one-match-out ≤7); `FrozenXt` frozen **exogenous** xT artifact (fit once on a corpus disjoint from the calibration matches, sha256 + fail-closed exclusion, train–serve-consistent); H1 stateless default-Brier-anchored penalty + per-provider signal-sanity + DAS-degradation surfacing + TF-25 provider-specific-defaults gate. I/O lives in `scripts/calibrate_tracking_defaults.py` (CLI) + `scripts/_loader_pining.py` (SkillCorner/IDSSE public, Gradient Sports owner-tier) + `scripts/_loader_databricks.py` (bronze). The harness **recommends** values + an auditable manifest; it does NOT change the library default constants (separate "apply" PR). Decision: ADR-009. **SK-xT-3 (4.20.0)** adds `XtBandwidthObjective` + `xt_bandwidth_config` + `scripts/calibrate_xt_bandwidth.py` — a per-corpus `bandwidth × GridSpec-resolution × adaptive` sweep minimizing K-fold held-out xT **transition-NLL** (Singh baseline reported alongside; recommendation scoped to destination-likelihood with a reported Spearman-vs-goals cross-check, ADR-009 amendment + ADR-021). Plain duck-typed objective (resolution-keyed lazy cache, NOT a `CachedObjective`); caches the small per-`(grid,fold)` grouped destinations and shares the vectorized gaussian `_gaussian_transition_from_grouped` seam with `xthreat` (gaussian KDE re-pinned to the vectorized core — Chesterton-verified single caller; sklearn retained for non-gaussian kernels). Does not change library defaults.

## Where the history lives

CLAUDE.md holds **durable contracts only** -- the rules a change must not break. Per-release
narrative deliberately lives elsewhere; grep these rather than re-adding paragraphs here.

| Question | Source |
|---|---|
| What shipped in a release, and what it broke | `CHANGELOG.md` (every version, keyed by `PR-Snnn`) |
| Why a design is the way it is | `docs/superpowers/adrs/` (ADR-001..ADR-059 plus `ADR-TEMPLATE.md`, referenced inline throughout) |
| What is planned or blocked | `TODO.md` |
| The measurement behind a claim | `docs/research/<topic>/` |
| Specs and plans | `docs/superpowers/specs/`, `docs/superpowers/plans/` |
| Downstream pins on private modules | `docs/PRIVATE_CONSUMERS.md` |

## Key conventions

- No pandera — schemas are plain Python dicts (`SPADL_COLUMNS`, `ATOMIC_SPADL_COLUMNS`).
- Config DataFrames (`actiontypes_df()`, etc.) are cached with `@functools.cache`.
- Vectorized dispatch: converters use `np.select` over pre-flattened columns, not `apply(axis=1)`.
- All `warnings.warn()` calls include `stacklevel=2`.
- ML naming conventions (uppercase `X`, `Y`, `Pscores`) are allowed in `vaep/` and `xthreat/*.py` per ruff per-file-ignores.
- **`detect_input_convention` must require DISCRIMINATING evidence, and DEFER when it has none (ADR-059).** Rule 1 infers `POSSESSION_PERSPECTIVE` from *"every reliable (match, team, period) group attacks high-x"* — but `reliable` is filtered to `n >= min_shots_per_group_medium` (5), so when that filter removes the low-side groups an all-high survivor set is an **artifact of the filter, not a measurement**. Measured wrong on **2 of 36** real Gradient Sports matches, at `confidence="medium"`, against a converter that correctly declares `PER_PERIOD_ABSOLUTE`. Rule 1 now fires only on a configuration an absolute convention could not produce — **two distinct teams reliable in the SAME period**, or **one team reliable across TWO periods** — and otherwise returns `convention=None` with a diagnostic naming why. **Deferral is the safe direction and that asymmetry is the whole argument:** `validate_input_convention` reads `None` as "keep the caller's declared convention", so a false ambiguous leaves output correct and loses only a cross-check, while a false positive contradicts a correct declaration and **raises** under `on_mismatch="raise"`. The second clause **IS** the guard TF-22 added inline to the ABSOLUTE branch in 3.0.1 — both branches now call `_a_team_spans_periods`, ONE spelling; the first clause is deliberately NOT given to that branch, because separating `ABSOLUTE` from `PER_PERIOD` requires observing a team ACROSS periods and a single shared "is this discriminating?" helper would silently loosen TF-22. Rule 1 is what validates statsbomb + skillcorner, so the coverage risk is a SILENT downgrade to ambiguous: `test_statsbomb_raw_detected_as_possession_perspective` guards it on 3 real matches; SkillCorner is guarded by `test_skillcorner_raw_detected_as_possession_perspective` on real public match `1886347` (`PUBLIC_CORPUS` registers TEN redistributable SkillCorner matches — **visibility is keyed PER MATCH, never on the provider name**, which is the inference `scripts/_corpus.py` exists to prevent and which was made anyway while reading that very file). Its fixture deliberately retains a below-threshold group, so it exercises the sparse-drop shape that caused the misfire and still classifies. **The durable rule: "no counter-evidence" is not evidence — when a FILTER precedes a universal claim, ask what the filter removed.**
- **Converter identifier conventions are sacred.** SPADL DataFrame converters never override the caller's `team_id` / `player_id` columns from provider-specific qualifiers. Qualifier-derived facts surface as dedicated output columns (see `tackle_winner_*` / `tackle_loser_*` on sportec). Decision: ADR-001.
- **Block-detection columns are cross-provider + nullable (ADR-046, 4.56.0).** Every converter emits `shot_blocked` + `cross_blocked` (in `SPADL_COLUMNS`, pandas `"boolean"`) via the shared `spadl/utils._blocked_flag` helper with 3-valued semantics: `True`/`False` on shot/cross rows the provider encodes a block, `pd.NA` on non-shot/non-cross rows AND on providers that cannot encode it (Opta unverified; SkillCorner absent — real-data verified both tiers; StatsBomb `cross_blocked` deferred at n=1; Sportec/Metrica/kloppy `cross_blocked` infeasible). `cross_blocked` = open-play `cross` only (set-piece crosses `pd.NA` in v1). Additive → **no retrain**; declared `"invariant"` in the ADR-045 reflection registry; atomic-SPADL unaffected (projects to `ATOMIC_SPADL_COLUMNS`). TF-51 prerequisite. GS `crossOutcomeType=="B"` pining-probed on real WC2022.
- **Public enrichment helpers (post-conversion `add_*` family) tolerate NaN in caller-supplied identifier columns.** NaN identifiers route to the documented per-row default (typically NaN-output / False / 0); helpers never crash on NaN input. Decoration with `@nan_safe_enrichment` from `silly_kicks._nan_safety` is the formal opt-in; the CI gate at `tests/test_enrichment_nan_safety.py` auto-discovers decorated helpers. Decision: ADR-003.
- **Public `add_*` enrichers are PURE — input-mutation is CI-gated (ADR-033, 4.32.0).** Every registered `add_*` (the full public surface across `spadl`/`atomic.spadl`/`tracking`/`atomic.tracking` — `__all__` UNION `.features.__all__`, since the 15 `atomic.tracking.features` mirrors export via the submodule, not the package) must NOT mutate any caller-supplied DataFrame/Series/ndarray and MUST return a NEW object. Gate: `tests/test_add_star_purity.py` — one canonical `PURITY_ENTRIES` registry (key `"<package>:<add_name>"`), build-fresh-owned-inputs-once + snapshot-every-array-arg + value-equality + `out is not input`; two meta-assertions pin the surface to the public export (a new `add_*` must register or CI fails). A best-effort AST heuristic NUDGES toward per-branch coverage. **Contributor contract:** any `add_*` that *conditionally* adds columns (a present/absent branch) MUST register ≥2 purity variants (both branches) — the heuristic is a nudge for the one known shape, NOT a guarantee, so the contract is the real backstop. Born from the `add_gk_distribution_metrics` gk_role-present in-place-mutation defect (the gk_role-absent path always copied, so the old column-list guard never caught it). Same PR renamed `pitch_control_at_action`→`pitch_control_at_target` (function aligns with its emitted `pitch_control_at_target__<method>` column base, unchanged since 4.31.0) — note the lakehouse keeps its own DEFCON `pitch_control_at_action` mart column (different semantics; not silly-kicks').
- **GK-domain keeper identity: use the RESOLVED `player_key`, never raw `player_id` (4.45.0/PR-S112).** SPADL `player_id` is **null for goal-kicks by design** (the taker isn't in the raw event — the reason `acting_gk_from_frames`/the lakehouse goal-kick-taker override exist), and it was 100% null across the whole GK-distribution domain in the gold marts. The resolved Kimball surrogate `fct_action_context.player_key` (99.9% populated on `is_gk_distribution` rows) is the keeper identity. Analysis loaders source resolved fields from GOLD marts, NOT raw `bronze.spadl_actions`. CI guards: `tests/xtgk/test_player_key_contract.py` (pure SQL-source guard + an `@e2e` `player_key ≥99%` data-contract that catches both a wrong-column read and a resolver regression).
- **Every band needs a test from BOTH sides, and every counterfactual needs a non-vacuity assertion that it actually moved something.** A gate that only asserts "value is inside the band" passes identically when the value is inside the band *and* when the computation silently produced nothing; a counterfactual test that only asserts `delta == expected` passes when both legs are secretly the same object. Assert the failing side too (a mutation that SHOULD move the number out of the band), and assert the counterfactual measurably differs from its factual twin. **Four silent-null defects in this codebase share exactly one shape** — the kloppy tracking y-inversion (ADR-031), the fabricated `flat_zones` grid origin (ADR-036/PR-S113), the identity-keyed `PitchControlCache` served to a moved-GK counterfactual (ADR-043), and a mirrored external-provider event frame — each produced a plausible number from a computation that had not happened. None was caught by a one-sided assertion; each was caught by a test that checked the other side. Precedents: `test_deep_zone_gate_nan_invariance.py`'s non-vacuity meta-assertion (its first draft compared an all-zero surface to an all-zero surface), the `test_ghost_gk_mirror_invariant` off-centre probe (ADR-040 strengthened it because the central probe moved ~0.1 m and was vacuous), and `tests/gkdv/test_arms.py::test_unpinned_implementation_would_measurably_differ`.
- **Private modules can have downstream consumers.** Before renaming, splitting or relocating any `silly_kicks/**/_*.py`, check `docs/PRIVATE_CONSUMERS.md`. Path pins fail **silently** — a renamed module degrades a consumer's guard with no `ImportError`.
- **Artifact-writing drivers REFUSE a dirty tree (4.63.0, `scripts/_provenance.py`).** Any `scripts/` driver that writes a registered artifact calls `require_clean_tree(git_provenance(), allow_dirty=...)` FIRST — before paying for any corpus work — and stamps `run_commit` + `run_tree_dirty` into its output. The reason is measured, not hypothetical: `git rev-parse HEAD` returns the same SHA whether or not the tree is modified, so a driver stamping the bare SHA records a commit that does **not** describe the code that ran. A corpus pass was launched exactly that way (HEAD clean, three drivers modified); the resulting numbers would have carried a verifiable-looking false provenance, which is strictly worse than none. Absent git counts as dirty, never clean. `--allow-dirty` permits a dev run but the artifact still records `dirty: true` — the escape hatch must never launder the fact. **Enforcement belongs in `main()`, not in the work function** (a `run()` that refuses to execute on a dirty checkout cannot be tested without mocking git; the CLI refuses, `run()` records the truth). **CI-gated as of 4.65.0** (`tests/scripts/test_provenance_wiring.py`): the rule was enforced by memory and memory had already failed twice — `validate_xshot_causal.py` wrote the §3.3 entanglement artifact with no provenance at all, and `validate_xs_probe.py` stamped a bare `git rev-parse HEAD` into the cited 4.60.0 result. The gate requires every artifact driver to import the helper, offer `--allow-dirty`, never shell out to `rev-parse` (AST-matched on CALLS, so prose describing the defect is not mistaken for committing it), and call `require_clean_tree` from `main()`. An artifact whose inputs came from another driver (e.g. the arm-values table feeding the §6.1 ICC number) needs provenance on BOTH, or the clean SHA on the downstream metrics launders the dirty upstream input.
- **Every corpus driver adopts the shared seam `scripts/_driver.py` -- CI-gated, not prose (ADR-052, 4.72.0).** This rule was prose for two releases and prose was not enough: of 21 in-population drivers, **three** survived a crash and **fourteen** held every result in memory and wrote once at the end. `for_each(items, key=, work=, shard_root=, token_inputs=)` owns the loop -- it STREAMS its corpus (never `list()`s it: `load_matches` parses a full tracking frame per match inside the generator), writes one shard per item into a generation DIRECTORY named by a `ruthless.fingerprint` digest of the DECLARED inputs, skips an item whose shard exists, prints a FLUSHED `[i/n]` line, records failures rather than losing the pass, and asserts conservation over its OWN keys. **An empty result still writes a shard** (absent = "not yet run", present-and-empty = "ran, produced nothing"; conflating them recomputes every barren item forever). The primitives are the escape hatch and a driver on that path must call `assert_conservation` **AND** `_require_injective` -- conservation alone is SATISFIABLE BY A LOSSY RUN, because a colliding key makes two items share one shard and `present` counts it once per duplicate key (measured: 2 items -> 1 processed -> conservation returns `(2,2)` and PASSES). **`reconcile` requires a partition surface**; selectors are deliberately outside `token_inputs` so narrowing reuses shards, which makes the generation a SUPERSET of any one run, so every other adopter combines from `res.keys`. Corpus-scoped counters survive resume via a `<key>.counters.json` sidecar (pass-scoped `n_attempted` deliberately does NOT -- a resumed pass must be able to declare it built nothing); use `CorpusPassResult.manifest()`, since a hand-written `manifest_fields(...)` silently took the old `counters_unrecorded=0` default and reported a complete corpus of nothing. **A driver that changes its SHARD SCHEMA must bump its schema token, and the two must be pinned TOGETHER (4.77.1).** This is the "token completeness" limit below, measured: 4.77.0 renamed a `measure_match` column and added another (the ADR-042 denominator) while leaving `token_inputs["schema"]` at `"sb360-coverage-2"`. The fingerprint digests `token_inputs` only, so the un-bumped token resolved to the SAME generation directory, where **22 stale shards** carrying the old column were waiting -- a re-run would have skipped all 22 as already-done, reported a clean CONSERVED pass, and combined the OLD schema, with the fix silently not taking effect. Nothing in the suite could see it: the driver's own assertion on the renamed column lived in an `e2e` test, which CI does not run, so CI was green on a broken driver. The pattern that fixes it is a declared `_EMITTED_SHARD_COLUMNS` + `_SHARD_SCHEMA_VERSION` pair, gated three ways -- the declaration matches the dict the work function ACTUALLY builds (AST, not a copy of it), `token_inputs["schema"]` references the constant rather than a literal, and the pair itself is pinned -- plus a run-time assertion that fails at the FIRST shard. **Never write `pd.DataFrame(rows, columns=DECLARED)` as the check:** it SELECTS to the declaration, so a dropped key vanishes and a missing one arrives as NaN, and the guard certifies both. Compare the keys the rows actually carry. **What it does NOT prove:** token completeness (a wrong declaration digests stably), and a SECOND uncounted loop -- that writes no shards and lists no keys, so nothing sees it; a fan-in check is the recorded follow-up. Two rules the migrations produced: `for_each` resumes WORK, never the PRODUCTION of its items, so invert onto an id list only where `work` is unambiguously trivial next to the load; and the `work -> tidy frame` contract carries no per-item METADATA (use the documented `counters(item, frame)` closure) and no non-tabular side state at all.
- **An expensive corpus pass is its OWN shardable driver, and an inestimable replicate is COUNTED (4.65.0, `scripts/build_layer2_spells.py` + `causal/power.py`).** **MECHANISM SUPERSEDED by ADR-052 (4.72.0) — the POLICY below still holds in full (per-match shards on completion, `--match-ids-json` partitionability, atomic writes, counted degenerate replicates), but shard writing and combination now live behind `scripts/_driver.py`: `build_layer2_spells` no longer calls `write_table_atomically` at all (measured: zero references), it calls `for_each` + `reconcile`, and `_partition.py` is retained for MANIFEST aggregation and partitioning. Read this bullet for the WHY, the seam bullet above for the HOW.** Any `scripts/` pass whose per-match work costs minutes writes **per-match shards on completion** (an existing shard is skipped, so a crash resumes) and accepts `--match-ids-json`/`--list-matches` so N processes share one `--out`. Measured: the power driver built its spells INLINE, spent **8.7h** walking 64 matches, then raised in the cheap analysis step that followed and lost every one of them — while the arm-values pass, which had shards, survived its own 64-match run twice. The consumer takes the persisted table (`--spells`, mirroring `--arm-values`), so a crash downstream costs seconds. Reconciliation lives in the shared `scripts/_partition.py` because it has already been wrong once (N workers writing one manifest let the last writer win, so a 64-match artifact reported one partition's `n_matches: 8`) and must not be fixable in one producer while live in the other; it also checks **commit consistency** across workers, since nothing stops one being launched from a different checkout. **A partition that names NO ids for a provider must DROP it** (`providers_for_slice`): the shared loader reads `(match_ids.get(p) if match_ids else None) or list(manifest_ids)`, so an empty list (falsy) and an absent key (None) BOTH expand to the entire manifest — correct for an unpartitioned caller, exactly inverted for a worker, and measured to make every worker of a multi-provider driver load the unsliced providers in full. Two corollaries of running N writers against one output dir: tables are written via `write_table_atomically` (private temp + `os.replace`), per-match shards included because N workers rebuilding it from the shared shard dir means N concurrent writers on one path, and the CONSUMER (`run_signoff_power.py --spells/--arm-values`) reads each input's sibling manifest and REFUSES a dirty upstream, a **missing** manifest (unprovenanced == dirty), or one whose workers ran at different commits — extending the ADR-037 rule above from "this run's tree" to "every artifact this number derives from". Statistically: `att_power_curve` scores a single-treatment-class resample as a **non-detection** and reports it in `n_degenerate_by_size` — never a crash (it used to raise out of sklearn) and never a silent skip. Counting is load-bearing in the same way conservation is: a size whose degenerate count approaches `n_replicates` reports an **inestimable design at that n**, which is not a weak effect, and excluding those replicates from the DENOMINATOR would condition on estimability and inflate the curve exactly where the design is weakest.
- **A registry gate DERIVES its population and asserts it EXACTLY; a floor cannot detect an omission (ADR-056, 4.78.0).** `assert len(ARTIFACT_DRIVERS) >= 6` passed at 18 entries while `validate_xcross_causal` was absent and its artifact carried no provenance at all -- and the same shape held in three more places (ADR-020's dup-`action_id` gate at `>= 21`, with a comment recording it had already been hand-bumped once; ADR-003's three NaN-safety floors; and three COPIES of one leakage-guard discovery rule behind `>= 10` against a real population of 19). Every such gate derives its population structurally and asserts equality in BOTH directions, with a **three-bucket** exemption shape: the registry itself; a `_NOT_A_DRIVER`-style bucket carrying a stated reason plus self-burning-down `is_file()` checks; and `_UNDERIVABLE`, **asserted EMPTY**. The third closes the blind spot the first two cannot see -- a script neither derivable NOR enrolled is absent from every set and the equality still holds, which is only unreachable while every enrolled entry IS derivable. Landed at 22 drivers, 5 exemptions, `_UNDERIVABLE` empty, `SWEPT` exactly 19. **Reconciling two gates is STRUCTURAL, never a set relation:** "every corpus-walking artifact driver is in ADR-052's population" is TAUTOLOGICAL once both call the same predicate over the same script set, so single-source the UNIVERSE (`tests/scripts/_script_population.py`) and pin that neither consumer re-grows its own `glob`. **Content predicates read CODE, never PROSE** -- `string_literals()` excludes docstrings because three separate source-text scans were fooled in one review (`make_ghost_gk_golden` matched a `_weights` rule through a module docstring; `render_sb360_matrix` through the sentence "No provenance guard, deliberately"; `regenerate_gs_et_native_gk` through a docstring saying it MIRRORS the loader), and none carried the literal in code. The probe test needs its own second assertion that the probe still probes something, or a docstring edit makes the first vacuously true.
- **An artifact declares WHICH SYMBOLS its numbers depend on, never what those symbols currently contain (ADR-056, 4.78.0).** `scripts/_input_contract.py::declare_inputs()` digests covariate tuples, extractor module identity and `GEOMETRY_VERSION`, so a new `SHOT_ARM_CONFOUNDERS` column or a `GEOMETRY_VERSION` bump moves the digest with nobody editing the driver -- the residual failure mode is "forgot to reference a symbol at all" (narrow, visible) rather than "typed a list that later went stale". Same shape as ADR-050's `feature_contract`. **Warn, never raise:** an artifact is not a serving path. The source-side rule ("do not produce artifacts from a dirty tree") and this output-side gate ("artifacts must carry provenance") are DIFFERENT assertions, which is why a stamper is `_NOT_A_DRIVER` while its output is still policed. **KNOWN LIMIT, declared rather than discovered -- it catches code drift, not under-declaration:** the mechanism names MODULES, so a behavioural change inside one moves nothing (the concurrent ADR-054 ghost-refusal change is a live near-miss; it cannot affect GS, so it is evidence of where the escalation trigger will come from, not the trigger). Provenance was found to have **FOUR** conventions, not the two the spec predicted -- top-level `run_commit` (12), nested `_provenance.commit` (2), `training_commit` (bundled weights, outside `docs/research/`) and nothing at all (3), plus one artifact that is not a JSON object -- so the gate picks one canonical shape per surface and records divergences WITH the location of their real provenance instead of back-stamping to a shape invented after the fact.
- **A fixture generator must reproduce its committed fixture BYTE-FOR-BYTE before it is allowed to reshape it (ADR-056, 4.78.0).** `_generate_synthetic_match.py` emitted **51** events against a committed **54**: the three ADR-018 goal-capture events (the RE+G own goal, the CR+G cross-goal, the `nonEvent` disallowed shot) had been appended straight to the JSON and the generator never updated, so **any regeneration silently deleted the two rows a live test asserts** -- invisible because nothing in CI ever runs a generator. Restore VERBATIM first, artifacts included (here: a truncated `ball` dict with no `visibility`/`z`, a raw-clock `startTime`, a `startFormattedGameClock` of `"00:12"` at 1000 s, and a trailing newline `main()` never wrote), and prove byte-identity against the git blob -- **a generator that "improves" the artifact in the same change that restores it cannot show which of the two edits moved the file.** Only then reshape, and prove the reshape purely additive. Two corollaries, both measured here: a fixture whose shape is an ACCIDENT can conceal a bug (reshaping exposed a synthesized row inheriting its pass-class parent's derived end, wrong on every real cross-goal); and a fixture that DEFERS silently leaves the guard it exists to exercise running down neither branch -- the GS convention guard had never fired either way in CI, so the reshape target is the MEASURED distribution (`docs/research/gs_input_convention/`) at the tier the corpus actually has (5 shots/group: 50 of 64 matches reach two such groups, only 6 of 64 reach two at `>= 10`).
- **Academic attribution discipline.** Every new feature implementing a published methodology gets an entry in the `NOTICE` file's "Mathematical / Methodological References" section. Cross-link from per-feature docstrings via `See NOTICE for full bibliographic citations.` Mirrors the lakehouse pattern. Decision: ADR-005.
- **Aggregator column liveness is CI-gated (non-NaN AND non-constant).** Every registered `tracking` `add_*` (the full `__all__` surface, jersey helper included) must produce non-null values for every column it ADDS, and every float METRIC column with ≥2 observed values must carry >1 distinct value, on the multi-domain fixture in `tests/tracking/test_aggregator_column_liveness.py` (5 action windows: pass / shot / GK goalkick / attacking-third / wide-area; actor carries the ball through a final-step kick; per-window velocity/layout/GK/kick variation; event clocks jittered off the frame grid). A meta-assertion pins the gate surface to `tracking.__all__`, so a NEW `add_*` must wire a liveness entry or CI fails. Constants BY DESIGN go in the `STRUCTURAL_CONSTANTS` registry with a justification + dedicated invariant test — never silent exclusions. The registry is currently EMPTY: the space-creation structural zeros were RETIRED from the contract (4.24.0), and the dead `pitch_control_at_ball__spearman` near-ball-PPCF degeneracy was RETIRED + replaced by the LIVE `pitch_control_at_target__spearman` (sampled at the action destination + ADR-028-reprojected; 4.31.0, ADR-032). Born from the TF-41 hard-coded-NaN `_opponent` defect (ADR-026; non-constant added 4.24.0 after `space_destroyed_m2 ≡ 0` showed non-NaN alone is insufficient).
- **Linkage-provenance columns are idempotent.** All tracking `add_*` aggregators that merge provenance columns (`frame_id`, `time_offset_seconds`, `link_quality_score`, `n_candidate_frames`) must skip the merge when those columns already exist on the input DataFrame. This allows callers to chain multiple `add_*` calls without producing `_x`/`_y` suffixed duplicates. Pattern: `if not any(c in out.columns for c in provenance_cols):`. CI gate: `tests/tracking/test_provenance_skip_guard.py`.
- **Pre-linking optimization via `links` kwarg.** All tracking `add_*` aggregators accept an optional `links: pd.DataFrame | None = None` kwarg. When provided, the function skips its internal `link_actions_to_frames` call and uses the caller-supplied pointers. Pipeline callers (e.g. lakehouse) pre-link once and pass `links` to all enrichment steps, reducing N × 2-5s to 1 × 2-5s per match. The `links` DataFrame must be the pointers output of `link_actions_to_frames` (columns: `action_id`, `frame_id`, `time_offset_seconds`, `n_candidate_frames`, `link_quality_score`). Internal helpers (`_resolve_action_frame_context`, kernel functions) also accept `links` for full thread-through. `infer_ball_carrier` additionally accepts `pre` (a precomputed `_pre_index_frames(frames)`) and `ball_carrier_at_action` accepts both `pre` and `links`; the pre-index dominates carrier cost (~99%) yet is param-invariant, so the TF-24 Stage-1 calibration objective caches `pre`+`links` per match (invariant-prepare) and re-runs only the carrier kernel per Optuna trial (~100× faster sweep; bit-identical).
- **Shared pitch-control surface via `pitch_control_cache` kwarg.** All pitch-control-consuming aggregators (`add_obso`, `add_cover_shadows`, `add_gk_influence`, `add_player_influence`, `add_space_creation`, `add_pitch_control`) accept an optional `pitch_control_cache: PitchControlCache | None = None` kwarg (mirrors `links`). `PitchControlCache` (in `tracking/pitch_control/`) memoizes **canonical** per-frame surfaces keyed on `(game_id, period_id, frame_id, team, method, params, ball_position, decompose)`, so families that need pitch control on overlapping frames compute each surface once instead of once per family. Each aggregator uses a fresh local cache by default (within-pass reuse); pipeline callers pre-build one cache and pass it to all steps for cross-family reuse (like `links`). Only canonical-frame surfaces are cached — **counterfactual** (player-removed) surfaces in `cover_shadows`/`space_creation` are never routed through the cache. Zero global state. Decision: ADR-008 (TF-7).
- **Dtype-safe id comparisons are a REPO-WIDE contract (ADR-019, 4.15.0; scope widened 4.53.0/ADR-043).** No consumer anywhere in the package compares ids (action↔frame, action↔action, or against a scalar `home_team_id`/`player_id`) with raw `==`/`!=`, and none merges on id keys without alignment — those silently mis-resolve across dtypes (`Int64(366) == "366"` → False) or raise on a mixed merge key. Originally written as a *tracking-feature-seam* rule; it never was one, and 4.53.0 stopped pretending — the seam is the **public `silly_kicks.id_compat`**, imported by **39 modules across 6 packages** (`spadl/`, `vaep/`, `atomic/`, `causal/`, `gkdv/`, `tracking/`; measured by AST over `silly_kicks/`, excluding the module itself). ADR-043 promoted it out of `tracking/_id_compat.py`: five packages were reaching into a private *tracking* submodule, two via function-local imports in `spadl/utils.py` written to dodge a circular import, and a seam ADR-019 makes MANDATORY is public API by definition. **Breaking import-path change, no shim** — the pin fails loud at import, and a shim would have made the promotion cosmetic. Surface: `ids_equal`/`ids_differ` (column-vs-column, NA-safe both-present), `ids_match` (Series/array vs scalar), `same_id` (scalar↔scalar groupby loops), `align_join_keys` (pre-merge, numeric-vs-object only), `restore_id_dtype` (put a kernel-built id column back on its SOURCE dtype where that dtype can represent the result — numpy ints can't hold NA, so a result with unmatched rows stays float). One `_canonical` truth (integral-float collapse; genuine strings pass through). **The both-object fast path is CONTENT-probed, not free** (`_raw_comparable`): an object column of boxed floats raw-compares False against the same id as a string, so dtype alone cannot license a raw `==`. Probe cost ≈15% of the comparison per side, ≈30% for the guard. Opt-in loud guard: public `validate_id_dtypes` + `IdDtypeDiagnosis` (mirrors ADR-017's `validate_time_base`). CI gates: red-first **asymmetric** dtype-invariance gate over every registered `add_*` (`tests/tracking/test_id_dtype_invariance.py`; numeric actions × string frames + reverse, `home_team_id` independent axis) + meta-assertion (gate surface == registered surface) + the **`PUBLIC_ID_SCALAR_ENTRIES` registry** (`tests/invariants/conftest_id_scalar.py`, exercised by `tests/invariants/test_public_id_scalar_registry.py`) + structural de-dup perf guard. **The AST lint is DELETED as of 4.53.0/ADR-043 — deleted, NOT widened, and do not reinstate it.** Widening was rejected on principle, not cost: it was a NAME heuristic (it missed ADR-027's `t != action_team`, and `_ghost_gk`'s `str(t) == home_team_id_norm` because the scalar was renamed), but the unfixable part is that **a safe same-source compare and an unsafe cross-source one are the IDENTICAL AST** — only the scalar's *provenance* separates them, and no syntactic rule sees provenance, so any widening either flags correct code (breeding the exemptions that grew `ALLOW_MODULES` in the first place) or keeps missing real defects. **Complete by ENUMERATION where the lint was incomplete by HEURISTIC** (same idiom as ADR-003's NaN-safety registry and ADR-033's `PURITY_ENTRIES`): the registry derives its surface from `inspect.signature` over the `__all__` exports of `spadl`/`atomic`/`vaep`/`causal`/`tracking`, keyed by defining qualname — **102 functions: 77 exercised directly, 22 delegated** to the `add_*` gate (machine-checked against that gate's registered surface, not asserted in prose), **3 justified non-invariant** — each invoked on a matched scalar, a mismatched-but-value-equal one (`5` vs `"5"`), and a **float** one (`5.0`; not redundant — a naive `str(x)==str(s)` renders identically for ints, so only the float axis exposes it). Two meta-assertions pin it to the public surface in BOTH directions, so a newly-exported id-scalar function fails CI until registered, delegated or justified — the anti-rot property the lint's glob lacked. It caught two live `str()`-comparison defects on its first run that the lint structurally could not have seen. Fixed three latent bugs (object-`is_ball` `~`-no-op ball-as-opponent; `str(5.0)` iterrows-upcast player-influence/cover-shadow mislabel; object-path opponent join-miss) → **VAEP retrain trigger**. **A FOURTH instance of the `str()`-on-a-float-id trap landed in 4.79.0, and it marks the registry's LIMIT: the enumeration is complete over a SURFACE — id-SCALAR arguments of public functions — and `_elastic_sync.py` keyed an internal DICT on `merged["player_id"].astype(str)` while querying it with `str(action_row["player_id"])`.** Every frame set carries a ball row whose `player_id` is NA, which upcasts an integer id column to float, so the lookup stored `"10.0"` and the query asked `"10"`; **every** lookup missed, `dist` fell to the caller's `inf` default, and `elastic_confidence` collapsed to a constant `accel_weight / (accel_weight + proximity_weight)` = 0.6. A miss is indistinguishable from "infinitely far from the ball", so nothing failed loudly — and the SB360 audit recorded the broken state as `identical` → **`works`**, because BOTH legs degraded identically. **Two rules follow.** Reach for `canonical_id_series`/`canonical_id` on any id used as a DICT KEY or join token, not just in a comparison — `astype(str)` on an id column is the defect, and the NA ball row makes the float upcast unavoidable rather than exotic. And a completeness-by-enumeration gate bounds the surface it enumerates, never the defect class: cite it as covering *its* surface, never as covering the trap. ADR-001 preserved (fix lives at consumer seams, not converters). **4.25.0 (ADR-027), retained as the historical case that killed the lint:** it missed a raw `t != action_team` in `_line_breaking.py` ward (a NaN-team crash + an Int64-vs-string silent opponent miscompute on GS) because the operands are not named `*_id`; the behavioral NaN-safety gate (`tests/test_enrichment_nan_safety.py`, NaN-team fixture) caught it. Fixed via `same_id` + a `pd.isna` NaN-route. **Behavioral gates, never a lint, are the backstop** — ADR-043 acted on exactly this by replacing the lint with the enumeration registry above. One site ships DELIBERATELY ungated (the velocity-state defending split, ADR-043 decision 6): its `gk_team` scalar comes from the same frame it is compared against, so no input can distinguish `ids_match` from `==` and no lint could flag it without flagging safe code — recorded so a future reader meets the reasoning instead of "fixing" an untested line.
- **Tracking-frame identifier dtypes are NULLABLE, and a constant every variant overrides is a DEFAULT masquerading as a CONTRACT (ADR-058).** `TRACKING_FRAMES_COLUMNS` declares `Int64` for `player_id`/`team_id`, not `int64`. Every frame set carries a ball row, which belongs to no team and holds no player, so both are NA on it **by construction**; numpy `int64` cannot represent NA, so the cast raised `IntCastingNaNError` on every producer and the base was satisfied by **nothing** — all five provider variants overrode it, and the Gradient Sports one had already worked out the right answer locally (its docstring says *"allows NaN on ball rows"*). Not `object` for the base: `id_compat`'s both-object path is CONTENT-probed (~15%/side), so defaulting every producer onto it taxes the seam ADR-019 makes mandatory. The four `object` variants are KEPT (kloppy domain types and SkillCorner's stringified ids are genuinely strings); `GRADIENTSPORTS_TRACKING_FRAMES_COLUMNS` becomes an **alias** rather than a deletion — it is in `tracking.__all__`, and aliasing is already the file's idiom (`SPORTEC`/`SKILLCORNER`/`METRICA` alias `KLOPPY`). Gated red-first in `tests/test_tracking_schema.py` by a base pin (which also asserts the cast preserves NA rather than producing an ADR-027 sentinel) plus one **complete by ENUMERATION** over every `*_TRACKING_FRAMES_COLUMNS` in the module, so a new provider with a non-nullable id dtype fails CI. **The durable lesson: when a cast to a schema constant fails, the constant is a suspect** — 4.77.0 read the same `IntCastingNaNError` as its dtype PIN being unimplementable and dropped it, twice bouncing off the wrong constant. When a schema grows a per-provider variant, ask whether it expresses a genuine provider difference or repairs the base; the second kind belongs at the base the day it is written. **`snapshot_to_tracking_frames` CASTS to this schema, per column** (`_cast_to_declared_schema`): identifier columns follow the CALLER's domain — genuinely-string ids keep `object` (the kloppy family), numeric ids take the base — decided per COLUMN, not per frame, because a caller can legitimately pair a numeric `game_id` with string `team_id`s. Before the cast the ball row's NA upcast integer ids to `float64`, i.e. ids were FLOATS (the `"366.0"` vs `"366"` shape), and the result was resolver-dependent: an `Int64` source stayed `Int64` on pandas 2.3.3 and became `Float64` on 3.0.5. **That cast is what exposed `add_elastic_sync` scoring every action against an empty distance lookup** — see ADR-019 and the CHANGELOG.
- **Per-action geometry is emitted in the SPADL action-LTR frame (ADR-028, 4.26.0).** Actions are per-acting-team LTR (acting team attacks x=105); `convert_to_frames` is home-attacks-right (home attacks x=105 every period). They are a 180° point reflection apart for **away-team actions**, and the geometry layer historically sampled frame positions WITHOUT re-projecting → ~50% of tracking action rows wrong: visibly-bimodal absolute positions (`pre_shot_gk_x`, `defensive_line_x`, `team_shape_centroid_*`) AND silently-wrong mixed-frame scalars (`nearest_defender_distance`, `receiver_zone_density`, `defenders_in_triangle_to_goal`, all `pressure_on_actor__*`, `pre_shot_gk_distance_to_shot`/`_angle_*` — action anchor combined with frame positions). One frame serves BOTH teams' actions, so the caller cannot pre-orient; re-projection is per-action + library-owned. Fixed by ONE helper `tracking/_action_orientation.py` (`acting_team_attacks_rtl` keyed on the frame's `team_attacking_direction`, `align_join_keys` for the id join; `reproject_to_action_ltr`) at THREE seams: the shared `_resolve_action_frame_context` (fixes all 8 context kernels at once + makes their hardcoded goal-at-(105,34) correct), `_kernels._defensive_line_at_actions`, and `add_team_shape`/`_team_shape_at_actions`. `add_ghost_gk` emits action-LTR (`x→105−gr_x` uniform; `y→68−gr_y` away only) — model stays goal-relative; `compute_team_shape` made **orientation-aware** (deepest line nearest the defended goal; ltr/home byte-identical, only rtl/away changes). Self-reconciling features (`structural_pass`/`shape_graph`/`das`/`pitch_control`/`xt_gk`) DON'T use the context and are untouched (no double-flip). **CORRECTED in 4.52.0 (ADR-041): `obso`, `space_creation`, `pausa`, `player_influence`, `cover_shadows` and `gk_influence` were listed here in error** — they don't use the shared context, but they were NOT self-reconciling either; `obso` simply never handled orientation at all (`home_team_id` was a dead parameter), and `player_influence` multiplied a y-mirrored raw `interpolator()` grid. They are now reprojected AT THEIR OWN QUERY SEAM: the action-LTR target is point-reflected into frame coords and the attack-LTR EPV/threat grid is point-reflected (`[::-1, ::-1]`, BOTH axes) whenever `acting_team_attacks_rtl`. **CORRECTED AGAIN 2026-07-29 — that sentence is TRUE of `obso`/`pausa`/`player_influence` and FALSE of two of the six** (spec `docs/superpowers/specs/2026-07-29-adr028-orientation-defect-class-design.md`): `space_creation` was NEVER reprojected — `_compute_space_creation_for_action`'s `home_team_id` is a DEAD parameter (verified: identical output for `{HOME, AWAY, 999}`) and ADR-041 gave it only an unconditional `axis=(0,1)` opponent mirror, so its two emitted columns were EXCHANGED for every away action (**RC3, FIXED 4.71.0/PR-S139**: `compute_space_created` gains a keyword-only `attacks_rtl` threaded from the FRAMES via `acting_team_attacks_rtl`, and point-reflects the attack-LTR `transition_grid`/`epv_grid` — **the two GRIDS, never the finished `obso_multiplier`**, which also carries a ball-anchored `distance_weight` computed in frame coords that must never be mirrored, the rule the opponent branch already followed; reflecting at the grid seam corrects the opponent multiplier for free since it is built as a flip of the same artifacts. `home_team_id` stays in the signature and stays UNREAD — D3 retires it by disuse, not removal); and `cover_shadows` was only HALF reprojected — the RECEIVER at `_cover_shadows.py:1164`, never the PASSER at `features.py:3698`/`:3861` (**RC1, FIXED 4.70.0/PR-S138**: the passer is now reprojected INTO frame coords at both seams — everything downstream of that tuple is frame-convention and the xT lookup already reprojects itself; in the xfns path the reprojection precedes `_get_cs`, which rounds `passer_xy` into its cache key). A third, `gk_influence`, reprojects the right thing keyed on the WRONG thing: `_gk_influence.py:371-372` applies the both-axes point reflection but gates it on `same_id(attacking_team_id, home_team_id)` — team IDENTITY, not attacking DIRECTION — so it is correct only while frames are home-attacks-right (**D3 — CLOSED in 4.80.0; see the D3 bullet below**). Durable guard: `tests/tracking/test_action_ltr_mirror_invariance.py` (hand-listed, 5 aggregators) PLUS `tests/tracking/test_mirror_registry.py` — a registry-driven pair of gates over all 33 registered `add_*`: **Gate A** (physical mirror; detects convention mixing) and **Gate B** (vary `home_team_id` on FIXED frames; detects identity-keyed direction, which Gate A is structurally BLIND to because swapping `home_team_id` restores the very invariant identity-keying assumes). **VAEP/tracking-retrain trigger** (away-team values change; home-team byte-identical). C4-free (no new aggregator/backend/model). Atomic mirror inherits.
- **Frame-LTR orientation is single-sourced for consumer-built frames (ADR-029, 4.27.0).** ADR-028's per-action re-projection only works on frames already in the canonical **home-attacks-right** convention; it no-ops on absolute frames (`team_attacking_direction` all-null) — filtering to `.notna()` rows yields an empty direction lookup → no flip → ~50% mirror-wrong geometry. Providers converted via `convert_to_frames(output_convention="ltr")` (sportec/GS) or the kloppy gateway (metrica/skillcorner) are already oriented, but a consumer holding **bronze DataFrames** (not a kloppy `TrackingDataset`) builds frames itself and must orient them. Public `tracking.orient_frames_to_ltr(frames, *, home_team_id, home_team_start_left, home_team_start_left_extratime=None)` is the **unlabeled-input** sibling of `play_left_to_right` (which serves already-labeled frames): pure composition of `compute_attacking_direction` (private) + `play_left_to_right` with fail-loud guards (missing-schema; already-labeled → raise + route to `play_left_to_right`; zero home-match (ADR-019); ET-without-flag). Two entry points by input state — labeled → `play_left_to_right`, unlabeled → `orient_frames_to_ltr`. **Consumer contract:** any consumer building frames from a non-kloppy source MUST orient before the geometry layer; the helper is only as correct as the caller-derived `home_team_start_left` (validate per game — e.g. defending GK lands near the attacked goal post-orient). Additive — no provider behaviour change, no silly-kicks retrain; the lakehouse re-materializes metrica/skillcorner after adopting it. The sportec/GS adapters are NOT refactored through the helper (primitives already shared; Chesterton's Fence). Positive ET orientation is regression-guarded on both the helper and the native adapters (`tests/tracking/test_adapter_extra_time_orientation.py`), prompted by a live GS-ET flip that was a consumer-side `home_team_start_left_extratime` placeholder bug, not a silly-kicks bug.
- **The kloppy TRACKING gateway pins the canonical SPADL coordinate system (ADR-031, 4.29.0).** `tracking/kloppy.py` previously emitted frames **y-mirrored** vs SPADL actions (`action_y == 68 − frame_y`) for SkillCorner / Metrica / IDSSE-dev: the EVENT gateway (`spadl/kloppy.py`) pinned `_SoccerActionCoordinateSystem` (now extracted to the shared `spadl/_kloppy_coordinates.py` + `socceraction_coordinate_system` helper, imported by both gateways), the tracking gateway never did, so frames kept each provider's kloppy-native vertical. Single-axis y mirror (NOT orientation — orthogonal to ADR-028/029; error `|68−2y|` → 0 at centre, ~full width at touchlines, which hid it). Fix: `convert_to_frames` pins the CS via a **CS-only `transform()` signature** — dropping `to_pitch_dimensions`, which otherwise silently OVERRIDES the CS vertical and leaves y inverted (Gate 0); the CS carries its own standardized 0–105/0–68 dims, so x is byte-identical. **NOT** a blanket `y=68−y` flip (which would double-invert an already-canonical provider; no-op-on-canonical guarded). DGX-verified: SkillCorner + Metrica both inverted → fixed (**calibration retrain trigger** — but the lakehouse builds SC/Metrica via its OWN bronze builders, so PR-S94 fixes the calibration/pining path + external gateway consumers, NOT lakehouse prod); the **native** sportec/IDSSE path is y-CORRECT (Gate D — no fix, no retrain). First of a sequence (PR-S94 / T1); the IDSSE/Sportec DFL **parse-port** single-sourcing (T3, PR-S95) + the lakehouse's own SC/Metrica builder y-check follow. C4-free (no new aggregator; count stays 28).
- **TF-23b geometric frame-LTR backstop on the native tracking adapters (ADR-035, 4.34.0).** `tracking.{gradientsports,sportec}.convert_to_frames` now self-correct a wrong/absent `home_team_start_left_extratime` from GK geometry: a shared `direction.finalize_orientation` tail (ET guard → per-period flag flip → period-gated label → the idempotent `orient_frames_to_ltr_by_geometry` backstop) replaces the duplicated inline orientation block in both adapters (**supersedes the ADR-029 no-refactor fence for the native adapters ONLY** — kloppy gateway stays intentionally un-routed). The net gains `on_missing_home` (default `"raise"`; adapters pass `"warn"` to keep their warn-don't-raise contract) + `copy` (default `True`; `finalize` passes `False` to drop the redundant 3rd full-frame copy on tracking-scale data) and **no longer orients period-5/PSO frames for ANY caller** (incl. the TF-23 SC/Metrica builders; PSO excluded from geometric analysis). **Byte-identical no-op on the correct-flag path** (DGX G1, strict-dtype: 69/71 corpus matches byte-identical; the only 2 changed are GS WC2022 ET-tracking matches **10506 + 10517** whose constant placeholder flag was geometrically wrong → **VAEP/tracking-retrain trigger for those 2 ET-frame sets only**). Closes ADR-031 **Gate D** (sportec/IDSSE-ET handedness; gated on synthetic sportec ET + the provider-agnostic mechanism — 0 real IDSSE-ET in the pining corpus, G3 re-scoped). C4-free (count stays 28).
- **Trained artifacts are parameters-only, pickle-free, and fail-closed on load (ADR-011, ADR-016, ADR-040, ADR-044, ADR-050).** Every bundled model (`GhostGkModel`, `XShotOccurrenceModel`, `XCrossAttemptModel`, `GkCompletionModel`, `GkRetentionModel`) serializes as npz or booster-JSON plus JSON metadata plus `SHA256SUMS` -- never pickle -- and inference never imports sklearn. Distributed ghost artifacts carry **no training data** (`stores_training_data:false`), so `predict_density` and `ghost_gk_density_spread` exist only on a locally `fit()` model. `load()` enforces two independent guards. **Chirality** (`_chirality.verify_chirality`) re-runs the model's own outputs on `canonical_probe_frame` and raises both on a MISMATCH and on a MISSING fingerprint -- every pre-enforcement artifact is the mis-served class -- with `legacy_override=True` as a warning escape hatch. The **feature contract** (`_feature_contract`) records the feature vector an extractor produces on a fixed probe plus the geometry constants it consumes: a missing contract WARNS (pre-contract artifacts are undeclared, not known-bad), a probe change warns and skips only the fingerprint comparison, and a fingerprint or declared-constant mismatch RAISES with that model's own `IntegrityError`. **Declared constants compare FIRST and ALWAYS**, including when the probe changed, because a sub-probe-resolution change moves no feature and only the declared constant catches it. Tolerance is chosen, not inherited (`atol=1e-6`, `rtol=0`); `equal_nan=True` is load-bearing, since `np.allclose(v, v)` is False for any NaN-bearing vector; and a declared constant MUST be load-bearing on the probe, or the guard fires when nothing changed and `legacy_override` becomes reflex. `load_xgb_booster_base_score_safe` strips the bracketed `"[X]"` `base_score` that xgboost 3.x writes and 2.x silently drops to the `0.5` default.
- **Geometry constants are canonical and CI-enumerated (ADR-050).** Use `spadlconfig.penalty_area_half_width` / `penalty_area_depth` and the two frame-explicit predicates `_geometry.in_penalty_area_absolute(x, y, *, attacked_goal_x)` / `in_penalty_area_goal_relative(gr_x, y)` -- two entry points because the call sites differ on FRAME, not just strictness, and `goal_x` already means the *defended* goal in `_geometry` (same name, two readings, an 88.5 m error). The goal-relative form takes no goal argument so the ambiguity cannot re-enter. `_ghost_gk` deliberately keeps its own 40.3 (its weights were fit on it), pinned by a raise rather than a comment a contributor can delete. Rule: **a module-level constant exists iff something in that module reads it** -- aliases would satisfy the enumerator vacuously. `tests/tracking/test_geometry_constant_enumeration.py` AST-walks the extractor modules and requires every geometry constant either declared in a model's contract or `_EXEMPT` with a reason.
- **The defended goal end has ONE implementation: `resolve_defended_goals(frames) -> GoalMap` (ADR-055).** It replaced **ten** hand-rolled `0.0 if same_id(team, home_team_id) else 105.0` forks across 5 modules, and an eleventh fails CI — `tests/tracking/test_goal_map_population.py` is a SEMANTIC AST gate (`IfExp` ∪ `If`/`else`-assigning-one-name ∪ `np.where`; a ternary-only predicate was blind to four `if` statements), landed RED and observed failing on all ten. Three rules, none optional. **(1) Build ONCE per match from the FULL frames and THREAD it in.** The quantity is a mean GK x per `(game, period, team)`; a per-frame map is a different estimator, and the cost is provider-DEPENDENT and measured on the four committed slim fixtures: per-frame disagrees with the per-match map on **7.1%** of team-frames (skillcorner) / 2.2% (metrica) / **0.0%** (sportec, gradientsports), while `attacked_goal` is unresolvable for **35.7%** (skillcorner) / 61.7% (metrica) / 0.0% (dense). The spec's headline **78.8%** comes from its own corpus and does NOT reproduce on these fixtures (max 7.1%); its 34.2% unresolvable rate does (35.7%). The design decision stands on the reproduced numbers alone -- a third of team-frames losing direction is decisive, and on dense tracking building once per match costs nothing. So per-frame functions (`lane_control`, `compute_blocking_score`, `compute_gk_influence`, `_voronoi_threat`, `_compute_cover_shadow_dict`) take `goal_map: GoalMap` **required, no default** — a default re-admits the per-frame path at exactly the call sites that forget it — while `add_*` aggregators take `goal_map=None` and build from their own frames, which is correct by construction AND what makes Gate C possible. **(2) Keys are canonical STRINGS** (`canonical_id(1) == '1'`, `canonical_id(pd.NA) is pd.NA`, never `None`): never hold the mappings as a plain dict, because a raw-tuple lookup MISSES SILENTLY — it shipped exactly that way in `scripts/validate_shot_goalmouth_sb.py`, scanning `(k[0], k[1]) == key` and returning NaN for every row. Use `get` (own end) or `attacked_goal` (opponent's end); `attacked_goal` is a REAL lookup of the opponent's entry, never `105.0 - get(...)`, which is wrong on a degenerate map and would say a team attacks the goal it defends. **(3) An unresolvable end REFUSES.** `GoalEndUnresolvedError` (a `ValueError` subclass) is raised by the per-frame functions and caught BY NAME at the `add_*` edge, which emits a NaN row — policy at the edge, and pre-checking at the aggregator is forbidden because it duplicates the exact lookup the callee is about to do. The old sites all failed OPEN (`nan < 52.5` is False → a confident 105.0). **15 breaking public signature changes across `tracking` and `gkdv`, two renames, and a VAEP/tracking retrain trigger**; `add_cover_shadows` is now KEEPER-DEPENDENT on freeze-frame input.
- **Gate B goes vacuous the moment an aggregator is re-keyed off `home_team_id`, so Gate C replaces its DETECTION (ADR-055).** Gate B varies `home_team_id` on fixed frames; once the parameter is gone it skips on `role="unused"`, and the AST population gate cannot see a direction *bool* — so a re-key would delete the only detector for the class it fixes. **Gate C holds the frames FIXED and swaps the MAP**, requiring the columns named in `gate_c_must_move` to move; it reproduces the D3 magnitudes exactly (`share 0.108532`, `closing_min 4.38062`, `closing_mean 4.02205`, `blocking_score 148.83`). Two limits are recorded AT the gate, not inferred: it proves the map is CONSULTED, not that the right accessor was chosen (`get` and `attacked_goal` both move) — correctness lives in `tests/tracking/test_goal_map_consumers.py`; and it is structurally BLIND to any path the registered aggregator does not call. Measured: patching `_closing_time_per_series` back onto a self-built map leaves Gate C GREEN, because `add_gk_influence` never calls it. Name the columns, never assert a bare `moved > 0`. **4.80.0 found the LIMIT of that substitution: Gate C only covers the sites that take a MAP.** Four of D3's six do; the two bool sites (`add_structural_pass`, `add_player_influence`) take none, so a Gate C entry for them would swap an input they never read and **pass by ignoring it** — vacuous by construction, and the completeness gate's `declared − observed` half rejects it. Their detector is a behavioural **direction-invariance** test (`tests/tracking/test_d3_direction_invariance.py`): mirror the FRAMES, hold `home_team_id` CONSTANT, require action-LTR geometry unchanged. It sees the defect AND survives the fix, which neither Gate A (it swaps the id too, restoring the assumed invariant) nor Gate B (vacuous after the fix) can do. Landed RED — observed at `defensive_line_x` 23.25, `packing_made` 6.0, `packing_goal_threat` 4.0. **`gate_c_must_move` is now checked for COMPLETENESS, not just satisfaction**, because a hand-picked subset lets a partial re-key ship green: `packing_goal_threat` is the ONLY witness for packing's back-line site and was nearly dropped as dead, being constant `0` on the base leg — `0` is CORRECT there, and flipping the end moves it to `[4, 1, 1, 1]`. **A detector's liveness is not "does it vary across rows" but "does it move when the thing it detects changes"**; a base-leg constancy screen classifies it identically to `back_n_count`, which genuinely cannot move, and they need opposite verdicts.
- **Direction NEVER comes from team identity, and "unresolved" is a VALUE, not a default (ADR-051 D3 closed, 4.80.0).** `same_id(team, home_team_id)` is correct only while frames are home-attacks-right and **silently inverts otherwise** — a confident wrong answer, which is why it outlived ADR-055 by three releases. **Mechanism is PER SITE, and ADR-055's `goal_map` ruling does NOT generalise:** functions serving BOTH teams take the map (`_defensive_line`, `_packing`); functions serving ONE take a **bool derived from `acting_team_attacks_rtl`** (`_structural_pass`, `_line_breaking`, `_off_ball_runs`, `_player_influence`). The bool is resolved ONCE at the `add_*` EDGE and threaded down — `acting_team_attacks_rtl` is per-ACTION while the sites are per-FRAME geometry, so calling it in situ would be a worse ISP violation than the map it replaces; and at `_player_influence` a map is a category error, since the site reflects a GRID and would collapse a pitch-x to a boolean on its first line. Threading a map into a one-team site also REVERSES the consolidation ADR-042 performed onto the single orientation authority. **`acting_team_attacks_rtl` returns `dtype="boolean"`, and `<NA>` means unresolved** — it previously returned bare `bool` with an internal `.fillna(False)`, so a resolved left-to-right team and an unknown one were THE SAME VALUE and 21 call sites inherited the guess. `.fillna(False)` is still right at many of them; it just has to be WRITTEN, with a reason. Where a guess would fabricate: `_player_influence` blanks its three xT columns and KEEPS `reachable_area*` (measured exactly flip-invariant, max |delta| 0.0 vs 1.17e3 for `off_ball_xt`); `add_space_creation` refuses the whole row, because its two columns are EXCHANGED rather than degraded and half an exchanged pair is not a partial answer. **Consequence: UNORIENTED frames now yield NaN where they used to yield a confident wrong number** (ADR-029's negative control flipped from asserting the defect to asserting the refusal). The contract paid for itself immediately, exposing two live defects: `frames["is_ball"].astype(bool)` — the ADR-019 string-qualifier trap, so `~` selected NO player rows and the resolver fell through for every provider with an object/string `is_ball`, invisible because the fall-through returned all-`False`; and `_unresolvable_direction_mask`, a SECOND hand-rolled resolvability test that repeated that trap AND tested membership with a RAW tuple against a `(game_id, period_id, team_id)` index (ADR-055 rule 2) — **deleted, not repaired**, since the `<NA>` value removes any reason for a consumer to re-derive it. **Two rules the migration produced.** A scope predicate bounds the DEFECT, never the API MIGRATION: the D12 pin (`in scope iff it CALLS same_id/ids_match with home_team_id`) is recall-complete over the defect and structurally BLIND to eleven public per-Series helpers in `features.py` that merely DECLARED and forwarded the parameter — five shipped forwarding into a kernel that had stopped accepting it. **Enumerate a removed parameter by AST signature-diff against the base commit**, which is also what separates them from the ten dead-parameter functions that pre-date the change. And an `add_*` refusal path must build every column the code AFTER the `try` reads, not just the ones it emits: `add_packing`'s `GoalEndUnresolvedError` fallback omitted the internal `line_x`, turning the ADR-055 NaN row into `KeyError`. **A dead parameter is removed WITH the use, or it is never removed** — 4.80.0 also deleted 25 `home_team_id` parameters that nothing read (8 dead at the base commit; the cascade killed 17 more, and the cycle total across all three arcs is 62 signatures), residue from ADR-028/041 re-keys that took out the *use* and left the *signature*, and they had to be driven to a FIXPOINT because the dead ones formed forwarding CHAINS (the obso family existed only to hand the argument to `_precompute_obso_lookup`, which ignored it; removing the sink killed five public signatures above it). Two mechanical traps, both hit: a reads-counter calls a parameter LIVE when its only use seeds an unused closure default (`_htid=home_team_id`), and a call-site sweep keyed on the callee's NAME misses ALIASED forwarding (`_std = tracking.add_xt_gk`), which is how the atomic mirrors were left broken after their target was cleaned. **Verify a fence before removing it, and expect the answer to differ per site:** `add_xt_gk`'s documented "GK-feature-family signature parity" was measurably stale (ADR-055 had re-keyed two of that family, so parity meant matching the one member that still READS it), while `_off_ball_runs_kernel` and `_compute_space_creation_for_action` KEEP their unread copies — the first because its Gate B green IS the standing measurement that the parameter is unread.
- **"Nothing there" is not "nothing VISIBLE there" (ADR-055).** `tracking/_visibility.py` ships `point_observed` (`bool | None` — `False` is a CLAIM, and a missing polygon supports no claim), `region_observed_fraction` (an `(M, 2)` POLYGON, never a bbox: a bbox can only OVER-report coverage for a triangle, i.e. fabricate observation) and `add_visible_area_coverage` emitting `visible_area_fraction` + `visible_area_source` over the closed `{observed, no_polygon, degenerate_polygon, unlinked}`; the fraction is NaN for every non-`observed` token — **never 1.0, never 0.0**. Sutherland–Hodgman needs a convex CLIP, so the provider polygon is always the SUBJECT and the REGION is the clip — that puts the convexity requirement on the argument the caller constructs and lets a concave region be REFUSED rather than silently mis-clipped. Primitives live in the neutral `silly_kicks/_polygon.py` (the `id_compat` / `reflection` position) because `providers/` has no runtime dependency on `tracking/` and adding one would invert the port layering. `visible_fraction` → **`observed_pitch_fraction`**, now CLIPPED and NaN-on-degenerate (the polygon VERTICES stay unclipped — ADR-054 D5 is about the vertices). **The PRODUCER must honour the same split (4.77.1):** `shape_snapshots` emits a `visible_area` row whenever something WAS published, even when it converts to nothing usable — dropping it collapses `degenerate_polygon` into `no_polygon`, which the seam exists to keep apart, and it shipped that way (a published 2-vertex polygon read as "nothing published"). `polygon_to_spadl` therefore returns an empty `(0, 2)` on a malformed odd-length list rather than raising mid-corpus; the empty array is below `MIN_VERTICES` and reads as degenerate. The 4.77.0 note declining this on "fail-loud vs degrade" grounds was **wrong** — it enumerated crash and silence while the seam already shipped the honest third answer. **A scope decision defended by a false dichotomy is still a scope decision;** the tell is a justification appealing to a principle the module's own vocabulary contradicts. Wiring coverage INTO the count features is deliberately NOT done: it changes existing values and decides for the consumer what a partial observation means (ADR-009).
- **The goal-relative transform is a 180-degree POINT REFLECTION, not an x mirror (ADR-051 §8b).** `_geometry.to_goal_relative_x`/`_y` map `(x, y) -> (105-x, 68-y)` when the defended goal is at high x, so the two goal ends differ by a ROTATION. Adding a signed-y quantity without routing it through `to_goal_relative_y` reintroduces the defect it fixes: before 4.74.0 there was no y counterpart, so `goal_x=105` was determinant **-1** against an identity at the other end, every RADIAL stayed byte-identical and every BEARING negated (xS 12/27 features, xCross 3/16) — one physical scene scored differently depending which END the acting team attacked. Gated per-feature by `tests/tracking/test_pr5_chirality_gates.py`; the durable evidence is end-invariance on REAL frames (1,360 scenes, feature and prediction delta exactly 0), because the synthetic gate alone would pass if BOTH ends were wrong the same way. **`GEOMETRY_VERSION` must be bumped whenever the transform's numeric output changes** — but note its load path only WARNS on mismatch (the fail-closed prong is on pitch dims), so the real protection is chirality + feature_contract, and a change relaxing those two must not assume `geometry_version` holds the line.
- **A liveness gate's FIXTURE needs its own precondition test (ADR-032 idiom, re-learned 4.74.0).** A gate is only as good as the rows it scores, and two silent fixture defects made the xS liveness gate score noise for three cycles: rows ~3x outside the model's trained domain (so it measured extrapolation) and a generator that zeroed `vx`/`vy`, pinning ball speed to ONE value — the same degeneracy PR-S118 found in the xCross fixture and left in the xS one. Two statistically equivalent models scored AUC 1.0 and 0.0 on it. When a gate asserts a behavioural property, pin the fixture's validity separately (in-domain, no constant feature, enough rows), and assert only what the guarded thing actually does — the retired version asserted `near > far`, which BOTH models contradict (`corr(r,p)` +0.89 and +0.94).
- **A feature that reads its own action's `result_id`, or any post-contact outcome, ships with NO `*_xfns` factory -- or stays out of every default xfn list.** Enforced by an auto-discovering absence guard anchored on the transformer NAME, so renaming a factory cannot silently disarm it. TF-48 shot goalmouth (ADR-030) and TF-51 defensive credit (ADR-047, ADR-049) ship no factory at all; `packing_xfns` (ADR-039) and `off_ball_run_value_xfns` (ADR-042) exist but are in no default list. Opting a leaky factory into a default list is a HybridVAEP-class correctness break, not a tuning choice.
- **`DasUnscoreableError` is the ONLY degradable DAS exception (ADR-043).** Everything else PROPAGATES. The former broad `except (ValueError, RuntimeError, ImportError, IndexError, TypeError)` swallowed silly-kicks' own bugs, and an all-NaN column is indistinguishable downstream from legitimately-absent DAS. `add_das` emits a `das_source` column over the closed `DAS_SOURCE_VALUES` vocabulary (each token also exported as a `DAS_SOURCE_*` constant so consumer enums pin to the library's set); `das_at_action` returns a bare Series and carries no provenance; `das_xfns` deliberately does not emit the string column, because VAEP matrices stay numeric.
- **`speed_source` has three tokens, not two.** `TRACKING_CATEGORICAL_DOMAINS["speed_source"]` is `{native, derived, unavailable}`. `"unavailable"` is a frame builder DECLARING that its source has no per-player temporal history, so `speed`/`vx`/`vy` can never exist -- deliberately distinct from a NULL `speed_source` ("not derived YET"), because the two demand opposite responses. Third-party builders may set it; it is a public contract, not a snapshot backdoor. An UNMARKED or PARTIALLY-marked frame set missing `vx`/`vy` still RAISES: fail-loud wins on a mixed frame set.
- **Public warning categories are deliberately separate, never one umbrella.** `SyntheticEPVWarning`, `IgnoredSurfaceInputsWarning`, `RunValueCoverageWarning`, `MissingFeatureContractWarning`, `UnverifiableFeatureContractWarning`, `OrientationUnresolvedWarning`, `GhostClampWarning`, `UndeclaredGeometricColumnWarning`. None subclasses another (test-enforced): silencing a routine notice must never silence genuine misuse, and escalating the missing-contract case must never turn every benign probe extension into a hard failure. CI escalates `SyntheticEPVWarning`, `IgnoredSurfaceInputsWarning` and `MissingFeatureContractWarning` only -- `OrientationUnresolvedWarning` is NOT escalated, and escalating it remains an open decision rather than a guarantee to rely on.
- **Reflection goes through the one public seam `silly_kicks/reflection.py` (ADR-045).** A 180-degree point reflection point-reflects POINTS, **negates VECTORS** (`vx`/`vy`/`dx`/`dy`), leaves MAGNITUDES (`speed`) alone and swaps DIRECTION LABELS. Every column declares a `ReflectionKind` in `TRACKING` / `SPADL` / `ATOMIC_SPADL_REFLECTION_KINDS`; `reflect()` serves schema-bearing tables and `reflect_columns()` derived or pre-canonical frames. The historical defect class is columns that an explicit per-site list simply omitted -- `vx`/`vy`/`x_smoothed`/`y_smoothed` are not in `TRACKING_FRAMES_COLUMNS` (preprocess adds them), so they rode through untransformed. `on_unknown` defaults to `"warn"`, NOT `"raise"`: `to_spadl_ltr` runs inside nine converters on frames carrying the caller's unbounded `preserve_native` columns, so a raise has no reachable remedy; fail-closed lives in the CI registry-completeness meta-assertion instead. Consumers who control their column universe escalate via `filterwarnings("error", category=UndeclaredGeometricColumnWarning)`. **Guards on reflection defects must be per-row, never aggregate** -- measured mean biases of -1.1% and -0.002 pass an aggregate check vacuously.
- **`ExpectedThreat.xT` stores rows y-INVERTED; `rate()` compensates and `interpolator()` does NOT (ADR-041).** `xthreat/_physical.py` is the ONE place that inversion is neutralized: `physical_grid(model, grid_x, grid_y)` resamples onto a **consumer-supplied** grid in ascending-y physical orientation (pass the consumer's own grid; do not invent one), `values_at_points` gives exact NaN-tolerant `rate(use_interpolation=False)` semantics, and `require_fitted_xt` is the single fitted-model guard. Never multiply a raw `interpolator()` output into a physical surface. `compute_threat_pc` REFUSES an unfitted xT -- it used to return `0.0`, so any caller persisting a threat column persisted structural zeros that look like a measurement.
- **Policy lives at the edge, never in the shared engine.** The ghost pitch clamp sits at the serving seam, not in `predict_mean` (whose exact-boosted parity contract must stay clean); the restart geometry tripwire sits at the `add_restart_coordinates` edge, not in `resolve_restart_geometry`; the per-type base-rate switch sits in `compute_xt_gk`, not in `predict_proba`, which stays a pure scorer. A guard that changes numbers belongs where the decision is made.
- **The library ships RAW primitives; composites, archetypes and rankings stay consumer-side (ADR-009).** TF-45 emits `structural_lbs` / `structural_sgm` / `structural_sdi`; the TIV z-norm composite, the K-means archetypes and the delta-TIV/cumulative rankings are frozen-exogenous consumer concerns.
- **Corpus visibility is fail-closed (`scripts/_corpus.py`, ADR-038).** `is_public_row` treats unknown or missing visibility as restricted, `artifact_label` derives from the ship-mask composition, and `assert_public_corpus` subset-gates. There is deliberately no `_PUBLIC_PROVIDERS` provider allowlist -- it labelled a restricted-corpus run `"public"`, i.e. marked a non-redistributable model reproducible. A trainer with no taxonomy enforcement must be pointed at a public-only corpus by hand.
- **A validation harness must make the observed outcome UNREPRESENTABLE, not merely refused.** `causal.power.att_power_curve` accepts no outcome vector at all -- only an `InjectionSpec` recipe it draws from per replicate -- so machinery that could answer an open research question before the sign-off meant to authorise it structurally cannot. A call-count spy on `estimate_att` would be vacuous (the harness always calls it); the guard is provenance, and its non-vacuity is demonstrated by removing the guard and watching a fully duck-typed fake succeed silently. Relatedly, an inestimable replicate is COUNTED (`n_degenerate_by_size`), never crashed on and never silently skipped: excluding it from the denominator would condition on estimability and inflate the curve exactly where the design is weakest.
- **A provenance COLUMN where the VALUE changes; a DIAGNOSTIC where only the INTERPRETATION changes (ADR-054).** `add_ghost_gk` earns `ghost_gk_source` because a number becomes NaN -- a per-row fact. The five other aggregators whose output moves with velocity (`add_elastic_sync`, `add_obso`, `add_pausa`, `add_pitch_control`, `add_space_creation`) produce an HONEST value either way -- pitch control at zero velocity is a well-defined positional model, not a fabrication -- so what changes is how it should be READ, which is a property of the whole frame set. Hence public `validate_velocity_regime`/`VelocityRegimeDiagnosis`, the third member of the `validate_time_base` (ADR-017) / `validate_id_dtypes` (ADR-019) family, split across `schema.py` and `utils.py` the way that family already is. It carries FIVE regimes, because "forgot `derive_velocities()`" is NOT a variant of `mixed`: nothing is structurally missing there, and labelling it `mixed` would raise with a false explanation in the case a user is most likely to hit. **The velocity guard itself lives at the SHARED SERVING SEAM `_serve_positions_core`**, never at one aggregator: there are THREE public ghost entry points and two bypass `add_ghost_gk` (`ghost_gk_xfns` -> `compute_ghost_gk`, and `gkdv` -> `serve_ghost_gk_positions`). The seams degrade DIFFERENTLY and the asymmetry is forced -- `serve_ghost_gk_positions` returns NO rows rather than NaN ones, because `gkdv/_engine.py` RAISES on a non-finite ghost on a scored frame.
- **Every `add_*` carries an SB360 freeze-frame verdict, and the OBSERVATION is locked while the ADJUDICATION is not (ADR-053).** `tests/sb360/` runs each aggregator on a paired fixture -- Leg A built by the real `snapshot_to_tracking_frames`, Leg B velocity-bearing with identical positions at the linked frame -- and records a per-COLUMN, per-AXIS verdict. CI re-derives the machine observation (`identical`/`differs`/`all_nan`/`partial_nan`/`no_signal`/`raises_a`) and asserts it; the human adjudication (`works`/`silent_degrade`/`differs_by_design`/`honest_nan`/`not_exercised`/`raises`) carries a mandatory rationale and is NOT locked, because a machine cannot tell *fabricated* from *legitimately different* -- pitch control at zero velocity is a valid positional model, a fitted model silently imputing the features it was trained on is not (`add_ghost_gk` WAS that case: the extractor yields NaN and the HGBR routes it down a LEARNED missing-value branch, which is not a zero-fill -- the verdict was right while the first-recorded mechanism was wrong, which is exactly why the adjudication stays reviewable). **REPAIRED: the ghost path now REFUSES on the velocity marker at the shared serving seam `_serve_positions_core`, so all three public entry points inherit it, and the audit re-derives to ZERO `silent_degrade` -- the positions moved to `honest_nan` by rule, not by hand-edit.** The example survives as the case that motivated the split, not as a live defect. Locking the verdict instead was tried and rejected: it pins the key set while the content rots. A new `add_*` must register or CI fails. Registry regenerable via `tests/sb360/_regenerate.py` + `_adjudicate.py` (round-trip verified byte-identical).
- **Feature columns are documented in `silly_kicks/feature_glossary.py`, and coverage is CI-gated (ADR-048).** Every derived column carries a `FeatureColumn(name, definition, unit, emitting_module, attribution, higher_is_better)` record; `emitting_module` is the metric's HOME compute module (`_packing`, `_obso`, ...), not the `features.py` monolith where the public producer happens to be defined. `describe_level(z, *, higher_is_better=True)` lives separately in `silly_kicks/reporting.py`, because direction is glossary metadata and the caller supplies it -- so a lower-is-better metric is never mislabelled. A new `add_*` or `*_xfns` must document its columns or the coverage gate fails; the load-bearing completeness dependency is the `add_*` / `*_xfns` NAME SHAPE.
- **The C4 model (`docs/c4/`) is pinned to the code.** Subpackages are derived from the package tree by a completeness gate -- nothing else pinned the diagram to the code, which is how a public subpackage went unmodelled for five releases. A new action-coupled `add_*` aggregator changes the documented count.
- **Frame-aware `*_xfns` must resolve frames POSITIONALLY, never by `action_id` lookup (ADR-020).** VAEP shifted gamestate slots repeat the boundary action, so `action_id` is NOT unique in an xfns matrix: `set_index("action_id").at[aid]` returns a Series and raises, and provenance merges fan out. `_kernels.resolve_frame_ids_by_position` is the single seam, byte-equivalent to the old `.at` on unique ids. The production `add_*` aggregator path has unique ids and is unaffected, which is precisely why this survived so long -- so the gate is behavioral and auto-enumerates every `*_xfns` (`tests/tracking/test_frame_aware_xfns_dup_action_id.py`). A `.at` grep missed two of the eight defective families; the behavioral gate found all eight.
- **A coverage denominator must never masquerade as a tactical signal (ADR-042).** Mean disruptive run value divides by `n_valued_disruptive_runs`, NEVER `n_disruptive_runs`, or tracking coverage reads as tactics. A runner absent from the linked frame keeps `run_value` NaN and keeps its ROW (`RunValueCoverageWarning`) -- never a fabricated `0`. Same reasoning behind emitting `peak_speed_source` as DATA: the all-NaN-speed fallback under-states peak speed, so sparse providers legitimately LOSE runs and the consumer has to be able to see that.
- **Detection lands BEFORE the fix, and every registry gate carries an anti-rot meta-assertion (ADR-051).** A gate written after its own repair arrives green and is never observed failing, so the mirror gates above were landed RED -- enumerating all 33 aggregators before a single fix existed. Both pin their registry to `tracking.__all__` in BOTH directions, so a new `add_*` must register or CI fails. Corollary to Gate A's blindness (see the ADR-028 bullet): a clean Gate A reading is NOT evidence about identity-keyed direction -- one such reading was withdrawn on exactly that ground.
- **A spec that changes a public seam enumerates every caller of every CHANGED FUNCTION and classifies each as affected or not WITH EVIDENCE ON BOTH SIDES -- and the sweep is the FLOOR, not the check.** Measured on the ADR-051 PR 5 cycle: the sweep took under a minute, returned five call sites, and resolved two questions in OPPOSITE directions (`causal/opportunities.py` affected because it builds causal covariates from the changed extractors; both frozen fixtures unaffected because they are built at `goal_x=0.0`, where the transform is the identity and every delta measured exactly zero). It also MISSED four things, which is why the rule names them. A symbol sweep cannot see (a) **a changed function nobody thought to grep** -- that PR changed FOUR and the sweep covered three, missing the `_dominant_region_area` grid re-anchor entirely, which moved a causal confounder at BOTH goal ends; (b) **wrapper callers** -- `derive_opengoal_range.py` imports `prepare_xshot_training_data`, which wraps the changed extractor, and is itself in `ARTIFACT_DRIVERS`; (c) **committed data artifacts**, which call nothing -- a frozen fixture went stale in `space_controlled` and no sweep could say so; (d) **numbers recorded in prose** rather than in a driver-produced file; (e) **the second hop** -- the sweep returns a module, never the research artifacts downstream of it. So ALSO enumerate: every function in the diff, every committed fixture derived from them, every research artifact downstream of a changed module, and every number recorded outside a driver artifact. Spec `2026-08-03-adr051-closeout-and-artifact-validity-design.md` §7.1.

## Testing

```bash
python -m pytest tests/ -m "not e2e" -v --tb=short
```

e2e tests require dataset fixtures not committed to the repo. Tests with
fixtures committed to the repo should not be marked e2e — they run in
the regular suite.

**Lint at the CI scope, never `.`** — `python -m ruff check silly_kicks/ tests/ scripts/` and
`python -m ruff format --check silly_kicks/ tests/ scripts/`, matching `ci.yml`. `ruff check .`
walks `.venv/` and `calibration_runs/` and reports ~234 vendored errors that are not the repo's,
which is enough noise to hide the real ones. `pyright` runs **bare** (config-driven include), and
neither tool is on PATH — use `python -m`.

CI runs a bulk suite step serial (`--benchmark-skip`) on every matrix leg and a benchmark
*measurements* step single-threaded (`--benchmark-only`) on the primary leg only (see the
slow-test-gating note below for the per-leg split). There are no wall-clock
`assert ms < budget` perf tests — performance regressions are guarded by
**deterministic structural guards** (call-count spies on each function's dominant
primitive; see `tests/_perf_structural.py` + the `*_perf_budget.py` files), which
never flake on shared runners. (An xdist parallelization was tried and reverted: it
regressed py3.12 on the 4-core/7GB CI runners — memory/JIT pressure.)

**CI's pandas-major span is DECLARED, not inherited (ADR-057).** The matrix is OS × Python with no
pandas axis, yet it spans both majors: `pyproject.toml` pins `pandas>=2.1.1,!=3.0.4` with **no upper
bound** and pandas 3 requires Python ≥3.11, so pip resolves the newest compatible pandas per
interpreter — measured, `ubuntu-3.10` → **2.3.3** and the other three legs → **3.0.5**. That coverage
was real but ACCIDENTAL, and could have vanished with no diff and no signal (this repo has one
measured instance of that class: DAS going silently all-NaN on pandas 3). It is now asserted by a
PAIR, because a test running in one leg cannot observe another: `tests/test_ci_pandas_span_wired.py`
pins that the **resolved leg set** still straddles the 3.11 boundary — parsing os × python-version
**minus `exclude` plus `include`**, never the axis, since `exclude` is the pruning mechanism already
in use and excluding a leg collapses the span while leaving its version in the axis — and a
`pandas-span` job (`needs: test`) asserts the union of each leg's recorded major covers both. Assert
the SPAN, never specific versions: pinning "3.10 → 2.3.3" fails on every routine bump and trains a
reader to edit the expectation without thinking. **Never assert a dtype literal across pandas
majors** — assert the behaviour (that `id_compat` comparisons still match), which is what made the
two-cycle-old `snapshot_to_tracking_frames` question finally answerable.

**The BUILD BACKEND is bounded and the PUBLISHER's validator is pinned — the pair, or neither works
(4.79.0).** Same shape as the span above, and it bit within hours of that ADR landing: every Action
in `.github/workflows/` is SHA-pinned while `[build-system] requires` was `["hatchling"]` with **no
upper bound**, so the artifact's `Metadata-Version` was a function of WALL-CLOCK TIME. hatchling
1.32.0 (2026-08-11 05:03Z) moved 2.4 → 2.5; `v4.78.0` built at 01:39Z and published, `v4.79.0` built
at 12:56Z and was REFUSED by the publisher's pinned `packaging==25.0`, whose valid set stops at 2.4
— **green build, green CI, failed upload, no diff between the two runs.** Fix BOTH sides and bump
FORWARD (`gh-action-pypi-publish` v1.14.2 = `packaging==26.2` + `twine==7.0.0`): metadata 2.5 is
legitimate, and pinning the backend below it only defers the failure. Bounded (`>=1.27,<2`), never
pinned — the point is that a backend release lands in a DIFF instead of in a failed publish.
`tests/test_ci_publish_guard_wired.py` rejects any unbounded `[build-system]` requirement; because a
bound-checker passes vacuously if its predicate says yes to everything, a companion test pins that
the predicate rejects the exact `"hatchling"` string that broke this release. **The durable rule: a
pinned consumer and a floating producer are one dependency, and pinning only the half you control is
not a pin.** Corollary for diagnosis: I first argued v1.14.2 wouldn't help because its RELEASE DATE
predates hatchling 1.32.0 — the wrong lens entirely; what decides it is the validator version the
action pins, which is readable in seconds from `requirements/runtime.txt` at that tag.

**Doctests: CI executes `--doctest-modules` on the PUBLIC surface only** (`pytest --doctest-modules
silly_kicks/ --ignore-glob="*/_[!_]*.py"`, every leg; PR-S124, wiring guarded by
`tests/test_ci_doctest_wired.py`). The glob skips single-underscore
private modules while KEEPING dunder `__init__.py`; private-module examples are kept CORRECT (the
whole-package sweep is clean) but are NOT executed in CI, to bound wall-clock — the initial
enforcement scope. Most public examples are the canonical indented RST **literal block** (the honest
form for anything needing a real match's `actions`/`frames`/`xt` — no docstring can conjure them);
`tests/test_public_api_examples.py::_has_real_example` accepts a ≥4-space-indented non-`>>>` line in
the Examples section as a REAL example, so converting a failing `>>> f(actions, ...)` doctest to a
literal block satisfies BOTH the doctest run (nothing to execute) and the Examples gate. Genuinely
self-contained examples stay executable `>>>` doctests. Do NOT reintroduce `>>> f(x)  # doctest: +SKIP`
filler — the gate's `_demonstrates_something` rejects it (the 4.53.0 tightening).

**`@pytest.mark.slow` = expensive AND platform/interpreter-INVARIANT** (does-it-run train-script
smokes, same-run internal-consistency/parity, calibration cache-equivalence). These run **once on the
CI primary leg (`ubuntu-3.12`, matrix `primary: true`)**; every other leg runs `-m "not e2e and not
slow"`. Do **NOT** mark version-sensitive tests `slow` (golden-hash / snapshot / absolute-numeric — e.g.
`test_golden_*`) — they must stay on all legs (OS + interpreter axes), as must cheap behavioral-contract
guards (dup-`action_id`, id-dtype-invariance, orientation/roster). The matrix partition is guarded by
`tests/test_ci_slow_gating_wired.py`. Decision: ADR-023.

**Claims about a gate's behaviour must quote the assertion body, not its registration.**
A gate's registry (`ENTRIES`, `PURITY_ENTRIES`, `AGGREGATORS`, `_PUBLIC_MODULE_FILES`,
`_EXHAUSTIVE_EMITTED`) tells you *whether* a helper is exercised — never *what is asserted
about it*. Two PR-S119 review errors came from this, one on each side: a reviewer read the
liveness gate's registration and concluded a partially-`pd.NA` column would fail, when
`tests/tracking/test_aggregator_column_liveness.py:416` asserts only
`not out[c].notna().any()` (100%-null), and `:448`'s non-constant check is
float-dtype-gated so non-numeric columns are exempt entirely; and a plan asserted the
synthetic reachability grid was exactly y-symmetric and that symmetrizing it would cost
"≤ 1 ulp", when neither had been run (measured: 4.44e-16 across 3752/6400 cells). Same
failure mode both times — adjacent evidence substituted for the executed thing. **Rule: any
claim that a test passes or fails, or that a numeric property holds, carries either the
quoted assertion body or a pasted measurement.** Registration proves coverage; only the
assertion proves behaviour. Corollary for tolerances: measure at the scale you assert at —
grid-level float noise does not transfer to a derived area (a 3.3e-16 relative grid
asymmetry is not a 1e-12 absolute bound on an m² quantity).

## Open Items

See [TODO.md](TODO.md) for tracked work.

## Dependencies

- Runtime: pandas, numpy, scipy, scikit-learn (no pandera, no multimethod)
- Optional: kloppy (>= 3.18 for tracking parsers), xgboost, catboost, lightgbm, accessible-space (for DAS features), ruthless-efficiency[optuna]>=0.4.0 + xgboost>=2.0,<4.0 (for the `[calibration]` harness AND the `[train]` extra — shared trained-model HPO substrate; the ruthless floor was RAISED to 0.4.0 by ADR-052 because `ruthless.fingerprint` -- the structural, fail-closed digest `scripts/_driver.py` keys shard generations on -- only became public in 0.4.0, and the old 0.2.1 floor permitted a resolve where `from ruthless import fingerprint` raises ImportError (measured: 73 test failures on a stale `.venv312`); it also fixes a `warm_start` n_trials off-by-one, the xgboost bound protects the 1e-9 cache-equivalence gate's `tree_method="hist"` determinism). xS inference gates on the `[xgboost]` extra. numba (>=0.59) accelerates the pitch-control `@njit` kernels (ADR-008) and the ghost-GK `cpu-numba` KDE backend (ADR-013) via the `[numba]` extra (also bundled in `[test]` so CI exercises the `@njit` parity). `statsbombpy` is an OPTIONAL, `importorskip`-guarded dep used only by the network-gated xT open-data e2e (`tests/test_xthreat_statsbomb_e2e.py`, `@pytest.mark.e2e`); not in any committed extra — the test self-skips without it.
- numpy>=2.0 compatible
