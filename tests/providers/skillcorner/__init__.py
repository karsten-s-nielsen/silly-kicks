# Package marker so this ``test_appearances.py`` gets a namespaced module name
# (``skillcorner.test_appearances``) and does NOT collide with the other providers'
# same-basename test modules under pytest's default prepend import mode (see the Task-6
# sportec fix -- ``tests/providers/sportec/__init__.py``).
