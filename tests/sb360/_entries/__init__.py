"""Per-family SB360 registry entries. Mirrors tests/tracking/_mirror_entries/.

Deliberately empty until the audit runs: the surface gate lands RED so the enumeration is
observed failing before any verdict exists (ADR-051).
"""
